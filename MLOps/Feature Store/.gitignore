# Per-series cleanup parameters written by each Environment notebook.
# Holds project-specific resource names for cleanup.py — not committed.
*/cleanup.json

# Terraform artifacts generated by Production Deployment notebooks:
# the downloaded terraform binary (~117 MB) and the working dirs
# (provider plugins + state — hundreds of MB, machine-specific).
*/terraform
*/terraform-spanner/
*/terraform-*/
*/.terraform/
*/*.tfstate
*/*.tfstate.*
*/.terraform.lock.hcl
