"""Skill Registry integration — publish packaged skill bundles to the platform.

`bundles.py` is the offline, unit-tested core (discover bundles, parse SKILL.md
front matter); `registry.py` is the thin CLI that publishes them through the
platform Skill Registry (`client.skills`).
"""
