# Agent Skills — Plan

Operating manual for this meta-project: the authoring standard, the backlog, and the audit log. Mirrors the `README.md` + `PLANS.md` + `RESOURCES.md` convention already used in `data+ai/bq-ml` and `data+ai/bq-ai-functions`.

## Vision

Package this repo's tested, verified content as Agent Skills usable from any tool that reads the `SKILL.md` convention (Claude Code, Antigravity, Codex, and others) — not just the two BigQuery projects that started this, but eventually the broader MLOps content across the repo (`MLOps/`, `Applied GenAI/`, etc.). Skills should be genuinely self-contained (usable standalone, outside this repo) and dependable to keep in sync as the source content evolves.

## Authoring standard

A new skill is a directory under `.agents/skills/<name>/`:

1. **`SKILL.md`** — frontmatter (`name` ≤64 chars lowercase/digits/hyphens, no "claude"/"anthropic"; `description` ≤1024 chars, this drives auto-matching so write it carefully) + a decision tree for the domain + cross-cutting gotchas that apply regardless of the specific choice + pointers to `reference/*.md`. Keep under ~500 lines.
2. **`reference/<bucket>.md`** — one file per use-case bucket (organize by what the user is trying to do, not by raw catalog order). Each: an options table, how to choose among them, verified gotchas (prioritize ones with concrete evidence — exact numbers, exact error messages — over generic advice a model already knows), a canonical snippet, and a "Go deeper" section with plain repo-relative path mentions (not clickable links — see README.md's "Skill anatomy"). Keep each under ~100 lines or add a table of contents.
3. **`narrative/<topic>.md`** — extracted markdown+code narrative from one flagship notebook per reference bucket, via `tooling/`'s `convert-notebook` command. Curate — one illustrative notebook per bucket, not every notebook in the project.
4. **`skill.manifest.json`** — generate via `tooling/`'s `manifest` command after any content change.
5. Run `agent-skills validate <skill-dir>` before considering a skill (or a change to one) done.

Content should be distilled and maintained, not duplicated — the source `RESOURCES.md`/notebooks remain the authoritative exhaustive detail; a skill's files are the deliberately-curated summary an agent actually needs.

## Change types and checklists

#### New skill
- [ ] Scaffold `.agents/skills/<name>/` per the authoring standard above.
- [ ] Wire the two repo-root symlinks: `.claude/skills/<name>` and `.agents/skills/<name>`, both → `agent-skills/.agents/skills/<name>`.
- [ ] Run `agent-skills validate` and fix anything flagged.
- [ ] Generate the manifest.
- [ ] Add a row to this file's "Current skills" list (README.md) and an audit-log entry below.
- [ ] If the skill documents a project with its own `PLANS.md` (e.g. `bq-ml`, `bq-ai-functions`), add the skill-maintenance checklist line there (see those files' "Change types and checklists" sections) so future content changes prompt a skill update.

#### Skill content update (source project added a model/function/workflow, a new gotcha, a new head-to-head comparison)
- [ ] Update the relevant `reference/<bucket>.md` and/or `SKILL.md` cross-cutting section.
- [ ] Re-run `agent-skills validate`.
- [ ] Bump the manifest (`agent-skills manifest <skill-dir>`).
- [ ] Audit-log entry.

#### Tooling change (`convert_notebook.py`, `validate.py`, `manifest.py`, the `check-*` commands)
- [ ] Update `tooling/src/agent_skills_tooling/`.
- [ ] Re-run `uv sync` and re-validate all skills to confirm nothing regressed.
- [ ] **Prove a new check fails, not just that it passes.** Break the thing it exists to
      catch — an orphan page, a renamed heading, an edited narrative — confirm the error,
      then restore. A check that has only ever been seen green is not known to work.
- [ ] Audit-log entry.

#### Checks the source projects depend on
`check-links`, `check-reference` and `check-narratives` are required before every commit
to `data+ai/bq-ml` and `data+ai/bq-ai-functions` (their `PLANS.md` files carry the
commands and the policy). Changing one of them changes those projects' definition of
"clean", so re-run all three against both projects in the same pass.

## Backlog / phases

**Phase 1 — local foundation (in progress)**: centralize skills in `agent-skills/`, notebook-narrative extraction, validation/manifest tooling. See the three current skills.

**Phase 2 — external hub + sync (not started, needs explicit go-ahead before creating external resources)**: a dedicated hub repo mirroring `.agents/skills/`, a GitHub Action that syncs on push to `main`, pre-push validation against the mirrored output.

**Phase 3 — distribution spokes (not started)**: the hub's `.claude-plugin/marketplace.json` (with the `dependencies` field for skills like the triage skill), submission to community catalogs (VoltAgent's `awesome-agent-skills`, the Antigravity-specific list, `skills.sh`), and a PyPI/`uv`-installable CLI for end users.

**Future skills** (candidates from this repo's broader content, not yet scoped): TBD — revisit once phase 1 is fully proven on the two BigQuery projects.

## Audit log

- **2026-07-29** — Phase 1 kickoff: scaffolded `agent-skills/`, built `tooling/` (`convert_notebook.py`, `validate.py`, `manifest.py`, CLI), proved the notebook-narrative extraction on `models/logistic_regression/logistic_regression.ipynb`.
- **2026-09-04** — Three checks added, one extended, after `RESOURCES.md` was split into `reference/` pages in both source projects. `check-reference` verifies a project's `RESOURCES.md` index and its `reference/` pages agree (no orphan page, no dead index entry, one H1 per page, filename matching that H1's slug) — the index became load-bearing the moment it stopped holding the content, and a page it does not list is a page nobody finds. `check-narratives` regenerates every `narrative/*.md` and diffs it against the committed copy, which is the regenerate-and-diff audit that had been run by hand; it finds each skill's notebooks through a new `source_project` field in `skill.manifest.json`, so a skill without that field is skipped rather than guessed at. `check-links` gained **anchor resolution** — a link to `page.md#heading` still resolves to the file after the heading is renamed, so nothing else notices — and the `PLANS.md` exemption was narrowed to the *target* policy, since a citation pointing at a heading that does not exist is broken wherever it lives. Anchors are computed by a shared `markdown_anchors.py` implementing GitHub's slug algorithm, fence-aware because a `#` inside a fenced block is a comment and treating it as a heading invents anchors that do not exist; validated against all 141 real headings across both projects with zero collisions, and it reads `.ipynb` markdown cells as well as `.md`, without which notebook self-anchors read as broken. **Every check was proved to fail before being trusted** — an orphan page, a page renamed out of sync with its title, a broken anchor and an edited narrative each produced the expected error and a clean result on restore. Both BigQuery manifests carry `source_project` and sit at 0.3.0; `choosing-a-bigquery-ai-approach` has no narratives and is skipped. Post-state across both projects: 0 link violations, 0 reference errors, 94/94 narratives current, `validate --all` OK on all three skills.
- **2026-09-04** — Anchor slugs verified against GitHub's own renderer, and one real defect found by doing it. The slug algorithm had only ever been checked for self-consistency — unique slugs, links that resolve — which cannot catch being uniformly wrong. Ground truth: POST each file to `api.github.com/markdown` and diff the `user-content-` ids it emits against `slugify` output. **All 14 reference and index pages match exactly, 132 anchors, zero differences.** The defect is in the 15th file. `bq-ai-functions/setup/README.md` repeats "When do you need one?" and "How to create one" four times each; GitHub keeps anchors unique by suffixing repeats in document order (`-1`, `-2`, `-3`), and `anchors_of` was returning a set of bare slugs, so a perfectly good link to `#when-do-you-need-one-2` would have been reported broken. Latent, not live — nothing links that way today — but a false positive is how a check stops being trusted, and it sat in the one file most likely to be linked into. Fixed by carrying the repeat count across the whole document, cells included, since GitHub renders a notebook as one page; that file's 54 anchors now match GitHub exactly, a `-3` link resolves and a `-9` link fails. **The lesson is the method:** a checker that models someone else's behavior has to be tested against that behavior, not against itself — the vendor's renderer was one HTTP call away the whole time. Suite unchanged: 0 link violations, 0 reference errors, 94/94 narratives current, `validate --all` OK.
- **2026-09-11** — `bigquery-ml` gained point-in-time feature retrieval, from `bq-ml`'s new `functions/feature_store/` and `workflows/feature_store/`. `reference/preprocessing-functions.md` picked up a category subsection for `ML.FEATURES_AT_TIME` / `ML.ENTITY_FEATURES_AT_TIME`, seven gotchas, and a canonical snippet; `reference/workflows-and-pipelines.md` gained the `feature_store` workflow row plus two verified gotchas (a "latest feature row" join leaks the future with no error and a *better* offline metric — `roc_auc` 0.9387 leaky vs. 0.5194 point-in-time-correct, the leaky model falling to 0.5299 once scored on correct features; and a two-arm comparison needs one shared deterministic split, not a per-arm `RANDOM` one). The existing query-cache-after-retrain gotcha was widened from orchestrators to plain notebooks, verified again here. `SKILL.md` got a seventh routing branch, two cross-cutting gotchas, and trigger words in its description. Two narratives generated; `ensembling.md` and `regression_based_forecasting.md` regenerated after their notebooks' cross-link edits — exactly the markdown-only drift `check-narratives` exists to catch. Skill at 0.3.4.
- **2026-09-11** — `check-narratives` no longer resolves an ambiguous narrative by sort order. Narratives are matched to notebooks by filename stem, which held until `bq-ml` shipped both `functions/feature_store/` and `workflows/feature_store/` — two notebooks named `feature_store.ipynb` in one project. The old `_find_notebook` returned the first `rglob` match, so `narrative/feature_store.md` would have been diffed against the *functions* notebook forever and the workflow narrative reported as having no source at all. Silent wrong-source diffing is worse than the miss: it reports a genuinely stale file as current. Now an ambiguous stem is an **error** naming both candidates, resolved by a new optional `narrative_sources` map in `skill.manifest.json` (narrative filename → project-relative notebook), which `manifest.py` preserves across regeneration the same way it preserves `source_project`. Proved by removing the map and re-running: the ambiguity error and the missing-source error both fire, and both clear on restore. `bigquery-ml` at 0.3.4, 60 narratives; suite green — 0 link violations, 0 reference errors, 99/99 narratives current, `validate --all` OK on all three skills.
- **2026-09-11** — `bigquery-ml` gained the image-preprocessing family, from `bq-ml`'s new `functions/image/`. `reference/preprocessing-functions.md`'s image subsection was rewritten around a fact no official page states — **all four functions require a BigQuery Editions reservation** and fail outright on on-demand pricing — plus the corrected field names (`dimensions`/`values`, not `shape`), the channel rule (decode always returns 3, `'GRAYSCALE'` returns 1, so the last function in the chain sets it), both closed ranges, the two lumas, the float32/half-to-even resize behavior, and the deterministic `80038528` failure whose surfaced exception class is not stable. `SKILL.md`'s routing branch and cross-cutting gotchas now carry the reservation warning alongside `MATRIX_FACTORIZATION`. One narrative generated (`image.md`); skill at 0.3.5, 61 narratives. Reciprocity in the same commit: `bq-ai-functions/reference/unstructured-data-infrastructure.md` and the `bigquery-ai-functions` skill's `document-processing.md` both now route the four functions out to the sibling and record that the reservation requirement holds for **any** input, not just object tables — a `FROM_BASE64` literal fails identically. `bigquery-ai-functions` at 0.3.3.
- **2026-09-11** — The link regex rewrote prose as if it were a link, and it had already corrupted a shipped narrative. `convert_notebook.py`'s `\[([^\]]+)\]\(([^)]+)\)` excludes `]` but not newlines, so an **unpaired `[` in ordinary prose** — a half-open interval such as `[0, 1)`, which is exactly what the image and causal-effect notebooks write — lets the match run forward to the next `](...)` anywhere later in the document and replace everything between with a rewritten link. In `narrative/causal_effect.md` that had turned ``FLOAT64` in `[0, 1)`` into ``FLOAT64` in ``workflows/difference_in_differences` (0, 1)`` and swallowed a sentence ten lines below it. Nothing caught it: the corrupted output contains no newline-spanning match, so scanning the committed files finds nothing, and `check-narratives` only compares against the same buggy generator. Fixed by excluding newlines from both halves in all five link regexes across the tooling (`convert_notebook`, `validate`, `link_policy`, `reference_structure`, `markdown_anchors`) — link targets never contain one, and link text spanning a line is not worth the failure mode. `causal_effect.md` regenerated; `check-narratives` went from `STALE` to current, which is the check finally reporting the defect it could not see before. Suite green — 0 link violations in both projects (129 and 82 files), 0 reference errors (5 and 7 pages), 100/100 narratives current, `validate --all` OK on all three skills.
- **2026-09-11** — `bigquery-ai-functions` gained the Cloud AI service models, a **third branch** the collection did not previously have a shape for: five functions that reach a pre-trained Google Cloud AI service through `OPTIONS(REMOTE_SERVICE_TYPE = '...')` — you train nothing, but you also do not prompt anything. New `reference/cloud-ai-service-models.md` carries the five-member options table, the choose-against-`AI.*` argument, and the measured gotchas: the model object is a handle (`ML.EVALUATE` rejected, absent from `INFORMATION_SCHEMA`, `CREATE MODEL` succeeding proves nothing about permissions), failures arrive **per row while the job succeeds**, the service-specific IAM role is not uniform and **two of the five have none** (Vision and Natural Language are gated by `serviceusage.services.use` alone), `ML.TRANSCRIBE` breaks the family's argument convention and is the one member that is not reproducible, and `ML.ANNOTATE_IMAGE` *accepts* feature names it silently returns nothing for. Four narratives generated; `ml_process_document.md` regenerated for its notebook's new sibling table and corrected grant loop. `document-processing.md` gained a header note placing `ML.PROCESS_DOCUMENT` as the fifth member. Skill at 0.4.0, 43 narratives, 8 reference pages. **Reciprocity in the same commit, both directions:** `bigquery-ml`'s `SKILL.md` and `reference/model-management.md` now open the `REMOTE WITH CONNECTION` fork explicitly — `endpoint =` is your hourly Vertex AI Endpoint consumed with `ML.PREDICT`, `REMOTE_SERVICE_TYPE =` is the sibling's five — and `narrative/remote.md` was regenerated for the notebook cell that says the same thing (0.3.6, 61 narratives). `choosing-a-bigquery-ai-approach` gained a tenth triage question and a head-to-head bullet, since "pre-trained service" was previously unreachable from a skill whose whole frame is trained-vs-generative (0.2.0). Suite green — 0 link violations (91 and 129 files), 0 reference errors (8 and 5 pages), 104/104 narratives current, `validate --all` OK on all three skills.
