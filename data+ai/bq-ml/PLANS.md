<!--
This file is the operating manual for the bq-ml project. A fresh Claude session
should be able to read this file top-to-bottom and know how to add a new model,
function, workflow, or pipeline using the same conventions as every existing one.
It mirrors the structure of the sibling project data+ai/bq-ai-functions/PLANS.md.
-->

# BigQuery ML — Project Plan

## Vision

A hands-on, learn-by-running collection for **BigQuery ML**: training and using machine learning models entirely in SQL with `CREATE MODEL` and the `ML.*` functions. The audience is data practitioners who know SQL and want to do ML where their data already lives — no data movement, no separate training infrastructure.

This is the sibling project to **`../bq-ai-functions/`** (the BigQuery AI/Gemini functions). Where that project is organized **per function** (each function is independent), BigQuery ML is organized around a **model lifecycle**, so the primary unit of content is the **model type**.

Goals:
- One runnable, progressive example per model type, covering the full lifecycle
- Clear coverage of the model-free `ML.*` utility functions (preprocessing, transforms, distance, text)
- End-to-end workflows that compose these into real tasks
- Operationalization patterns (MLOps on BigQuery) via pipelines
- Reference docs (RESOURCES.md) and a map (README.md) kept in sync with the official documentation

---

## Content Architecture

Four top-level content directories. The **unit of content differs by directory** — this is the key design decision.

### 1. `models/` — Per-model-type lifecycle deep dives

One folder per model type. Each folder has `{name}.ipynb` + `{name}.sql`. The notebook walks the **full lifecycle**:

`CREATE MODEL` → `ML.EVALUATE` → (`ML.CONFUSION_MATRIX` / `ML.ROC_CURVE` / etc.) → `ML.PREDICT` (or `ML.FORECAST` / `ML.RECOMMEND`) → `ML.EXPLAIN_PREDICT` / `ML.GLOBAL_EXPLAIN` → `ML.FEATURE_INFO` / `ML.TRAINING_INFO` → hyperparameter tuning (`ML.TRIAL_INFO`)

Model **categories** (imported, remote, export) also live under `models/` because they are variations on how a model is created/stored, not separate concepts.

### 2. `functions/` — Model-free `ML.*` utilities

One folder per function, `{name}.ipynb` + `{name}.sql` — exactly the per-function style used in `bq-ai-functions/`. These are the `ML.*` functions that operate **directly on data without a model** (preprocessing, transforms, distance, text vectorization).

### 3. `workflows/` — Composed end-to-end SQL logic

One notebook per workflow. Combines preprocessing functions + a model lifecycle into a real task (e.g. churn classification end to end). **Workflows = what to do, in SQL (logic).**

### 4. `pipelines/` — Orchestration / MLOps

One folder per orchestration approach. Takes the SQL logic of a workflow and **operationalizes/schedules** it. **Pipelines = how to run it in production (orchestration).** All 8 built (Phase 8 complete):
- `sql_scripting/` — multi-statement scripts, `BEGIN/END`, no external orchestrator
- `scheduled_queries/` — BigQuery scheduled queries (Data Transfer API) for retrain/score
- `dataform/` — version-controlled SQL pipeline via `google.cloud.dataform_v1`
- `cloud_workflows/` — Cloud Workflows YAML + the BigQuery connector
- `dbt/` — `dbt-core`/`dbt-bigquery` via `dbtRunner`, a custom `CREATE MODEL` materialization
- `vertex_kfp/` — Vertex AI Pipelines (KFP), official prebuilt BQML components
- `composer_airflow/` — Cloud Composer 3 / Airflow DAGs (`BigQueryInsertJobOperator`)
- `airflow_with_kfp/` — Airflow (`RunPipelineJobOperator`) triggering the `vertex_kfp/` pipeline, sharing one Composer 3 environment with `composer_airflow/`

### Supporting files
- `README.md` — the landing-page map (tables + diagram + tree)
- `RESOURCES.md` — the deep per-item reference
- `setup/README.md` — one-stop setup reference (connections, IAM, CREATE MODEL deep dive, quotas, BigFrames)
- `overview.ipynb` — a short interactive tour (one example per category), grown as content is added

---

## Component Details

### Model `.sql` file (in `models/{name}/`)

Progressive, runnable SQL covering the lifecycle. Conventions:
- Header comment block: title, one-line description, the lifecycle order, the dataset used, and doc links
- Each example has a `-- ===` delimiter and a comment header explaining what it demonstrates
- Use placeholders `PROJECT_ID.DATASET` (notebooks substitute real values)
- Examples build up: create → evaluate → inspect → predict → explain → TRANSFORM → tune → cleanup (commented `DROP MODEL`)

### Model `.ipynb` notebook (in `models/{name}/`)

Cell sequence (canonical skeleton — see `models/logistic_regression/` as the reference):
1. **Header** — tracking-pixel + links table (cell 0). Copy from an existing notebook and replace the path/file segments (`bq-ml`, `models/{name}`, `{name}.ipynb`). Path is URL-encoded: `data%2Bai%2Fbq-ml%2Fmodels%2F{name}`.
2. **Overview** — title, a **Lifecycle:** line (the function chain), when-to-use bullets, the dataset, and a **References:** line. Add a **Featured in:** line if any workflow/pipeline uses the model.
3. **Setup** — markdown + config code (PROJECT_ID, LOCATION, DATASET_ID=`bq_ml`) + environment markdown + install code + auth code + client code (creates dataset, loads `bigquery_magics`).
4. **Step N — {lifecycle stage}** — one markdown + code pair per stage. Materialize models with `CREATE OR REPLACE MODEL`; show metrics, predictions, explanations, plots.
5. **Examples — `%%bigquery` Magics** — one representative call.
6. **Examples — BigFrames** — the `bigframes.ml` equivalent (these exist for most model types — e.g. `bigframes.ml.linear_model.LogisticRegression`). **Check parity with the SQL model's options before writing this cell** (e.g. class weighting) — don't assume the BigFrames constructor exposes every `CREATE MODEL` option; verify against the actual installed signature (`inspect.signature(...)`) rather than guessing a parameter name. If no equivalent exists, say so in the markdown rather than silently presenting a mismatched comparison (see `models/boosted_tree_classifier/`: `bigframes.ml.ensemble.XGBClassifier` has no `class_weight` param, unlike `LogisticRegression`).
7. **Cleanup** — drop the models created; a commented-out full-dataset-delete cell. If the notebook exported anything to GCS (tree visualization, etc.), delete those blobs too.

**Convention (decided 2026-06-20, `models/boosted_tree_classifier/`) — visualizing tree-ensemble models:** for any `BOOSTED_TREE_*` / `RANDOM_FOREST_*` notebook, add a step that exports the model (`EXPORT MODEL`) and renders an individual tree with `xgboost.plot_tree()`. Requires a `BUCKET` config variable (alongside `PROJECT_ID`/`LOCATION`/`DATASET_ID`) in the same location as the dataset. Two verified gotchas that MUST be called out in the notebook, not just assumed away:
1. **Pin `xgboost==1.7.6`** (or another pre-2.0 release) in that step's `install()` call — modern xgboost (2.0+) cannot load BQML's exported legacy-binary-format `model.bst`.
2. **Manually reassign `booster.feature_names`** to the training query's non-label `SELECT` column order — the export does not preserve them (`feature_names` loads as `None`).

Rendering also needs the system `graphviz` package (`dot` binary) — pre-installed in Colab; elsewhere `!apt-get install -y graphviz`. Full details in RESOURCES.md's `EXPORT MODEL` entry.

**`RANDOM_FOREST_*`-specific addendum (decided 2026-06-21, `models/random_forest_classifier/`):** do NOT try to render a tree from the notebook's main (full-power) model — verified that a default-settings forest's tree is far too dense (thousands of dump lines, depth 15+) and produces an illegible `xgboost.plot_tree()` image (confirmed even with SVG output). Instead, train a small, separate **illustrative forest** (e.g. `num_parallel_tree=10`, `max_tree_depth=3`) just for the visualization step, clearly labeled as such in the markdown, and drop it in cleanup alongside the other models. This does not apply to `BOOSTED_TREE_*` — its early-round trees are shallow (residual-fitting stages) and render cleanly by default.

### Model-free function `.sql` / `.ipynb` (in `functions/{name}/`)

Same template as `bq-ai-functions/` function notebooks: header → overview (with **Featured in:**) → setup → progressive SQL examples → magics → BigFrames → cleanup.

### Workflow `.ipynb` (in `workflows/{name}/`)

Header → overview (with **Models used:** and **Functions used:** lines) → setup → Step-by-step (data → preprocess → train → evaluate → predict/explain) → interpretation → cleanup.

### Pipeline content (in `pipelines/{approach}/`)

A notebook and/or supporting files (DAG `.py`, KFP component specs) showing how to operationalize a workflow with that orchestrator. Include a **Workflow operationalized:** line linking back to the `workflows/` notebook it schedules.

---

## Sample Data Strategy

Examples must be self-contained and runnable. Prefer **BigQuery public datasets** — no data to load, realistic results.

- **Classification / regression:** `bigquery-public-data.ml_datasets.census_adult_income` (binary label `income_bracket`), `bigquery-public-data.ml_datasets.penguins` (multiclass/regression), `iris`.
- **Clustering:** `penguins`, or any feature-rich public table.
- **Time series:** `bigquery-public-data.new_york_citibike.citibike_trips`, `bigquery-public-data.google_analytics_sample.*`, or synthetic series generated with SQL (`GENERATE_DATE_ARRAY` + trend/seasonality). Cross-link to the TimesFM-based `AI.FORECAST` work in `bq-ai-functions/`.
- **Recommendation:** `bigquery-public-data.google_analytics_sample` or MovieLens-style public data.
- **Causal inference (treatment-effect estimation, not prediction):** `bigquery-public-data.samples.natality` — real (non-synthetic) US birth-certificate data with a genuine, verifiable treatment/confounder/outcome structure (`cigarette_use` as treatment). Verify any causal-inference dataset candidate live before committing — synthetic datasets (tried: `thelook_ecommerce`/GA4 marketing channels, `cms_synthetic_patient_data_omop` drug comparisons) frequently don't bake in the real-world confounding/effect relationships their schema suggests they should.
- **Survival analysis (time-to-event, censoring):** `bigquery-public-data.ga4_obfuscated_sample_ecommerce` — reuses `ga4_churn_prediction`'s exact cohort/first-week features, verified live to carry a strong, monotonic covariate-hazard relationship (activity level → time-to-purchase). `thelook_ecommerce` was checked first (repeat-purchase rate / time-to-second-order by traffic source, gender/age band, first-order size) and again showed flat rates across every segment — the same "synthetic data doesn't bake in real covariate-outcome relationships" lesson as the causal-inference row above, now confirmed a second time on a second technique.
- **Cross-sectional causal ML (price elasticity via Double ML):** `bigquery-public-data.iowa_liquor_sales.sales` — real cross-brand price variation within a category (state-set retail pricing rules out a time-series design, but genuinely varies across ~1,130 real SKUs), with a real, verified confounder (distribution breadth) to correct for. `thelook_ecommerce`'s `sale_price` was checked and found fixed per product (no variation at all) before landing on this design.
- **Uplift / CATE (heterogeneous treatment effects):** `bigquery-public-data.google_analytics_sample` (the older Universal Analytics/GA360 export, distinct from `ga4_obfuscated_sample_ecommerce`) — a real observational treatment (paid marketing vs. organic search) with verified real heterogeneity across segments (desktop uplift ~10x tablet's), not just a flat average effect. `thelook_ecommerce`'s `traffic_source` field was checked with a fresh treatment definition and again showed flat conversion across every channel.
- **Panel/geo causal inference (DiD, synthetic control):** `bigquery-public-data.covid19_open_data.covid19_open_data` — real US state-level policy (`facial_coverings`) and case-rate data, 2020; a real single-date policy change (Texas's mask mandate) with a real untreated comparison state (Georgia) and a real multi-state staggered-adoption panel for testing modern DiD estimators. **Live-data caveat**: this is a still-updated public dataset — historical rows can shift between a scratch validation check and a notebook's final run (confirmed directly: a naive TWFE regression's sign changed between two checks of the identical query). Re-verify key numbers close to shipping, and expect (and disclose) some drift on the user's own run.

**Standing practice (carried over from bq-ai-functions):** validate every SQL statement with `bq query` against the real dataset **before** writing it into a notebook cell. Training models in validation is fine (drop them afterward). This catches issues like option requirements (e.g. `enable_global_explain` must be set at `CREATE MODEL` time for `ML.GLOBAL_EXPLAIN`) before a notebook is handed off for Restart & Run All.

---

## Resource Naming Strategy

All notebooks share a single dataset and use `CREATE OR REPLACE` so running them in any order is safe.

### Config variables (every notebook)
```python
PROJECT_ID = 'your-project-id'
LOCATION = 'US'
DATASET_ID = 'bq_ml'          # Shared dataset (configurable, default: bq_ml)
CONNECTION_ID = 'bq_ml'        # Only notebooks that need one (remote/imported models)
BUCKET = 'your-bucket-name'    # Only notebooks that use EXPORT MODEL (e.g. tree visualization) -- same location as DATASET_ID
```
Most BQML model types train on in-BigQuery data and need **no connection**. A connection is only required for **remote models** (Vertex endpoints) and **imported models** (read artifacts from GCS). Those notebooks add `CONNECTION_ID` and create the connection via the `bq` CLI (same pattern as bq-ai-functions, granting the connection SA the needed roles).

**Verified: `EXPORT MODEL` does NOT need a connection** — it only needs GCS write IAM on the credentials running the query (confirmed by testing an export against a `US-CENTRAL1` bucket from a `US` multi-region dataset with no connection object at all). Notebooks that call `EXPORT MODEL` (e.g. tree visualization) add `BUCKET` instead of `CONNECTION_ID`.

### Naming
- **Models / tables:** prefix with the content name, e.g. `logistic_regression_income`, `logistic_regression_income_tuned`, `kmeans_penguins`.
- **GCS (export/import):** `gs://BUCKET/bq_ml/{name}/`.
- All `CREATE OR REPLACE` — idempotent and safe to re-run.

### Cleanup
- **Per-notebook:** drop only the models/tables it created (`DROP MODEL IF EXISTS ...`). Leave the shared dataset.
- **Full:** a commented-out cell at the bottom of every notebook: `DROP SCHEMA \`PROJECT.bq_ml\` CASCADE;` (or `client.delete_dataset(..., delete_contents=True)`).

---

## Cross-Referencing Convention

Bidirectional links help users navigate. Maintain these whenever adding/updating content.

- **Model / function notebooks → consumers:** a **Featured in:** line in the overview cell listing the workflows/pipelines that use it.
- **Workflow / pipeline notebooks → components:** **Models used:** and **Functions used:** lines in the overview cell.
- **Mapping table** (below) records the current state.

### Current mapping

| Content | Type | Featured in |
|---------|------|-------------|
| logistic_regression | model | — |
| linear_regression | model | `workflows/regression_based_forecasting/` |
| boosted_tree_classifier | model | — |
| boosted_tree_regressor | model | `workflows/regression_based_forecasting/` |
| random_forest_classifier | model | — |
| random_forest_regressor | model | — |
| arima_plus | model | `workflows/hierarchical_forecasting/` (top-down comparison; the bottom-up option itself is covered in `arima_plus` Step 10) |
| boosted_tree_classifier | model | `workflows/embeddings_classification/`, `workflows/churn_retention/`, `workflows/ga4_churn_prediction/` |
| regression_based_forecasting | workflow | — (uses `linear_regression`, `boosted_tree_regressor`; compares against `arima_plus`) |
| hierarchical_forecasting | workflow | — (uses `arima_plus`; compares built-in bottom-up vs. custom top-down) |
| embeddings_classification | workflow | — (uses `boosted_tree_classifier`) |
| ga4_churn_prediction | workflow | `pipelines/sql_scripting/`, `pipelines/scheduled_queries/`, `pipelines/dataform/`, `pipelines/cloud_workflows/`, `pipelines/dbt/`, `pipelines/vertex_kfp/`, `pipelines/composer_airflow/`, `pipelines/airflow_with_kfp/` — the one workflow all 8 Phase 8 pipelines operationalize (uses `boosted_tree_classifier`; complements `churn_retention`) |
| transform_only | model | — |
| imported | model | — |
| export | model | `remote` (picks up where `export` leaves off) |
| remote | model | — (uses `export`'s pattern; deploys to a Vertex AI Endpoint) |
| sql_scripting | pipeline | — (operationalizes `ga4_churn_prediction`) |
| scheduled_queries | pipeline | — (schedules `sql_scripting`'s script unmodified) |
| dataform | pipeline | — (operationalizes `ga4_churn_prediction`) |
| cloud_workflows | pipeline | — (re-expresses `sql_scripting`'s drift-check/retrain logic externally) |
| dbt | pipeline | — (operationalizes `ga4_churn_prediction`) |
| vertex_kfp | pipeline | `airflow_with_kfp` (triggers this pipeline's compiled spec) |
| composer_airflow | pipeline | — (operationalizes `ga4_churn_prediction`; shares a Composer 3 environment with `airflow_with_kfp`) |
| airflow_with_kfp | pipeline | — (triggers `vertex_kfp`; shares a Composer 3 environment with `composer_airflow`) |
| propensity_score_matching | workflow | — (uses `logistic_regression`'s `LOGISTIC_REG` mechanics for propensity scoring, not prediction; this project's first causal-inference, not predictive, workflow) |
| survival_analysis | workflow | — (uses `logistic_regression`'s `LOGISTIC_REG` mechanics as a discrete-time hazard model, not prediction; reuses `ga4_churn_prediction`'s cohort; also bridges to `lifelines.CoxPHFitter` outside BigQuery for the one technique BQML can't do natively) |
| price_elasticity_dml | workflow | — (uses `boosted_tree_regressor`'s mechanics as DML nuisance models, not prediction; reuses `cross_validation`'s fold-assignment pattern for cross-fitting instead of evaluation) |
| uplift_cate | workflow | — (uses `boosted_tree_classifier` mechanics twice, once per T-learner arm; complements `propensity_score_matching`'s average-effect estimate with a heterogeneous/individual one) |
| difference_in_differences | workflow | `synthetic_control` (shares its panel and donor pool; DiD picks one comparison state, synthetic control replaces it with a weighted blend) |
| synthetic_control | workflow | — (extends `difference_in_differences`'s panel; uses `linear_regression` mechanics for the native unconstrained approximation, `scipy.optimize`/`pysyncon` outside BigQuery for the real constrained fit) |

(Update this table on every add. Note: this table predates several phases and is not exhaustive for every workflow/function — e.g. `customer_segmentation`, `recommendation`, `anomaly_fraud_detection`, `cross_validation`, `ensembling`, and the `functions/` entries aren't listed. Phase 8's pipeline rows above are complete as of Phase 8's close; a full backfill of the rest is a separate, not-yet-scheduled cleanup.)

---

## Development Phases

This is the **backlog** — a fresh session can pick the next unchecked item and build it using the templates above.

### Phase 1: Foundation (this bootstrap)
- [x] Folder skeleton (`models/`, `functions/`, `workflows/`, `pipelines/`, `setup/`)
- [x] PLANS.md (this operating manual)
- [x] RESOURCES.md (CREATE MODEL + lifecycle entries; full for logistic regression, stubs + doc links for the rest)
- [x] README.md + setup/README.md
- [x] **Canonical model: `models/logistic_regression/`** (full lifecycle, verified)
- [x] overview.ipynb stub
- [x] Memory entries

### Phase 2: Core supervised models

**Convention (decided 2026-06-20):** when a technique pairs a classifier and a regressor `model_type` (boosted tree, random forest, DNN, wide-and-deep), build **separate folders/notebooks per `model_type`** — mirroring the `models/logistic_regression/` + `models/linear_regression/` precedent — rather than one combined notebook per technique. Each notebook stays tightly focused and matches the canonical skeleton exactly; RESOURCES.md can still document the pair under one combined heading (as it already does for GLM). Folder names follow the `model_type` lowercased (e.g. `BOOSTED_TREE_CLASSIFIER` → `models/boosted_tree_classifier/`).

- [x] `models/linear_regression/` (LINEAR_REG; regression metrics) — built 2026-06-20, `penguins`/`body_mass_g`
- [x] `models/boosted_tree_classifier/` (BOOSTED_TREE_CLASSIFIER, XGBoost) — built + fully verified 2026-06-20, `census_adult_income` (same data/label as `logistic_regression`); includes tree visualization via EXPORT MODEL (Step 7)
- [x] `models/boosted_tree_regressor/` (BOOSTED_TREE_REGRESSOR, XGBoost) — built + fully verified 2026-06-20, `penguins`/`body_mass_g` (same data/label as `linear_regression`); includes tree visualization via EXPORT MODEL
- [x] `models/random_forest_classifier/` (RANDOM_FOREST_CLASSIFIER) — built 2026-06-21, `census_adult_income` (same data/label as `logistic_regression`/`boosted_tree_classifier`); tree visualization uses a dedicated shallow illustrative forest
- [x] `models/random_forest_regressor/` (RANDOM_FOREST_REGRESSOR) — built 2026-06-21, `penguins`/`body_mass_g` (same data/label as `linear_regression`/`boosted_tree_regressor`); genuinely underperforms boosting here (r2≈0.74 vs ≈0.97), discussed honestly in the notebook
- [x] `models/dnn_classifier/` (DNN_CLASSIFIER) — built 2026-07-16, `census_adult_income` (same data/label as `logistic_regression`/`boosted_tree_classifier`/`random_forest_classifier`); DNN training verified dramatically slower than tree models (12-46 min for one CREATE MODEL)
- [x] `models/dnn_regressor/` (DNN_REGRESSOR) — built 2026-07-16, `penguins`/`body_mass_g` (same data/label as `linear_regression`/`boosted_tree_regressor`/`random_forest_regressor`); baseline is badly broken (r2≈-27.5) on this small dataset — fixed with TRANSFORM scaling + a much higher learn_rate (r2≈0.82), discussed honestly as a debugging narrative
- [x] `models/wide_and_deep_classifier/` (DNN_LINEAR_COMBINED_CLASSIFIER) — built 2026-07-17, `census_adult_income` (same data/label as the other four classifiers)
- [x] `models/wide_and_deep_regressor/` (DNN_LINEAR_COMBINED_REGRESSOR) — built 2026-07-17, `penguins`/`body_mass_g` (same data/label as the other four regressors); same small-data convergence gotcha as `dnn_regressor`, fixed the same way (scale + higher learn_rate)
- [x] `models/automl_classifier/` (AUTOML_CLASSIFIER) — notebook + SQL built 2026-07-20, `census_adult_income` (same data/label as the other five classifiers); first model type in the project with real, substantial dollar cost (~$21-32/run, verified ~2.63hr wall-clock at budget_hours=1.0 — far more than the ~1.5hr originally quoted for cost approval) — CREATE MODEL syntax validated for free via `bq query --dry_run`. Fully reviewed against real output (twice); found and fixed a genuine `ML.CONFUSION_MATRIX` zero-arg hang (Error 21631273, client silently retries a deterministic failure, confirmed fixed and stable on re-run) and a zero-arg `ML.EVALUATE` accuracy anomaly (accuracy=0.5 doesn't reconcile with the model's own confusion-matrix curve). Cleanup confirmed executed live (model dropped, 12 exported GCS files deleted) — fully reviewed and complete, ninth and tenth (paired) model types close out Phase 2
- [x] `models/automl_regressor/` (AUTOML_REGRESSOR) — notebook + SQL built 2026-07-20, then rebuilt same day on `bigquery-public-data.samples.natality`/`weight_pounds` (WA, 2003, ~80K rows) after the user's real run hit a genuine platform limitation: AutoML Tables requires >=1,000 training rows, and `penguins` (~333 rows) fails immediately regardless of `budget_hours`. Fully run (incl. Cleanup) and reviewed: verified ~2.25hr wall-clock (far more than the ~1.5hr originally quoted); found and documented an `ML.EVALUATE` anomaly (median_absolute_error/explained_variance both exactly 0.0, un-diagnosable further since Cleanup had already dropped the model) and a benign 98%-NULL `cigarette_use` data-quality note consistent with its low `ML.GLOBAL_EXPLAIN` attribution. Fully reviewed and complete — tenth and final Phase 2 model type

### Phase 3: Unsupervised + insight models
- [x] `models/kmeans/` (KMEANS; `ML.CENTROIDS`, no label) — built 2026-07-17, `penguins` (4 numeric measurements, no label); num_clusters=3 chosen to match the 3 real species, independently confirmed optimal by HP tuning; genuine BigFrames comparison (`bigframes.ml.cluster.KMeans` exists, unlike DNN/wide-and-deep)
- [x] `models/pca/` (PCA; `ML.PRINCIPAL_COMPONENTS`) — built 2026-07-17, `penguins` (4 numeric measurements, no label); num_principal_components=2 baseline plus a pca_explained_variance_ratio=0.90 alternative (selects 3); verified fully deterministic (unlike KMEANS); genuine BigFrames comparison (`bigframes.ml.decomposition.PCA`, `.predict()` not `.transform()`)
- [x] `models/matrix_factorization/` (recommendation; `ML.RECOMMEND`) — built 2026-07-19, IMPLICIT feedback on `bigquery-public-data.google_analytics_sample` (42K users x 320 items); required setting up/tearing down a temporary BigQuery Editions reservation (ENTERPRISE, autoscale, no capacity commitment) since this is the one model type that can't train on-demand; genuine BigFrames comparison, positive HP-tuning result (contrast with autoencoder's failed tuning)
- [x] `models/contribution_analysis/` (CONTRIBUTION_ANALYSIS; `ML.GET_INSIGHTS`) — built 2026-07-20, `bigquery-public-data.new_york_citibike.citibike_trips` (same test/control split as the `AI.KEY_DRIVERS` sibling notebook, for direct comparison); verified `ML.GET_INSIGHTS` has three different output schemas depending on metric type (summable/ratio/category); verified >12 dimensions works (beyond `AI.KEY_DRIVERS`' cap) but costs real time; no BigFrames wrapper
- [x] `models/autoencoder/` (AUTOENCODER; anomaly/embedding) — built 2026-07-18, `penguins` (4 numeric measurements, no label); hidden_units=[3,2,3] for a 2-dim latent space comparable to PCA's 2 components; default RELU activation genuinely collapses to dead latent units on this small network, fixed with TANH (~3x better mean_squared_error); verified ML.EVALUATE reproduces bit-for-bit across retrains but latent-space values don't (no BigFrames wrapper exists for this model type)

### Phase 4: Time series
- [x] `models/arima_plus/` (ARIMA_PLUS; `ML.FORECAST`, `ML.EXPLAIN_FORECAST`) — cross-link to `bq-ai-functions` AI.FORECAST/TimesFM — built 2026-07-21, modernizing `Applied Forecasting/BQML Univariate Forecasting with ARIMA+.ipynb`: 5 real Citi Bike stations (daily trip counts), single-series then multi-series via `time_series_id_col` as the primary example (Pershing Square North has a shorter real history — genuine multi-length-series demo), folds in the old separate "Notes" notebook's granularity/missing-data gotchas as a dedicated step (verified a real gap day interpolates to exactly `(141+363)/2=252`); Step 10 covers custom holidays (verified exact two-block syntax), manual ARIMA order, and forecast bounds (verified incompatible with `ML.EXPLAIN_FORECAST` — a real, previously-undocumented restriction found and fixed during review). Fully reviewed across three passes against real output — eleventh model type, first in Phase 4, complete and verified.
- [x] `models/arima_plus_xreg/` (with external regressors) — built 2026-07-21, same 5-station data + TEST window as `models/arima_plus/` + 3 covariates (`avg_tripduration`/`pct_subscriber`/`ratio_gender`, `capacity` dropped by design — needed a join and was NULL for some stations), modernizing the old notebook's `EXECUTE IMMEDIATE`-loop multi-series workaround to native `time_series_id_col`. Applying the lesson from `models/arima_plus/`'s review, every option was tested against every downstream lifecycle function before handoff — found real cross-model-type differences from plain `ARIMA_PLUS`: `ML.EVALUATE` lacks `mean_absolute_scaled_error`; `forecast_limit_lower_bound` is rejected outright (not merely incompatible with one function); `ML.FORECAST`/`ML.EXPLAIN_FORECAST` strictly require the 3-argument covariate form. User ran the notebook fully in one pass, every cell matched pre-validation exactly, Cleanup confirmed — twelfth model type, second in Phase 4, complete and verified with no fixes needed on review.

### Phase 5: Model categories

**Scope amendment (decided 2026-07-22):** expanded from the original 3 items to 4, per the "no stretch goals, every capability" mandate — both additions were already fully documented in RESOURCES.md's Phase 1 research pass but never made it onto this checklist: `TENSORFLOW_LITE` folded into `models/imported/` (4th import format, identical mechanics to the other three), and `TRANSFORM_ONLY` added as its own folder (a real `model_type` that packages preprocessing with no estimator).

- [x] `models/transform_only/` — `TRANSFORM_ONLY` (preprocessing-only model, no estimator) — built 2026-07-22, `penguins` (same dataset as `kmeans`/`pca`/`autoencoder`): `ML.IMPUTER` + `ML.STANDARD_SCALER`/`ML.ROBUST_SCALER`/`ML.ONE_HOT_ENCODER` chained in one pipeline, `ML.TRANSFORM` to apply it, feeds into a downstream `LOGISTIC_REG`. Verified two real gotchas: (1) `ML.TRANSFORM` passes through any input column not referenced by the `TRANSFORM` clause untouched — useful but easy to mistake for pipeline output; (2) a downstream model with no embedded `TRANSFORM` silently predicts garbage on raw (untransformed) data — reproduced live (every row predicted "Gentoo penguin" until `ML.TRANSFORM` was re-applied). Fully pre-executed via `nbclient`, zero errors.
- [x] `models/imported/` (`TENSORFLOW`, `TENSORFLOW_LITE`, `ONNX`, `XGBOOST` via `model_path`, no connection) — built 2026-07-22, one notebook covering all 4 formats, `penguins` trained **locally** (sklearn→ONNX, xgboost native→XGBOOST, Keras→TF SavedModel→TFLite). Verified real gotchas beyond what RESOURCES.md already documented: (1) `XGBOOST` import caps at **XGBoost ≤ 1.5.1** for the training-side Booster file (not just the reverse direction already known from `boosted_tree_classifier`'s export step) — modern xgboost 2.x/3.x produces a file BQML rejects outright; (2) a `multi:softprob` XGBoost objective still predicts fine through a declared scalar `OUTPUT`, but silently returns an ARRAY — switched the XGBoost example to binary classification to keep the demo unambiguous; (3) **correction to RESOURCES.md's own capability table**: `ML.EXPLAIN_PREDICT` is documented as supported for `TENSORFLOW` imports but is rejected live ("TENSORFLOW model is unsupported in ml.explain_predict") — flagged as a doc correction, not reproduced as a working example. Fully pre-executed via `nbclient` (isolated test kernel, to avoid polluting the shared bq-ml venv with the `numpy<2`/`xgboost==1.5.1` pins this notebook needs), zero errors.
- [x] `models/export/` (`EXPORT MODEL` to GCS — TensorFlow SavedModel and XGBoost Booster; `model_registry='VERTEX_AI'`; `bq extract --model` CLI equivalent) — built 2026-07-22, `census_adult_income` (same data/label as `logistic_regression`/`boosted_tree_classifier`): a `LOGISTIC_REG` (→ TF SavedModel) and small `BOOSTED_TREE_CLASSIFIER` (→ XGBoost Booster, reusing the `xgboost==1.7.6` pin/`feature_names` gotcha already verified in `boosted_tree_classifier`), both downloaded and proven to load/predict **entirely outside BigQuery** with no BQ client involved. Verified a genuine finding: dropping a model registered via `model_registry='VERTEX_AI'` also removes its Vertex AI Model Registry entry automatically — no separate `gcloud`/`aiplatform` deletion step needed. Fully pre-executed via `nbclient` in two passes (the long boosted-tree training cell repeatedly hit an intermittent sandbox-specific kernel/BigQuery-client hang unrelated to the SQL itself — worked around by training that one model directly via `bq query` and validating the rest of the notebook, including the BigFrames/export/registry steps, live through the kernel).
- [x] `models/remote/` (`REMOTE WITH CONNECTION` → custom Vertex AI endpoint) — built 2026-07-22, `census_adult_income`, the full round trip: train a `LOGISTIC_REG` → `EXPORT MODEL` → `aiplatform.Model.upload()` with the pre-built `tf2-cpu.2-15` serving container (no Dockerfile) → deploy to a minimal `n1-standard-2` Endpoint → create a `CLOUD_RESOURCE` connection + grant `roles/aiplatform.user` (the connection genuinely required — unlike the false alarm on `AI.EMBED`/`AI.GENERATE_EMBEDDING` earlier in this project) → `CREATE MODEL ... INPUT(...) OUTPUT(...) REMOTE WITH CONNECTION` → `ML.PREDICT` (single-row + 200-row batch, 0 `remote_model_status` errors). **Real dollar cost**: the IAM grant was blocked by the permission-gate classifier and required explicit user authorization (obtained live) before proceeding; the Endpoint was live for only a few minutes end-to-end before full teardown (Endpoint undeploy+delete, Vertex Model delete, BQML models dropped, connection deleted, GCS export deleted) — verified fully cleaned up. Not re-run via `nbclient` (a second live endpoint deploy would double the cost and require a second authorization) — validated with the exact same code executed directly via the `bq`/`aiplatform`/`gcloud` CLIs and Python clients instead, then transcribed into the notebook. **Revised 2026-07-23** after the user's own live run surfaced two real bugs and prompted a scope discussion — see the audit log entry below for the full story: added Step 4 (the `model_registry='VERTEX_AI'` native-registration shortcut, demonstrated as a documented failure — a confirmed, unfixed Google bug) and Step 5 (an XGBoost model trained entirely outside BigQuery, registered to Vertex AI Model Registry but not deployed, proving `REMOTE` works with non-BQML models); fixed a silent IAM-grant failure and a case-sensitive "already exists" check in the connection-setup step; added a retry loop around `CREATE MODEL ... REMOTE WITH CONNECTION` for IAM propagation delay (verified live to exceed two minutes in one case).
- Also corrected `setup/README.md`'s connection matrix: it previously listed imported models and `EXPORT MODEL` as needing a Cloud Resource connection — verified live in both `models/imported/` and `models/export/` that neither does (only `REMOTE WITH CONNECTION` genuinely requires one; imported/export only need ordinary GCS IAM on the caller's own credentials).

### Phase 6: Model-free functions

**Scope amendment (decided 2026-07-23):** grouped by sub-category rather than one-function-per-notebook or one-mega-notebook (mirrors the `models/imported/` precedent — bundle when mechanics are shared / best taught side-by-side). Folded in 3 functions already covered by RESOURCES.md/legacy content but never listed on this checklist: `ML.NORMALIZER` (scalers), `ML.HASH_BUCKETIZE` (bucketizing), `ML.MULTI_HOT_ENCODER` (encoding) — same "no stretch goals, every capability" pattern as Phase 5's `TENSORFLOW_LITE`/`TRANSFORM_ONLY` amendment.

- [x] `functions/scalers/` — `ML.STANDARD_SCALER`, `ML.MIN_MAX_SCALER`, `ML.MAX_ABS_SCALER`, `ML.ROBUST_SCALER`, `ML.NORMALIZER` — built 2026-07-23 on `penguins`. **Real findings beyond RESOURCES.md's prior docs-derived content**: `ML.STANDARD_SCALER` verified to use population stddev (`STDDEV_POP`, ÷N), not the sample stddev (`STDDEV`/`STDDEV_SAMP`, ÷N-1) a naive manual check would use — a real, previously-undocumented discrepancy; `ML.MIN_MAX_SCALER`'s prediction-time capping to `[0,1]` for out-of-range values confirmed via a live `CREATE MODEL`+`ML.TRANSFORM` test; `ML.ROBUST_SCALER`'s outlier immunity proven side-by-side against `ML.STANDARD_SCALER` on an injected extreme outlier (500 among 10-14 range values). Ends with a `LOGISTIC_REG` embedding the `TRANSFORM` directly, contrasted explicitly with `models/transform_only/`'s standalone-pipeline pattern. Fully pre-executed via `nbclient`, zero errors.
- [x] `functions/feature_engineering/` — `ML.IMPUTER`, `ML.FEATURE_CROSS`, `ML.POLYNOMIAL_EXPAND` — built 2026-07-23 on `penguins`. **Closed a real gap the legacy source material left**: `ML.FEATURE_CROSS`/`ML.POLYNOMIAL_EXPAND` were never actually plugged into a live `CREATE MODEL` anywhere in this repo before — verified live that both train and predict completely normally inside a `TRANSFORM`, and the documented "not exportable" limitation only bites at `EXPORT MODEL` time (exact error captured: `"400 Model TRANSFORM contains unsupported function for exporting"`). `ML.IMPUTER` (which IS exportable) embedded in a real `LOGISTIC_REG`, verified predicting with an artificially-`NULL` input still auto-imputes correctly. Fully pre-executed via `nbclient`, zero errors.
- [x] `functions/encoding/` — `ML.ONE_HOT_ENCODER`, `ML.LABEL_ENCODER`, `ML.MULTI_HOT_ENCODER` — built 2026-07-23 on `penguins`. **Major finding, live-verifying a discrepancy RESOURCES.md had already flagged but never tested**: the current default `frequency_threshold=5` is not just a docs footnote — with categories occurring 6x/7x/3x, the 3x category silently collapses into bucket 0 (indistinguishable from NULL/unseen) under current defaults, but keeps its own index under the legacy `frequency_threshold=0`. Any real dataset with a rare-but-meaningful category (<5 occurrences) silently loses it under current defaults. Fully pre-executed via `nbclient`, zero errors.
- [x] `functions/bucketizing/` — `ML.BUCKETIZE`, `ML.QUANTILE_BUCKETIZE`, `ML.HASH_BUCKETIZE` — built 2026-07-23 on `penguins`. **Real finding**: clarified live what `ML.BUCKETIZE`'s `exclude_boundaries=TRUE` actually does — RESOURCES.md's docs-derived wording ("drops the implicit overflow buckets") is easy to misread as "out-of-range values become NULL," but verified live the outermost split points are dropped entirely instead, merging the overflow bucket into its nearest interior neighbor (3 split points → 2 bins, not NULL rows). Fully pre-executed via `nbclient`, zero errors.
- [x] `functions/distance/` — `ML.DISTANCE`, `ML.LP_NORM` — built 2026-07-23. **No legacy or prior repo example existed for either function** — first live validation from scratch. Verified `ML.NORMALIZER(v,p)` (from `functions/scalers/`) equals `v / ML.LP_NORM(v,p)` element-wise; derived Jaccard similarity (a metric `ML.DISTANCE` doesn't support directly) via a dot product + `ML.LP_NORM`'s L1 norm; real embedding-similarity worked example training a scratch `PCA` model on `penguins` and computing `ML.DISTANCE` between two different species' projections. Fully pre-executed via `nbclient`, zero errors.
- [x] `functions/text/` — `ML.NGRAMS`, `ML.TF_IDF`, `ML.BAG_OF_WORDS` — built 2026-07-23 on `bigquery-public-data.thelook_ecommerce.products.name`. **`ML.TF_IDF`/`ML.BAG_OF_WORDS` had zero prior examples anywhere in this repo.** Major finding: both share the exact same `frequency_threshold=5` default gotcha discovered in `functions/encoding/` — verified with 6 tiny documents that a word appearing in 2 different documents is still indistinguishable from a word appearing in only 1, both collapsing to the unknown bucket. Ends with `ML.BAG_OF_WORDS` embedded in a real `LOGISTIC_REG` classifying product category from the product name alone (~0.95 accuracy). Fully pre-executed via `nbclient`, zero errors.
- [x] Model-agnostic introspection: `ML.FEATURE_INFO`, `ML.TRAINING_INFO`, `ML.TRIAL_INFO` — intentionally NOT given dedicated notebooks; already fully documented in RESOURCES.md and demonstrated live across nearly every model-type notebook in Phases 1-4 (not a gap).

**Scope amendment (decided 2026-07-23, closes a gap found while planning Phase 7):** `ML.DESCRIBE_DATA`, `ML.VALIDATE_DATA_SKEW`, `ML.VALIDATE_DATA_DRIFT`, `ML.TFDV_DESCRIBE`, `ML.TFDV_VALIDATE` were already fully documented in RESOURCES.md (citing `MLOps/Model Monitoring/bqml-model-monitoring-tutorial.ipynb` as source) but had zero dedicated `bq-ml/functions/` notebook — same "no stretch goals, every capability" pattern as this phase's `ML.NORMALIZER`/`ML.HASH_BUCKETIZE`/`ML.MULTI_HOT_ENCODER` amendment and Phase 5's `TENSORFLOW_LITE`/`TRANSFORM_ONLY` amendment.

- [x] `functions/data_quality/` — `ML.DESCRIBE_DATA`, `ML.VALIDATE_DATA_SKEW`, `ML.VALIDATE_DATA_DRIFT`, `ML.TFDV_DESCRIBE`, `ML.TFDV_VALIDATE` — built 2026-07-23 on `census_adult_income` (self-contained scratch `LOGISTIC_REG`, matching `models/logistic_regression/`'s dataset rather than depending on the separate `MLOps/Model Monitoring/` project). Overview explicitly distinguishes this notebook's **dataset-level** distribution monitoring from `ML.DETECT_ANOMALIES`'s **row-level** anomaly detection (already covered 5x across `models/kmeans/`, `models/pca/`, `models/autoencoder/`, `models/arima_plus/`, `models/arima_plus_xreg/`) — different concept, similar-sounding name. **Real finding**: `ML.VALIDATE_DATA_SKEW` flags a false-positive skew alarm (`education_num`, JS divergence ~0.65 vs 0.3 threshold) when the "serving data" comparison uses a naive `LIMIT 5000` with no `ORDER BY` on the non-randomly-ordered public `census_adult_income` table — purely a sampling artifact, not real skew; fixed with `WHERE RAND() < p` (near-zero divergence). Turned into a deliberate two-part teaching example rather than hidden. Fully pre-executed via `nbclient`, zero errors.

### Phase 7: Workflows
- [x] `workflows/embeddings_classification/` — embeddings-as-features hierarchical classification, realizing "End-to-end classification (preprocess → train → evaluate → predict → explain)". Modernizes `Applied GenAI/Embeddings/Vertex AI GenAI Embeddings - As Features For Hierarchical Classification.ipynb`. Retail product-catalog use case (`bigquery-public-data.thelook_ecommerce.products`, 29,118 products, 2 departments, 36 `department: category` combinations): embeds product names and hierarchy nodes with `AI.EMBED` (`gemini-embedding-001`, `output_dimensionality=256` — no connection needed, since text-only `AI.EMBED` requires none), trains 3 `BOOSTED_TREE_CLASSIFIER` pairwise binary classifiers (absolute-difference / concatenated+abs-diff / concatenated+abs-diff+`auto_class_weights`) with embeddings passed as `ARRAY<FLOAT64>` feature columns directly (verified live — no need to unnest into individual columns), and resolves the hierarchy top-down per product via `ML.PREDICT` + `UNNEST`/`QUALIFY`. **Expanded post-build with 2 baselines that test whether the pairwise approach is even the right tool**: a direct multiclass `BOOSTED_TREE_CLASSIFIER` (product embedding → category label, no cross-join) and a pure `VECTOR_SEARCH` nearest-neighbor lookup (no training, no index needed at this data scale). **Major finding: the pairwise approach loses to both baselines, on accuracy AND cost** — multiclass baseline reaches ~69-70% category accuracy on a table ~38x smaller in a few minutes; `VECTOR_SEARCH` (zero training) reaches ~52%; the best pairwise variant only reaches ~42-48%. Built and fully verified 2026-07-22.
- [x] `workflows/customer_segmentation/` — `KMEANS` + RFM (Recency/Frequency/Monetary) feature engineering on `bigquery-public-data.thelook_ecommerce` (`orders`/`order_items`, fixed `2024-01-01` analysis cutoff). Built 2026-07-23. Caught and self-corrected a real training mistake before it reached the notebook: `user_id` accidentally included in the `TRANSFORM`/training query (an unscaled, meaningless identifier distorting distance calculations) — fixed by excluding it from training and re-joining via `ML.PREDICT` afterward, the same pattern already established for `species` in `models/kmeans/`. Real result: 4 interpretable segments (champions ~6%, loyal regulars ~18%, recent-low-value majority ~53%, lapsed/lost ~24%). Distinct from `models/kmeans/`'s penguins mechanism demo — the real business-clustering version, cross-linking back for algorithm mechanics. Fully pre-executed via `nbclient`, zero errors.
- [x] `workflows/churn_retention/` — reuses `customer_segmentation`'s dataset for narrative continuity. Built 2026-07-23. Defines churn from a real forward-looking backtest (feature cutoff `2024-01-01`, 180-day label window through `2024-06-29`, both far enough in the past to be genuine observed history) rather than a synthetic label — 91.2% of customers churned, an extreme-imbalance finding in its own right. `BOOSTED_TREE_CLASSIFIER` with `auto_class_weights=TRUE`; honest metric-literacy finding: adding richer features (tenure, returns, demographics) beyond bare RFM improves accuracy/recall/F1 substantially (accuracy 0.49→0.60, recall 0.48→0.61, F1 0.64→0.73) but barely moves `roc_auc` (0.531→0.544) — a real, not-glossed-over finding about this dataset's weak individual-level churn signal. `ML.GLOBAL_EXPLAIN`/`ML.FEATURE_IMPORTANCE` show `tenure_days` as the dominant driver. Ties back to segmentation: repeat customers (2+ orders) churn at 90.0% vs. 91.6% for one-time buyers — loyalty barely reduces churn risk in this dataset. Fully pre-executed via `nbclient`, zero errors.
- [x] `workflows/regression_based_forecasting/` — regression-based demand forecasting. Modernizes `Applied Forecasting/BQML Regression Based Forecasting.ipynb`: `LINEAR_REG`/`BOOSTED_TREE_REGRESSOR` (already built in Phase 2) applied to forecasting via time features, lags, and direct multi-step (`LEAD`) models on the same Citi Bike station/TEST window as `models/arima_plus/`, with a cross-technique comparison back to ARIMA_PLUS's own accuracy on that window. Built and fully pre-validated 2026-07-21 (first workflow in the project).
- [x] `workflows/hierarchical_forecasting/` — hierarchical forecasting comparison, modernizing `Applied ML/Forecasting/BigQuery ML For Hierarchical Forecasting.ipynb`. Uses `bigquery-public-data.iowa_liquor_sales.sales` (real State → County → City → Store hierarchy, 2 counties × 2 cities × 2 stores, weekly granularity). Verifies BQML's built-in `hierarchical_time_series_cols` (`models/arima_plus/` Step 10) is bottom-up reconciliation, builds a from-scratch custom top-down disaggregation (forecast-proportions method), and compares accuracy between the two at every hierarchy level — plus a generalized Python function automating the top-down approach for any hierarchy depth. Built and fully pre-validated (including a full end-to-end `nbclient` run) 2026-07-21.
- [x] `workflows/recommendation/` — extends `models/matrix_factorization/`'s `MATRIX_FACTORIZATION` setup (identical `ga_sessions_*` July 2017 IMPLICIT-feedback training query, for a fair comparable baseline) into a full workflow. Built 2026-07-23, with explicit user authorization for the real-dollar-cost temporary Enterprise autoscale reservation (0 baseline slots, autoscale to 100, billed per-second only) this model type requires. Adds: a non-personalized popularity baseline (personalized top-10 for a real user shares 0/10 items with it — real evidence personalization is doing distinct work); batch top-N generation for many users in one pass via `ROW_NUMBER()` (vs. `models/matrix_factorization/`'s one-user-at-a-time demo); and an empirical cold-start deep dive — two different absent `visitor_id`s get an identical, non-personalized fallback ranking, and that fallback overlaps substantially (6-9/10 across runs) with the popularity baseline, meaning `ML.RECOMMEND`'s own default behavior for new users already approximates a popularity fallback with no extra system needed. Fully pre-executed via `nbclient` (reservation created and torn down cleanly), zero errors.
- [x] `workflows/anomaly_fraud_detection/` — the real ground-truth validation the 5 existing `ML.DETECT_ANOMALIES` demos (`models/kmeans/`, `models/pca/`, `models/autoencoder/`, `models/arima_plus/`, `models/arima_plus_xreg/`) all lack, since none of them have real labeled anomalies. Built 2026-07-23 on `bigquery-public-data.ml_datasets.ulb_fraud_detection` (confirmed live to exist natively in `bigquery-public-data` per direct user correction of an earlier research-agent negative finding — 284,807 rows, 492 real fraud cases). Trains unsupervised `PCA` and `AUTOENCODER` on features only (no label), scores with `ML.DETECT_ANOMALIES` using the true fraud rate as an "oracle" `contamination`, and measures real precision/recall against the `Class` label — both land in the same ~20-29% precision/recall range, contrasted with a supervised `BOOSTED_TREE_CLASSIFIER` (`auto_class_weights=TRUE`) reaching 87% recall on its held-out eval split, a ~3x improvement from having labels. **Major bonus finding, refining a Phase 3 RESOURCES.md claim**: `PCA`'s `pca_explained_variance_ratio` mode (variable component count) produced wildly different `ML.DETECT_ANOMALIES` results across identical retrainings (TP 3, 235, 279 out of 492) even though `ML.EVALUATE`'s `total_explained_variance_ratio` stayed bit-for-bit stable — near-threshold eigenvalue ties can flip which components are retained. Fixed/mitigated by switching to a fixed `num_principal_components`, substantially more stable (though not perfectly bit-for-bit) across retrainings. RESOURCES.md's PCA determinism claim updated with this nuance. Fully pre-executed via `nbclient` twice (once to discover the PCA instability, once more after switching to fixed `num_principal_components`), zero errors both times.
- [x] `workflows/cross_validation/` — modernizes `03 - BigQuery ML (BQML)/BQML Cross-validation Example.ipynb`. Confirmed live: BigQuery ML has no native k-fold cross-validation (`data_split_method` only supports single-split holdout) — hand-rolled 5-fold CV via `FARM_FINGERPRINT`/`MOD` hash-based fold assignment (fewer than the legacy's 10, to keep 5 `CREATE MODEL` calls fast) on `bigquery-public-data.ml_datasets.ulb_fraud_detection` (reused from `workflows/anomaly_fraud_detection/`, not the legacy's private `fraud.fraud_prepped` table — motivated directly by that workflow's ~15-real-fraud-case eval split). 5 `LOGISTIC_REG` fold models submitted concurrently (same pattern as `workflows/regression_based_forecasting/`), evaluated per-fold, aggregated to mean±stddev (precision 0.083±0.014, recall 0.901±0.034, roc_auc 0.980±0.007), then compared against a same-model-type single holdout (precision 0.089, recall 0.893, roc_auc 0.974) — a fair, controlled comparison, deliberately not a cross-model comparison against `anomaly_fraud_detection`'s `BOOSTED_TREE_CLASSIFIER` numbers (would confound model choice with sampling variance). Finding: the single holdout landed close to the fold means for precision/recall but ~1 stddev below the fold mean for roc_auc — a real, honest, non-dramatic illustration of single-split uncertainty. Built and fully pre-validated 2026-07-24 (`LOGISTIC_REG` is deterministic on this data, so results reproduce bit-for-bit).
- [x] `workflows/ensembling/` — modernizes `03 - BigQuery ML (BQML)/BQML Ensemble Example.ipynb`. Rebuilt self-contained against this project's own model recipes (`models/logistic_regression/`, `models/boosted_tree_classifier/`, `models/random_forest_classifier/` — confirmed identical `census_adult_income` feature set/label/`auto_class_weights` via direct file comparison) rather than the legacy's fragile `bq.list_models()` dependency on 6 other notebooks (`03a`-`03f`) having been run first. Uses a **3-way TRAIN/VALIDATE/TEST split**, stricter than the legacy's 2-way approach, to avoid stacking leakage: base models train on TRAIN only, their predictions on VALIDATE (never seen in training) become the meta-model's training features, TEST stays untouched until the final comparison. **Real bug caught live during pre-validation**: joining base-model predictions back together on raw feature columns fanned out from 6,587 VALIDATE rows to 11,027 (different people can share identical feature values across every column) — fixed with a synthetic `ROW_NUMBER()` row_id generated once at split time. Final honest comparison on TEST: the stacked meta-model wins on `roc_auc` (~0.896 vs. best individual base model, `boosted_tree`, at ~0.893); a free simple-average ensemble (no training) wins on F1 instead (~0.665 vs ~0.663) — a genuine, metric-dependent, non-dramatic finding reported exactly as measured, unlike the legacy notebook (whose ensemble didn't clearly beat its best individual model at all). Built and fully pre-validated 2026-07-24 (`RANDOM_FOREST_CLASSIFIER`'s known non-determinism causes small run-to-run variation in its own numbers, noted explicitly).

**Scope amendment (decided 2026-07-24, ahead of Phase 8 planning):** while researching Phase 8's orchestration approaches, the user proposed tapping a real GA4 (Google Analytics 4) BigQuery public dataset for a churn workflow — reused across all of Phase 8's pipelines as the one workflow all 8 orchestration approaches operationalize. Confirmed live and added as `workflows/ga4_churn_prediction/`:
- [x] `workflows/ga4_churn_prediction/` — engagement-based churn from `bigquery-public-data.ga4_obfuscated_sample_ecommerce` (real Google Merchandise Store GA4 export, 2020-11-01 to 2021-01-31), a genuine complement to `churn_retention`'s order-lapse definition. Cohort = users whose first-ever event falls 2020-11-01 to 2020-12-24 (162,339 users, chosen so every user's 30-day label window completes within the dataset's date range); features engineered from each user's first 7 days of activity (event-type counts, session count, active days, device/geo/traffic-source, engagement time); churned = no events in the following 30 days — a real backtest against static historical data, not a simulated projection. 94.3% churned, an even more extreme imbalance than `churn_retention`'s 91.2%. `BOOSTED_TREE_CLASSIFIER` with `auto_class_weights=TRUE`, baseline (behavioral counts only, `roc_auc`≈0.744) vs. richer (+device/geo/traffic/engagement-time, `roc_auc`≈0.771) — the mirror image of `churn_retention`'s finding: here `roc_auc` improves meaningfully while fixed-threshold metrics (accuracy/recall/F1) move slightly the other way, instead of the reverse. `ML.GLOBAL_EXPLAIN` shows `n_events` dominating (≈3x the next feature) with `did_purchase` fully subsumed (attribution ≈0.0) once activity-volume features are present. Ties back to `churn_retention`: early purchase intent is a much stronger churn signal here (73.2% vs. 94.6% churn, a 21-point gap) than repeat-purchase history was there (91.6% vs. 90.0%, a 1.6-point gap) — two genuinely different, both-honest answers to a similar-sounding question. Built and fully pre-executed via `nbclient` 2026-07-24; two evaluation cells drifted slightly from live pre-validation on the shipped run (`BOOSTED_TREE_CLASSIFIER`'s normal run-to-run training variation) and markdown was resynced to the notebook's actual displayed output, with a non-determinism hedge added.

### Phase 8: Pipelines (MLOps on BigQuery)

**Scope amendment (decided 2026-07-24, same "no stretch goals, every capability" pattern as Phase 5's `TENSORFLOW_LITE`/`TRANSFORM_ONLY` and Phase 6's `ML.NORMALIZER`/`ML.HASH_BUCKETIZE`/`ML.MULTI_HOT_ENCODER`):** researched Google Cloud Workflows, Dataform, and dbt-for-BigQuery alongside the originally-planned 5 approaches — all three are genuine, real orchestration paths for BQML this backlog would otherwise have missed. Expanded from 5 to 8 items, build order = cheapest/simplest infrastructure first:
- [x] `pipelines/sql_scripting/` — modernizes `MLOps/Model Monitoring/model_monitoring_job.sql`'s `DECLARE`/`IF`/`BEGIN...END` + `SELECT ERROR()` alerting pattern, self-contained on `ga4_churn_prediction`'s cohort (own copies of the feature table/model, since the workflow's own cleanup drops its artifacts). Simulates production drift: an initial model trained on users whose first activity was on/before `2020-11-20` (pre-Black-Friday), checked against everyone who arrived after via `ML.VALIDATE_DATA_DRIFT`'s 3-argument form (verified live: a plain `CREATE MODEL` `BOOSTED_TREE_CLASSIFIER` does not qualify as the "Model Registry MODEL" the function's optional 4th argument requires, so the simpler 3-arg form already established in `functions/data_quality/` is used instead — universal, no extra setup). Real, explainable finding: drift detected in 5 of 12 features (`total_engagement_time_msec` strongest), because the "new arrivals" window genuinely spans Black Friday/Cyber Monday (`2020-11-27`/`2020-11-30`) — not a data-quality artifact. Conditional retrain on the full cohort is a real positive outcome, `roc_auc` improving from ~0.73-0.74 to ~0.77. **Real bug caught and fixed during build**: a build-script string meant to reach the notebook cell as literal `\n`/`\t` escape sequences (for `SELECT ERROR()`'s report formatting) was double-processed — the *build* script's own Python string parsing consumed one level of backslash-escaping, then the *notebook cell*, once actually executed by the kernel, consumed a second level, turning intended literal `\n`/`\t` text into real newline/tab characters mid-query and breaking BigQuery's own string-literal parsing (`Unclosed string literal`). Fixed by making the notebook's generated Python string a raw string (`r"""..."""`) so the SQL's own escape sequences survive kernel execution untouched. Built and fully pre-executed via `nbclient` 2026-07-24; numbers resynced to the final shipped run (`BOOSTED_TREE_CLASSIFIER`'s normal training variation) with a non-determinism hedge added.
- [x] `pipelines/scheduled_queries/` — takes `pipelines/sql_scripting/`'s exact script, unmodified, and schedules it via `google.cloud.bigquery_datatransfer` (`TransferConfig`, `data_source_id='scheduled_query'`, the same mechanism behind BigQuery Studio's "Scheduled queries" UI). Modernizes `MLOps/Model Monitoring/bqml-model-monitoring-tutorial.ipynb`'s "From Job To Scheduled Query" section; disclosed one honest design difference — that tutorial's script uses the `@run_date` pseudo-parameter for rolling windows against a live serving table, while this pipeline's underlying script uses fixed literal dates against a frozen historical GA4 export, so `@run_date` isn't needed here. **Two real gotchas caught live during build, both now in the notebook**: (1) creating a `TransferConfig` directly via the API (not the Console's interactive flow) fails with `Failed to find a valid credential. The field 'version_info' or 'service_account_name' must be specified.` unless an explicit `service_account_name` is supplied — fixed by running the schedule as the project's default Compute Engine service account; (2) the schedule's own near-term automatic first run raced a manually-triggered demo run, both targeting the same `CREATE OR REPLACE MODEL` concurrently — BigQuery genuinely rejects this (`Model can not be updated by multiple create model query jobs at the same time`) — fixed by setting `schedule_options.start_time` a few days out so only the manual demo run executes during the notebook's short session (a real production schedule wouldn't need this). Manually triggered a real backfill run via `start_manual_transfer_runs`, confirmed `FAILED` is the correct terminal state (the `SELECT ERROR()` report becomes the transfer run's error message, exactly what `email_preferences.enable_failure_email=True` would forward as a real alert email) — verified via `run.error_status.message`. Real, positive retrain outcome: `roc_auc` improved from ~0.74 to ~0.77, consistent with `sql_scripting`. Cleanup includes `delete_transfer_config` (essential — an undeleted schedule keeps re-running every 24 hours indefinitely). Built and fully pre-executed via `nbclient` 2026-07-24.
- [x] `pipelines/dataform/` — version-controlled SQL pipeline via `google.cloud.dataform_v1`: a real 5-action dependency graph — `ga4_churn_pipeline_features` (`type: "table"`) → `ga4_churn_pipeline_model` (`type: "operations", hasOutput: true`, `CREATE OR REPLACE MODEL ${self()} ... FROM ${ref(...)}`) → two `ML.EVALUATE`-based assertions (`quality_reasonable` at a `roc_auc < 0.6` bar, `quality_strict` at a deliberately-unrealistic `< 0.99` bar to force a genuine live failure) → `ga4_churn_pipeline_scoring` (`type: "table", dependOnDependencyAssertions: true`). **Real, comprehensive result verified live**: features/model/quality_reasonable all `SUCCEEDED` (`roc_auc` ≈ 0.75), `quality_strict` genuinely `FAILED` (`Assertion failed, expected zero rows`), and `scoring` was correctly `SKIPPED` — `dependOnDependencyAssertions` made it depend on both quality assertions (not just the model), so the one failure actually blocked downstream work; the scoring table was never created. **Four real gotchas caught live, none documented in the tutorials researched**: (1) `dataform.json` (legacy config) alone fails with `Can't find package.json` — switched to the modern `workflow_settings.yaml`, which needs no separate `package.json`; (2) a workspace cannot be named `main` (the repo's default branch name) — `FAILED_PRECONDITION`; (3) a workflow invocation requires an explicit `service_account` in `invocation_config`; (4) that service account's IAM policy must separately grant `roles/iam.serviceAccountTokenCreator` to Dataform's per-project service agent (`service-PROJECT_NUMBER@gcp-sa-dataform.iam.gserviceaccount.com`) or every action fails with permission-denied before any BigQuery work starts — **this IAM grant required explicit user authorization** (blocked by the permission system as a real IAM-policy change) via `AskUserQuestion`; user chose granting it directly. **Per explicit user follow-up instruction, the notebook itself now performs API enablement (`gcloud services enable dataform.googleapis.com`) and IAM permission checking/granting automatically** (Step 0), checking first and only granting if missing, with a clear fallback message (the exact `gcloud` command) if the runtime credentials lack `roles/iam.serviceAccountAdmin` to grant it themselves — so a fresh environment doesn't hit the same wall blind. A short retry-with-wait is built into the first workflow-invocation attempt for the ~1-2 minute IAM propagation window (a lesson carried over from `scheduled_queries`). Built and fully pre-executed via `nbclient` 2026-07-25; added a `roc_auc` display cell after an initial draft asserted a specific number in markdown without ever showing it live in the notebook's own output (caught during self-review before user review). **Follow-up correction (2026-07-25, prompted by a direct user question after `cloud_workflows` shipped)**: that same added `roc_auc` display cell — a plain standalone `SELECT ... FROM ML.EVALUATE(MODEL ...)` — turned out to be exposed to the exact query-cache staleness bug found in `pipelines/cloud_workflows/`. Confirmed via `INFORMATION_SCHEMA.JOBS_BY_PROJECT`: the cell's job showed `cache_hit: true`, serving a stale value (`0.746749`) from an earlier test iteration rather than the notebook's own freshly-trained model. The Dataform *assertions* themselves were unaffected (confirmed via the same job-history query — Dataform compiles each assertion into a `CREATE VIEW` + `ASSERT` script, which shows `cache_hit: null` and reliably re-evaluates fresh, the same script-scoped immunity `sql_scripting` has). Fixed by adding `use_query_cache=False` to that one display query and re-running the full notebook end-to-end; the genuinely fresh value (`0.725708`, confirmed `cache_hit: false`) replaced the stale one in both the notebook and this file. **General lesson: after discovering a caching/staleness bug in one pipeline, immediately audit every other notebook for the same structural pattern (a bare, standalone query re-reading a model's state) rather than assuming the fix is scoped to where it was found — confirmed via `INFORMATION_SCHEMA.JOBS_BY_PROJECT` query-history search, not by re-reading code and guessing.** A full project-wide scan (`grep` for every `CREATE OR REPLACE MODEL` target name across every `bq-ml` notebook) confirmed no other notebook reuses a single model name across a retrain-then-reread cycle outside these four Phase 8 pipelines — every other model-type/workflow notebook uses distinct names per variant (baseline/tuned/transform/etc.).
- [x] `pipelines/cloud_workflows/` — the same `sql_scripting` drift-check/retrain logic re-expressed as external declarative orchestration via `google.cloud.workflows`/`google.cloud.workflows.executions`: a YAML Workflow with 3 routines (`main`, `run_bq_query`, `run_bq_job`) submitting one BigQuery job per step via the `googleapis.bigquery.v2.jobs.*` connector, polling `jobs.get` every 5s until `DONE` — the standard long-running-job pattern from Google's own "Running BigQuery jobs in parallel with Workflows" codelab. **Two real gotchas caught live, one of them major and project-wide**: (1) Workflows expressions cap at 400 characters — the full drift-check query and `CREATE MODEL` statement both exceed this inline, fixed by building query text across several small `assign` steps instead of one large expression (a real YAML-authoring constraint, not present in Python string building); (2) **BigQuery's automatic query result cache does not reliably invalidate when a referenced MODEL is replaced via `CREATE OR REPLACE MODEL`** — the first live run reported bit-for-bit identical `roc_auc_before_retrain`/`roc_auc_after_retrain` (both `0.7688881118881119`), traced to a `cacheHit: true` stale result from *before* the retrain; confirmed directly with `bq query --nouse_cache` that the cache was serving wrong data. Fixed by setting `useQueryCache: false` on every job the workflow submits, after which before/after values were genuinely distinct (`0.729021978021978` → `0.7794135864135864` in the isolated repro; `0.7421368631368631` → `0.7695144855144855` in the notebook's own shipped run). **This finding is now documented prominently in [RESOURCES.md](RESOURCES.md)** under the `CREATE MODEL — Model Types` section as a general, cross-cutting gotcha — it is specific to external orchestrators issuing separate top-level query jobs before/after a retrain (Cloud Workflows, a monitoring script, a dashboard) and was *not* observed inside `sql_scripting`'s single multi-statement script, where the equivalent before/after `ML.EVALUATE` checks reliably differed every time without any extra fix. Built and fully pre-executed via `nbclient` 2026-07-25, zero errors; cleanup verified via `gcloud workflows list` (zero remaining) after the run.
- [x] `pipelines/dbt/` — honest treatment via `dbt-core`/`dbt-bigquery` invoked programmatically through `dbtRunner` (dbt's supported Python API, no subprocess/CLI parsing) against a temporary dbt project written to disk. Since dbt has no purpose-built `CREATE MODEL` materialization (confirmed — unlike Dataform's official `type: "operations", hasOutput: true`), wrote a **custom materialization macro** (`materialization_bqml_model_default`-style, `materialization bqml_model, adapter='bigquery'`) wrapping the raw `CREATE OR REPLACE MODEL {{ self() }}-equivalent... OPTIONS(...) AS {{ sql }}` DDL — the real, practical workaround the dbt community uses today. Two dbt tests (`quality_reasonable` at `roc_auc < 0.6`, `quality_strict` at a deliberately-unrealistic `< 0.99`) mirror Dataform's assertion pair exactly, same zero-rows-means-pass convention. **Real, corrected assumption, verified live rather than asserted from prior knowledge**: went in assuming dbt tests are report-only with no cross-node blocking (unlike Dataform's `dependOnDependencyAssertions`) — wrong. `dbt build` (running models + tests together in one DAG-aware pass) genuinely **skips** a downstream model (`ga4_churn_pipeline_scoring`) when a test on its upstream dependency fails, with zero extra configuration needed — confirmed live (`5 of 5 SKIP relation ... ga4_churn_pipeline_scoring`). This protection is specific to `dbt build`, though — running `dbt run` then `dbt test` as two separate invocations (common when "deploy" and "verify" are distinct CI stages) does **not** get it, since `dbt run` already builds everything before `dbt test` ever checks anything. **Real gotcha caught live**: passing model options via a bare custom `config()` key triggers a `CustomKeyInConfigDeprecation` warning in dbt 1.12 (`Custom key 'model_options' found in config... should move into config.meta`) — fixed by nesting under `meta` in both the model file and the macro, eliminating the warning entirely. Also honestly notes dbt's actual "more native" 2026 ML story (Python models via BigFrames, transpiled to BQML via Dataproc/serverless/Colab Enterprise) without implementing it live — a literal `CREATE MODEL` artifact isn't what that route produces, and standing up its required compute infrastructure would be disproportionate to a brief, honest mention. `use_query_cache=False` applied to the post-build `roc_auc` display query per the now-standing practice from `cloud_workflows`/`dataform`; confirmed `cache_hit: false` via `INFORMATION_SCHEMA.JOBS`. Built and fully pre-executed via `nbclient` 2026-07-25, zero errors; cleanup drops all 3 BigQuery objects and removes the temporary local dbt project directory.
- [x] `pipelines/vertex_kfp/` — train → evaluate → quality-gate → conditionally score, built from the official prebuilt `google_cloud_pipeline_components.v1.bigquery` ops (`BigqueryCreateModelJobOp`/`BigqueryEvaluateModelJobOp`/`BigqueryPredictModelJobOp`) — no prior repo example uses these; cross-links the repo's own legacy custom-`@dsl.component` pattern in `03Tools - Pipelines Example 1/2.ipynb`. One custom `@dsl.component` (`check_quality_gate`) fills the one gap the prebuilt ops don't cover: reading a metric back out of an artifact and branching on it via `dsl.If`. **Verified live both ways, mirroring Dataform's/dbt's halt-on-failure demonstrations**: `quality_threshold=0.6` → gate passes → `condition-1` `TRIGGERED` → `bigquery-predict-model-job` runs and succeeds; `quality_threshold=0.99` (same model, same real `roc_auc`≈0.728) → gate fails → `condition-1` `NOT_TRIGGERED` → the predict job never even appears in the task list, genuinely never created, not just skipped-and-reported. **Two real gotchas caught live during pre-validation in `/tmp` before any notebook cell was written, neither documented anywhere researched**: (1) `BigqueryEvaluateModelJobOp`'s `evaluation_metrics` artifact `.metadata` is not a flat `{metric_name: value}` dict — it stores the raw BigQuery REST API tabular response (`schema.fields` + `rows[].f[].v`), the same shape as `jobs.getQueryResults` and the Cloud Workflows connector's own result rows; a naive `.metadata.get('roc_auc')` silently returns `None` and the gate always evaluates `False`, with no error — caught by dumping raw `task.outputs` from a live `PipelineJob`'s `task_details` after `condition-1` suspiciously showed `NOT_TRIGGERED` despite a comfortably-passing model. Fixed by finding `roc_auc`'s column position in `schema.fields` and reading that position out of `rows[0].f`. (2) **Major, project-wide**: since this pipeline re-runs the identical `ML.EVALUATE` query text every run against a model that gets `CREATE OR REPLACE`'d each time — the same staleness scenario already found in `cloud_workflows`/`dataform` — the fix seemed obvious: `job_configuration_query={'useQueryCache': False}`. It's a no-op. The component library's own JSON-cleanup step (`google_cloud_pipeline_components...json_util.recursive_remove_empty`) strips any dict value that's falsy under plain Python truthiness (`if v:`) before merging the override into the job body, so the Python bool `False` never reaches the BigQuery API call and caching silently stays on — confirmed via `INFORMATION_SCHEMA.JOBS_BY_PROJECT` showing `cache_hit: true` despite the override. The BigQuery REST API itself accepts the **string** `'false'` for this boolean field and genuinely disables caching (confirmed via a direct REST call and inside a real pipeline run, `cache_hit: false`) — the actual, verified fix is `job_configuration_query={'useQueryCache': 'false'}`, the string not the bool. **Both gotchas now documented in [RESOURCES.md](RESOURCES.md)** under `CREATE MODEL — Model Types`, alongside the original `cloud_workflows` finding. Confirmed the default Compute Engine service account already had sufficient BigQuery/Storage access in this project; Step 0 still checks and self-grants `roles/bigquery.dataEditor`+`roles/bigquery.jobUser`/`roles/storage.objectAdmin` if either is missing, per the standing self-provisioning instruction, since a fresh reader's project won't already have this project's broad `owner`-level grants. Built and fully pre-executed via `nbclient` 2026-07-25, zero errors across both pipeline runs (~20 min combined, two real `BOOSTED_TREE_CLASSIFIER` trainings); outputs cleared for the user's own Restart & Run All per standing handoff practice.
- [x] `pipelines/composer_airflow/` — the same `sql_scripting` drift-check/conditional-retrain/report logic as a real Apache Airflow DAG on a live **Cloud Composer 3** environment (per explicit user request to use Composer 3, not the originally-planned Composer 2) — `BigQueryInsertJobOperator` for every BigQuery job (first use of this operator in the repo), `BranchPythonOperator` + XCom for the conditional retrain, a join task with `trigger_rule=NONE_FAILED_MIN_ONE_SUCCESS`. First Composer 3 usage anywhere in this repo. **Composer 3 config researched live via the installed `google.cloud.orchestration.airflow.service_v1` SDK itself** (not guessed): confirmed real field names for `environments.WorkloadsConfig`'s 5 sub-resources (`scheduler`/`web_server`/`worker`/`triggerer`/`dag_processor`, each with `cpu`/`memory_gb`/`storage_gb` and count fields) and the live-available image versions via `ImageVersionsClient` (`composer-3-airflow-2.11.1-build.11` confirmed as the current default). Environment sized to the floor on every component (0.5 vCPU) — the real cost lever this Composer generation exposes, billed in DCU-hours (~$0.06/DCU-hour in `us-central1`) rather than Composer 2's fixed node pools. **Real, live-verified outcome**: `check_drift` → `branch_on_drift` correctly routes to the retrain path (5/12 features drifted, same Black-Friday shift as every sibling pipeline), `no_drift` shows genuine `skipped` (not just an unused return value), `roc_auc` 0.7515 → 0.7697. **Two genuinely new gotchas found and fixed, both from live operational experience, not code review**: (1) the Airflow webserver lags the `Environment` resource's own `RUNNING` state by several minutes on first boot, and — floored at `cpu=0.5, memory_gb=2` with no replica — **restarts periodically** (~every 10-15 min, confirmed via repeated `Starting gunicorn` log timestamps), causing brief `502` windows from the REST API while the scheduler and any in-progress DAG run are completely unaffected; a first retry budget (6×15s) genuinely wasn't enough and a live run crashed on `resp.json()` when it gave up — fixed with a longer budget (12×20s) and explicit `status_code == 200` checks before parsing JSON anywhere, not just at the first call site. (2) The documented `gcloud composer environments run ... dags list` CLI path took over 10 minutes to return anything in this session (it spins up its own execution context) — switched entirely to direct Airflow REST API calls via `google.auth.transport.requests.AuthorizedSession`, which is what the official "Access the Airflow REST API" docs actually recommend and is far faster once the webserver is up. **Proactively checked the query-cache staleness finding from `cloud_workflows`/`dataform`/`vertex_kfp` here too — confirmed it does NOT need a workaround via the same buggy path**: `BigQueryInsertJobOperator`'s `configuration={'query': {..., 'useQueryCache': False}}` uses a real Python `bool` and works correctly (`cache_hit: false` confirmed via `INFORMATION_SCHEMA.JOBS_BY_PROJECT`), because this operator submits jobs through the standard `google-cloud-bigquery` client's own `QueryJob.from_api_repr()`, not the custom JSON-cleanup logic that silently drops `False` in `google_cloud_pipeline_components` (the `vertex_kfp` bug) — a good example of verifying per-library rather than assuming a bug generalizes. Built and fully pre-executed via `nbclient` against a real, live environment (multiple full runs during iteration — see `airflow_with_kfp` entry below for why); outputs cleared for handoff.
- [x] `pipelines/airflow_with_kfp/` — the "meta-orchestration" pairing, sharing the same live Composer 3 environment: one Airflow task (`RunPipelineJobOperator`) triggers `pipelines/vertex_kfp/`'s already-built Vertex Pipeline, reproducing that notebook's exact pipeline definition inline for a self-contained build (compile → upload JSON to GCS → `template_path`). The repo's original "DAG 3" pattern (`MLOps/Serving/Batch/Orchestrating Batch Inference With Airflow.ipynb`), now pointed at a real BQML pipeline instead of Dataflow/Dataproc. **Verified live end-to-end, confirming the triggered `PipelineJob` itself succeeded independently** (not just trusting the Airflow task's own `success` status) via `aiplatform.PipelineJob.list()`/`.task_details`: `bigquery-create-model-job` → `bigquery-evaluate-model-job` → `check-quality-gate` → `condition-1` `TRIGGERED` → `bigquery-predict-model-job`, all `SUCCEEDED`. **A real, live-caught bug in this notebook's own first draft**: it assumed `pipelines/composer_airflow/`'s feature table would still exist when run right after it in the same session — wrong, since that notebook's own Cleanup drops its copy immediately at the end of *its* run, before this one ever starts. First live attempt failed with `Not found: Table ... ga4_churn_pipeline_features` inside the triggered pipeline's `bigquery-create-model-job` task. **Fixed by giving this notebook its own independent Step 1** that recreates the feature table, the same self-contained pattern every other Phase 8 pipeline already follows — a good reminder that "runs right after its sibling in the same session" is not the same guarantee as "that sibling's artifacts are still there," especially when the sibling's own cleanup is unconditional. **Two more real gotchas found live while iterating**: (1) a manually-triggered run of a DAG that's been **paused** (done mid-session to stop an unrelated Airflow-level automatic retry storm from a crashed test run) is accepted by the API and creates a `dag_run`, but that run sits at `queued` forever with zero error, since the scheduler never runs a paused DAG's tasks regardless of trigger method — caught by noticing a run stuck at `queued` far longer than the pipeline should ever take, and confirmed via the DAG's own `is_paused` field. (2) `composer_client.delete_environment(...).result()` genuinely deletes the `Environment` resource (confirmed via a subsequent `describe` returning `NOT_FOUND`) but does **not** delete its GCS bucket (`dags`/`logs`/`data`/`plugins`) — left orphaned, small but billed separately, with an opt-in (commented-out) cleanup cell provided rather than deleted automatically (deleting a bucket recursively is exactly the kind of action this project's standing practice reserves for explicit user opt-in, not automatic notebook cleanup). **This notebook performs the real, uncommented shared-environment teardown** — explicitly confirmed with the user via `AskUserQuestion` before actually executing `delete_environment()` for real (~3 hours of runtime, both notebooks' full live validation already complete by that point) — consistent with this project's standing practice of pausing for explicit confirmation before destructive/costly infrastructure actions, even when a general build plan had already been approved. Built and fully pre-executed via `nbclient` against the same live environment as `composer_airflow/` (several iterations during debugging, described above); outputs cleared for handoff. This completes Phase 8 — all 8 pipeline notebooks are now built, reviewed, and shipped.

All 8 operationalize `workflows/ga4_churn_prediction/`.

**Scope amendment (decided 2026-07-29, post-Phase-8): first causal-inference workflow.** User proposed a Propensity Score Matching (PSM) deep dive — every existing workflow predicts an outcome; this is the project's first workflow estimating a *causal treatment effect* from observational data, entirely via `LOGISTIC_REG` (the propensity model) plus plain SQL (matching, IPTW, balance diagnostics). User explicitly wanted the notebook to teach the topic well (a strong upfront overview, real cross-industry use cases), not just demonstrate the SQL.
- [x] `workflows/propensity_score_matching/` — does maternal smoking during pregnancy affect birth weight, estimated on `bigquery-public-data.samples.natality` (year 2005, real non-synthetic US birth-certificate data). **Two dataset candidates were tried and rejected live before landing on natality, a genuinely important finding in its own right**: `thelook_ecommerce`/`ga4_obfuscated_sample_ecommerce` (a marketing-channel "treatment") showed essentially flat covariates/outcomes across channels — no real confounding baked into that synthetic data. `bigquery-public-data.cms_synthetic_patient_data_omop` (metformin vs. glyburide, the textbook pharmacoepi PSM comparative-drug-effectiveness example) looked ideal on paper and was fully built out — a real "new-user" cohort design (first-ever exposure, excluding prior exposure to the other drug), real covariates (age, comorbidity count, prior renal disease), real query-cost verification (~$0.11 for a full 23GB scan) — but live queries showed the *synthetic* OMOP data doesn't encode the real-world "confounding by indication" story at all: covariates were nearly identical between drug groups (72.3 vs. 72.2 avg age, 0.244 vs. 0.244 renal-disease rate), mortality was far too rare to use as an outcome (5 deaths per ~135K-person group), and even new-onset hypoglycemia — the actual textbook sulfonylurea-vs-metformin safety difference — showed no real difference (2.08% vs. 2.10%, if anything reversed from the known clinical direction). Presented this finding to the user via `AskUserQuestion` (options: force a semi-synthetic simulated-treatment design on top of OMOP, keep hunting for a different real drug pair, or switch datasets entirely) — user chose to find real data instead, which led directly to `natality`'s `cigarette_use`/birth-weight relationship, verified live to have both real confounding (smokers: avg. age 25.3 vs. 27.8, married rate 36% vs. 66%) and a real effect (6.80 vs. 7.25 lb avg. birth weight). **Two more real BigQuery-specific gotchas found live during the matching step**: (1) `day` is `NULL` for every single 2005 natality row (real de-identification suppression in the public dataset) — an initial cohort-sampling hash built on it silently zeroed out the entire cohort via `CONCAT`'s NULL-propagation, fixed by hashing only on confirmed-non-null columns; (2) BigQuery rejects a correlated subquery referencing a table inside a `JOIN ... ON` predicate (`"Unsupported subquery with table in join predicate"`) and, separately, a direct inequality self-join for nearest-neighbor matching at full population scale (240K treated × 2M control) blew through the on-demand CPU-to-bytes-billed ratio limit (`"Query exceeded resource limits"`) even though the output was tiny — fixed by computing the caliper as its own query first (avoiding the subquery-in-join restriction) and by sample-size discipline: the cohort itself is built at a deterministic ~1.5% sample (3,604 treated / 30,428 control) sized so every step — propensity model, matching self-join, IPTW — runs fast and cheap directly, with the bucketed-equi-join pattern documented as the scalable alternative if a reader needs full population scale. Second workflow's core method — inverse probability of treatment weighting (IPTW) — built alongside matching for an honest, two-technique comparison matching this project's `ensembling`/`anomaly_fraud_detection` habit. **Real, non-dramatic honest finding**: naive/matched/IPTW effect estimates all land in a narrow band (roughly -0.40 to -0.43 lbs) despite the demonstrably real confounding — the measured confounders are real and imbalanced but aren't the dominant driver of the naive birth-weight gap, and the smoking effect looks robust to adjusting for them. Explicitly notes PSM's central limitation (corrects only for measured confounders) rather than overclaiming causal certainty. **Caught during the user's own live run (differed from the pre-validation/`nbclient` run): `LOGISTIC_REG`'s `AUTO_SPLIT` randomizes the train/eval split on every retrain, shifting fitted propensity scores slightly run to run** — the user's shipped run showed matched (-0.4095) landing *further* from zero than naive (-0.4001), the opposite direction from an initial draft's "matching pulls toward zero, IPTW pushes further" claim (written against a different retrain). Resynced the notebook's honest-finding markdown to state only what's actually robust across reruns (the narrow band, the "confounders don't dominate" conclusion) plus an explicit non-determinism hedge, rather than a specific direction that can flip — same discipline already established for `ga4_churn_prediction`/`ensembling`'s non-deterministic figures. Upfront overview includes a cross-industry use-case table (healthcare/critical care, labor economics/policy, marketing/pharma sales, education policy, legal/policy) with real citations researched via `WebSearch`. Built and fully pre-executed via `nbclient`, then re-verified against the user's own independent run; outputs cleared for handoff.

**Scope amendment (decided 2026-07-29, post-PSM): survival analysis, with an upfront honest limitation.** User asked whether survival analysis (time-to-event, censoring) would fit, noting correctly that BQML has no model type for Cox Proportional Hazards. Explicit instruction: build the notebook, state right up front (before building anything) that Cox isn't possible directly in BQML and would require reading data into Python with a real package (name real options), *then* show what genuinely is possible natively. Confirmed via `AskUserQuestion` that the Cox limitation should include a small, real, working example (via `lifelines`), not prose-only naming.
- [x] `workflows/survival_analysis/` — time-to-first-purchase on `bigquery-public-data.ga4_obfuscated_sample_ecommerce`, reusing `ga4_churn_prediction`'s exact cohort (first-time visitors, 2020-11-01 to 2020-12-24) with a uniform 35-day follow-up window instead of that workflow's 30-day churn label. **Overview states upfront, before any building, that Cox PH is not possible natively**: its partial-likelihood/risk-set estimation is row-dependent, and every BQML model type fits row-independent likelihoods — no `CREATE MODEL` incantation bridges that gap. Names three real external options (`lifelines`, used live below; `scikit-survival`; `statsmodels.duration.hazard_regression.PHReg`) and states the real tradeoff plainly (data has to leave BigQuery). **What genuinely is possible natively, built for real**: (1) Kaplan-Meier survival curves via pure SQL (standard product-limit estimator, `EXP(SUM(LN(1-hazard)) OVER (...))`), segmented by first-week activity band — verified live to separate dramatically and monotonically (survival at week 5 ranges ~99.9% for 1-5 events down to ~70.2% for 50+ events); (2) a discrete-time hazard model — person-period reshape (`CROSS JOIN UNNEST(GENERATE_ARRAY(...))`, one row per user per at-risk week) + `LOGISTIC_REG` predicting the per-period event indicator, `roc_auc` ~0.901, all activity-covariate weights positive (higher activity → higher hazard). **Also fits real Cox PH via `lifelines`, not just naming it**: hit and fixed two genuine convergence errors along the way — fitting on `device_category`+`n_events`+`n_active_days`+`n_sessions` together failed with `ConvergenceError: ... high collinearity ... singular matrix` (these activity features are highly mutually correlated); dropping to just `n_events` hit a *different* error, `ConvergenceError: delta contains nan value(s)`, traced to `n_events`' extreme right-skew (median ~5, max ~1,298) destabilizing Cox's Newton-Raphson optimizer — fixed by log-transforming (`log1p`). Resulting Cox fit: hazard ratio ~4.3 for log-activity (p<0.001), concordance ~0.95 — same direction as the KM curves and the discrete-hazard model's positive weight, three independently-built techniques converging on one answer. **Dataset lesson recurs a second time on a second technique**: `thelook_ecommerce` was checked first (repeat-purchase rate / time-to-second-order by `traffic_source`, `gender`×`age_band`, first-order size) and again showed flat ~42-46% rates / ~460-490 avg days across every segment — the same synthetic-data-doesn't-encode-real-relationships finding as PSM's rejected candidates, now confirmed on a completely different technique and dataset check. Honest finding explicitly restates both limitations found: Cox required leaving BigQuery (a real, disclosed-upfront gap, not a late discovery), and the discrete-time model's weekly granularity coarsens exact event timing versus continuous-time Cox. Built and fully pre-executed via `nbclient` twice (once before, once after a legend-ordering polish fix); markdown numbers checked against the actual executed output, not an earlier scratch validation run (an initial "~4.29" hazard-ratio draft, written from a pre-notebook scratch check, was corrected to the notebook's own executed "~4.3"/4.32 once the final assembled notebook actually ran) — outputs cleared for handoff.

**Scope amendment (decided 2026-07-29, post-survival_analysis): 4 more workflows, verified against a real "is this still current in industry" challenge.** User asked for more workflow ideas; proposed DiD, uplift/CATE, multi-touch attribution, and price elasticity, then pushed back: "are these all real approaches that are still used in industry today? Want to make sure we are covering how to do real work all in BigQuery directly but not making it look easy by using an antiquated method." Verified via `WebSearch`, not assumed — this changed the picks twice: multi-touch attribution is genuinely degrading (cookie/IDFA loss cut its coverage from 90%+ to 30-60%; 2026 practice has shifted to Marketing Mix Modeling), and naive price-elasticity regression is now considered biased (2018+ standard is Double Machine Learning). MMM itself was then researched live and dropped — no real ad-spend data exists in `bigquery-public-data` (checked channel-volume proxies, AdWords cost/campaign fields, GA4 campaign names — all absent, scrubbed, or fully obfuscated) — presented this honestly via `AskUserQuestion` rather than shipping a weak-signal build; user chose **Synthetic Control** instead, reusing the DiD dataset already verified. Final four: DML price elasticity and uplift/CATE (fully native), DiD and synthetic control (native core + honest external-package boundary, mirroring `survival_analysis`'s Cox PH pattern).
- [x] `workflows/price_elasticity_dml/` — Double Machine Learning (Chernozhukov et al. 2018) for price elasticity, fully native. Cross-sectional design across 1,130 real Iowa liquor SKUs (`bigquery-public-data.iowa_liquor_sales.sales`, 2022-2023, 750ml, 8 categories) — retail price is state-fixed over time (ruling out a time-series design) but genuinely varies across brands, correlated with a real confounder (distribution breadth: `CORR(log_price, log_n_stores) = -0.28`). Naive `LINEAR_REG` elasticity ≈ -1.4; 5-fold cross-fitting (reusing `cross_validation/`'s exact fold-assignment pattern) with `BOOSTED_TREE_REGRESSOR` nuisance models for outcome and treatment, residual regression gives DML-corrected elasticity ≈ -0.7 — roughly half the naive magnitude was confounding, not a true price effect. **Real join-key gotcha found live twice this batch** (also hit in `uplift_cate/`): `item_number` isn't unique across categories (3 SKUs appear in 2 categories each) — an initial residuals table came back with 1,136 rows instead of the expected 1,130 before adding a synthetic `ROW_NUMBER()` `row_id` and joining on that instead. Fold-model training cell initially timed out at 1200s training 10 `BOOSTED_TREE_REGRESSOR` models sequentially — fixed by submitting all 10 jobs asynchronously first (reusing `cross_validation/`'s `client.query()`-without-`.result()` concurrent-submission pattern) before awaiting them. Built and fully pre-executed via `nbclient` (three passes: one hit the timeout, one after the async-submission fix, one final with hedged non-determinism language since `BOOSTED_TREE_REGRESSOR`'s residuals shift the DML figure slightly run to run, -0.71 to -0.74 observed) — outputs cleared for handoff.
- [x] `workflows/uplift_cate/` — a T-learner (two independent `BOOSTED_TREE_CLASSIFIER` models, one per treatment arm) for CATE estimation, fully native. Dataset: `bigquery-public-data.google_analytics_sample` (the older Universal Analytics/GA360 Merchandise Store export, 2016-08-01 to 2017-08-01 — distinct from `ga4_obfuscated_sample_ecommerce`). Treatment: paid marketing (`Paid Search`/`Display`) vs. organic search, on 413,149 real sessions. Verified real average effect (0.902% → 1.937% conversion, ~2.1x) AND real heterogeneity (desktop uplift +1.71pp vs. tablet's +0.16pp, ~10x; new-visitor +0.90pp vs. returning-visitor's +0.30pp, ~3x) — the entire point of a CATE workflow, not just an average effect. Qini curve (computed in pandas: cumulative incremental conversions by predicted-CATE rank vs. random) shows real front-loaded separation in the top ~30% of the population, honestly described as such rather than smoothed into a uniformly-above-diagonal shape it doesn't have. **Same join-key class of gotcha as `price_elasticity_dml/`**: `fullVisitorId`+`visitId` isn't a perfectly unique session key (903,653 rows vs. 902,755 distinct combinations) — fixed with a synthetic `row_id`. **A second real gotcha**: `BOOSTED_TREE_CLASSIFIER`'s `predicted_purchased_probs` array is not guaranteed sorted by ascending label value — verified live it orders `[label='1', label='0']` here; an initial `WHERE label = '1'` (string literal) also failed with a type mismatch once retested, since the label column is actually `INT64` — fixed to `WHERE label = 1`, the same explicit-filter pattern (not positional `OFFSET`) already used in `propensity_score_matching/`. Built and fully pre-executed via `nbclient`; outputs cleared for handoff.
- [x] `workflows/difference_in_differences/` — states plainly that naive two-way-fixed-effects (TWFE) DiD is unreliable under staggered treatment timing (Goodman-Bacon 2021, Callaway-Sant'Anna 2021), then proves it with real data. Dataset: `bigquery-public-data.covid19_open_data.covid19_open_data`, Texas's real 2020-07-03 mask mandate vs. Georgia (a real untreated comparison — Georgia's governor barred local mandates during this window). Clean 2x2 `LINEAR_REG` DiD = -19.29, exactly matching an independent `statsmodels.OLS` fit. **Major, previously-undocumented BQML gotcha found live**: `LINEAR_REG`'s default `optimize_strategy='AUTO_STRATEGY'` chose iterative gradient descent that stopped after 8 iterations without converging on this small (28-row), collinear (`treated`/`post`/their product) design — silently returning -6.19, 68% smaller than the true -19.29, with no error or warning; `ML.TRAINING_INFO` showed the loss still decreasing at the final iteration. `optimize_strategy='NORMAL_EQUATION'` fixes it (confirmed this does NOT affect `price_elasticity_dml/`'s larger, less collinear 1,130-row single-predictor regression). Honest finding on post-period-horizon choice: DiD estimated against the full pre-period average starts *positive* at 2 post-weeks (+7.45, the pre-period's own upward trend still dominating), crosses to negative by 3 weeks (-5.20), reaches -19.29 by 5 — a too-short window can give a directionally wrong answer, not just an imprecise one. Staggered-timing demo (9 real states, real different mandate dates, `statsmodels` TWFE with state+week fixed effects) showed naive TWFE substantially understating the true effect (-7.36 vs. -19.29/-31.79) in the shipped run. **A genuinely real live-data-instability finding**: a scratch check of the identical staggered-TWFE query, run during planning before the notebook was finalized, returned +9.78 — the WRONG SIGN. `covid19_open_data` is a live public dataset that receives revisions to historical rows; re-running the identical query later gave -7.36. Documented both figures in the notebook rather than picking one — the literature's point (naive TWFE bias ranges from attenuation to full sign-flip) held both times, just via different failure modes. Bridged to the real, installable `differences` package (Callaway-Sant'Anna estimator): ATT = -31.79, same direction as and larger in magnitude than the clean 2x2 DiD, consistent with naive TWFE badly understating the effect. Built and fully pre-executed via `nbclient`; markdown updated post-execution to match the real run's numbers (not a stale scratch check) before outputs were cleared for handoff.
- [x] `workflows/synthetic_control/` — extends `difference_in_differences/`'s panel to a 13-state donor pool (every state whose `facial_coverings` level never reached Texas's post-mandate level 3), replacing DiD's single somewhat-arbitrary comparison state (Georgia) with an optimized weighted blend. States upfront that the real constrained optimization (weights ≥ 0, summing to 1 — a literal blend of real states) is a quadratic program no `CREATE MODEL` option expresses, then proves the native unconstrained `LINEAR_REG` approximation's failure concretely rather than asserting it: several donor weights come back negative (South Dakota ≈ -1.28, North Dakota ≈ -1.07, New Hampshire ≈ -0.90, Virginia ≈ -0.82) and the full set sums to roughly -0.83, nowhere near 1 — compounded by a genuinely underdetermined system (9 pre-period weeks vs. 13 donors, more unknowns than equations). **Confirmed live that the underdetermined system's exact unconstrained weights are themselves unstable**: a scratch check during planning produced a visibly different extreme-weight pattern than this notebook's own final run — the general finding (negative/out-of-range weights, no valid blend) reproduces; the exact numbers don't, and the notebook says so rather than asserting one set as canonical. Real constrained fit (`scipy.optimize`, matching `pysyncon`'s approach): 82.4% Georgia + 17.6% Idaho, pre-period MSE improves from 189.98 (Georgia alone) to 143.95 (~24% better fit) — genuinely re-discovers Georgia as dominant but improves on it. Post-period ATT = -19.28, closely corroborating `difference_in_differences/`'s own 2x2 estimate (-19.29) — two independently-built counterfactuals agreeing, the same "converging evidence" pattern as `survival_analysis`. Built and fully pre-executed via `nbclient`; outputs cleared for handoff.

---

## Maintenance & Audit

Keep our content in sync with the official BigQuery ML documentation. Mirrors the bq-ai-functions audit discipline.

### Change types and checklists

#### New model type
- [ ] `RESOURCES.md`: Add a full entry in the CREATE MODEL catalog + the lifecycle functions it uses; update any comparison table
- [ ] `README.md`: Add a row to the Models table; update the diagram if needed
- [ ] Create `models/{name}/` with `{name}.ipynb` (full lifecycle template) + `{name}.sql` (progressive examples)
- [ ] Pre-validate all SQL with `bq query` (train + drop validation models)
- [ ] Cross-reference: add **Featured in:** if used by a workflow/pipeline
- [ ] `PLANS.md`: check the Development Phases item; update the mapping table; add an audit-log entry
- [ ] If this changes a decision tree, introduces a new cross-cutting gotcha, or adds a new head-to-head comparison with `bq-ai-functions`: update `../../agent-skills/.agents/skills/bigquery-ml/` (SKILL.md and/or the relevant `reference/*.md`)

#### New model-free function
- [ ] `RESOURCES.md`: Add a full entry in the Model-Free Functions section
- [ ] `README.md`: Add to the Functions table
- [ ] Create `functions/{name}/` with `{name}.ipynb` + `{name}.sql`
- [ ] Cross-reference + `PLANS.md` mapping + audit-log entry
- [ ] If this changes a decision tree, introduces a new cross-cutting gotcha, or adds a new head-to-head comparison with `bq-ai-functions`: update `../../agent-skills/.agents/skills/bigquery-ml/` (SKILL.md and/or the relevant `reference/*.md`)

#### New workflow
- [ ] Create `workflows/{name}/` notebook
- [ ] `README.md`: Add to the Workflows table
- [ ] Cross-reference: **Models used:** / **Functions used:** in the workflow; **Featured in:** on each component
- [ ] `PLANS.md`: mapping table + audit-log entry
- [ ] If this changes a decision tree, introduces a new cross-cutting gotcha, or adds a new head-to-head comparison with `bq-ai-functions`: update `../../agent-skills/.agents/skills/bigquery-ml/` (SKILL.md and/or the relevant `reference/*.md`)

#### New pipeline
- [ ] Create `pipelines/{approach}/` content
- [ ] `README.md`: Add to the Pipelines table
- [ ] Cross-reference: **Workflow operationalized:** link
- [ ] `PLANS.md`: mapping table + audit-log entry
- [ ] If this changes a decision tree, introduces a new cross-cutting gotcha, or adds a new head-to-head comparison with `bq-ai-functions`: update `../../agent-skills/.agents/skills/bigquery-ml/` (SKILL.md and/or the relevant `reference/*.md`)

#### Status / capability change (Preview→GA, new option, new model_type, etc.)
- [ ] Update `RESOURCES.md` entry (status, options, syntax, outputs, limitations)
- [ ] Update `README.md` status labels and tables
- [ ] If it affects a built notebook, revise + re-run (Restart & Run All) before review
- [ ] `PLANS.md`: audit-log entry
- [ ] If this changes a decision tree, introduces a new cross-cutting gotcha, or adds a new head-to-head comparison with `bq-ai-functions`: update `../../agent-skills/.agents/skills/bigquery-ml/` (SKILL.md and/or the relevant `reference/*.md`)

### How to run an audit
1. **Prepare** — review the audit log; note Google Cloud Next / release-note announcements since last audit.
2. **Fetch and compare by category** — for each documented item, fetch its doc URL (table below) and compare against `RESOURCES.md`: status, options/parameters, syntax, outputs, limitations, new `model_type` values, new `ML.*` functions.
3. **Classify each change** and apply the relevant checklist above.
4. **Update files in order:** RESOURCES.md entries → RESOURCES.md comparison tables → README.md tables/diagram → PLANS.md (URLs table, mapping, audit log).
5. **Check for new items** — new model types or `ML.*` functions. If docs exist, add full coverage; if only announced, add to Tracked-upcoming.
6. **Verify consistency** — grep status labels across RESOURCES/README/PLANS; confirm the URL table is complete.
7. **Identify notebook impacts** — list notebooks needing a fresh Restart & Run All.

### Documentation URLs

| Item | Documentation URL |
|------|-------------------|
| The CREATE MODEL statement | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create |
| CREATE MODEL (GLM: linear & logistic reg) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-glm |
| CREATE MODEL (Boosted Tree) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-boosted-tree |
| CREATE MODEL (Random Forest) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-random-forest |
| CREATE MODEL (DNN) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-dnn-models |
| CREATE MODEL (K-means) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-kmeans |
| CREATE MODEL (PCA) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-pca |
| CREATE MODEL (Matrix Factorization) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-matrix-factorization |
| CREATE MODEL (ARIMA_PLUS) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-time-series |
| CREATE MODEL (Autoencoder) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-autoencoder |
| CREATE MODEL (Imported TF/ONNX/XGBoost) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-tensorflow |
| CREATE MODEL (Remote) | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-create-remote-model |
| Hyperparameter tuning | https://cloud.google.com/bigquery/docs/hp-tuning-overview |
| ML.EVALUATE | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-evaluate |
| ML.PREDICT | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-predict |
| ML.FORECAST | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-forecast |
| ML.RECOMMEND | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-recommend |
| ML.EXPLAIN_PREDICT | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-explain-predict |
| ML.GLOBAL_EXPLAIN | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-global-explain |
| ML.CONFUSION_MATRIX | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-confusion |
| ML.ROC_CURVE | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-roc |
| ML.FEATURE_INFO | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-feature |
| ML.TRAINING_INFO | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-train |
| ML.TRIAL_INFO | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-trial-info |
| ML.TRANSFORM / preprocessing functions | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-preprocessing-functions |
| EXPORT MODEL | https://cloud.google.com/bigquery/docs/reference/standard-sql/bigqueryml-syntax-export-model |
| BigFrames ML (bigframes.ml) | https://cloud.google.com/python/docs/reference/bigframes/latest/bigframes.ml |

### Tracked upcoming

Items announced but without published reference docs, or not yet covered. Move to full coverage when ready.

| Item | Category | Status | Notes |
|------|----------|--------|-------|
| (none yet) | | | |

### Audit log

| Date | Scope | Changes |
|------|-------|---------|
| 2026-06-19 | Project bootstrap | Created the bq-ml project parallel to bq-ai-functions: folder skeleton (models/functions/workflows/pipelines/setup), PLANS.md operating manual, RESOURCES.md (CREATE MODEL catalog + lifecycle/model-free/management sections; full entries for logistic regression and the functions it uses, stubs + doc links for the rest), README.md map, setup/README.md. Built the canonical reference model `models/logistic_regression/` (notebook + SQL) on `census_adult_income`, covering CREATE MODEL → ML.EVALUATE → confusion matrix/ROC → ML.PREDICT → ML.EXPLAIN_PREDICT → ML.GLOBAL_EXPLAIN → ML.FEATURE_INFO/TRAINING_INFO → TRANSFORM clause → hyperparameter tuning (ML.TRIAL_INFO) → magics → BigFrames → cleanup. All SQL pre-validated against BigQuery (caught: ML.GLOBAL_EXPLAIN requires enable_global_explain=TRUE at CREATE MODEL time). Backlog for all other models/functions/workflows/pipelines recorded in Development Phases. |
| 2026-06-20 | RESOURCES.md full build (docs-first) | Replaced the stub RESOURCES.md (198 lines) with a comprehensive reference (~4,400 lines) built docs-first via an exhaustive multi-agent research pass (Google Cloud BQML docs as source of truth + this repo's tested notebooks/SQL for real syntax). Locked an entry schema first (per-model-type Template A, per-function Template B) + a scope-boundary policy (cross-link, don't duplicate, bq-ai-functions-owned topics: contribution analysis→AI.KEY_DRIVERS, TimesFM→AI.FORECAST/AI.EVALUATE/AI.DETECT_ANOMALIES, doc processing, LLM text/foundation embeddings, remote-LLM). Coverage: 5 comparison tables (model_type catalog, task→eval metrics, capability matrix, explainability, connection) + full entries for **all** model types (GLM, boosted tree, random forest, DNN, wide-and-deep, AutoML, k-means, PCA, autoencoder, matrix factorization, ARIMA_PLUS, ARIMA_PLUS_XREG, contribution analysis, imported TF/TFLite/ONNX/XGBoost, remote mechanism, transform-only) + all lifecycle functions (EVALUATE/PREDICT/CONFUSION_MATRIX/ROC_CURVE/EXPLAIN_PREDICT/GLOBAL_EXPLAIN/FEATURE_IMPORTANCE/WEIGHTS/ADVANCED_WEIGHTS/FEATURE_INFO/TRAINING_INFO/TRIAL_INFO/CENTROIDS/PRINCIPAL_COMPONENTS/_INFO/RECONSTRUCTION_LOSS/RECOMMEND/GENERATE_EMBEDDING(in-house)/FORECAST/EXPLAIN_FORECAST/ARIMA_EVALUATE/ARIMA_COEFFICIENTS/HOLIDAY_INFO/DETECT_ANOMALIES/TRANSFORM) + model-free families (scalers, bucketizers, encoders, feateng, text, distance, image) + management/monitoring (EXPORT MODEL, DESCRIBE_DATA, VALIDATE_DATA_SKEW/DRIFT, TFDV_DESCRIBE/VALIDATE). Each entry cites a tested repo example where one exists; gaps with no example flagged (ML.RECOMMEND, CONTRIBUTION_ANALYSIS, TF_IDF/BAG_OF_WORDS, image fns, LP_NORM). Research-discovered corrections: `ML.TRANSPOSE` is NOT a real function (it's the TRANSFORM-clause technique); encoder defaults now top_k=32000/frequency_threshold=5. Per-notebook RESOURCES refinement from running code remains the standing practice as notebooks are built. |
| 2026-06-20 | Phase 2: `models/linear_regression/` | Built the second model type: `LINEAR_REG` on `bigquery-public-data.ml_datasets.penguins` (label `body_mass_g`; rows with NULL label or unrecorded `sex` filtered out; 333 clean rows). Notebook + SQL mirror the `logistic_regression` skeleton, swapping classification-only steps (confusion matrix/ROC) for `ML.WEIGHTS` (interpretable regression coefficients — a differentiator from the classifier example). Lifecycle: CREATE MODEL → ML.EVALUATE → ML.PREDICT → ML.EXPLAIN_PREDICT → ML.GLOBAL_EXPLAIN → ML.WEIGHTS → ML.FEATURE_INFO/TRAINING_INFO → TRANSFORM clause → hyperparameter tuning (ML.TRIAL_INFO) → magics → BigFrames → cleanup. All SQL pre-validated end-to-end against BigQuery via `bq query` (confirmed: BQML auto-selects the `NORMAL_EQUATION` solver for this small/unregularized problem — a single closed-form training pass with no `eval_loss`, matching the RESOURCES.md GLM entry's note). Notebook cells built with validated source but not yet executed in a live kernel — needs a Restart & Run All pass to populate outputs before being treated as a second verified canonical reference. |
| 2026-06-20 | RESOURCES.md refinement from running code (task standing practice) | User ran `models/linear_regression/linear_regression.ipynb` (Restart & Run All); reviewing the real outputs against a second independent `bq query` pre-validation run surfaced a genuine, non-obvious BQML gotcha: with the default `category_encoding_method = 'ONE_HOT_ENCODING'`, `ML.WEIGHTS`/`ML.GLOBAL_EXPLAIN` per-category values for a GLM are **not uniquely identified** (one-hot dummies + intercept are collinear) — confirmed by retraining the identical model twice and watching an `island` category's weight swing from +305/+353/+340 to −39/−4/+8.6 between runs (different scale, different sign) while `ML.PREDICT`/`ML.EVALUATE` stayed stable. Fix: `category_encoding_method = 'DUMMY_ENCODING'` pins one baseline category per feature to `weight: 0.0`, making the rest stable, well-defined deltas. Updated `linear_regression.sql`/`.ipynb` (added the option to `CREATE MODEL`, explained why in cell markdown, cleared now-stale outputs pending a fresh Restart & Run All) and `RESOURCES.md` (GLM entry best-practices/limitations, `ML.WEIGHTS` entry best-practices/limitations/repo-example). Also caught and fixed a research-pass citation error: the `ML.WEIGHTS` entry had cited `data+ai/bq-ml/models/logistic_regression/logistic_regression.sql` as a `ML.WEIGHTS` example — that file does not use `ML.WEIGHTS` (only the repo's separate `03a - BQML Logistic Regression.ipynb` does); corrected to cite `linear_regression.sql` instead, which now legitimately demonstrates it. |
| 2026-06-20 | Phase 2: `models/linear_regression/` — final verification | User re-ran the notebook (Restart & Run All) after the `DUMMY_ENCODING` fix. Reviewed all outputs: `ML.WEIGHTS` now shows clean baselines (`species`=Adelie, `island`=Biscoe, `sex`=MALE all pinned to `weight: 0.0`) with stable deltas for every other category; this ties out exactly with `ML.EXPLAIN_PREDICT`, where Adelie-species rows correctly show a `species` attribution of `0.0`. `ML.EVALUATE`'s `r2_score` (0.875223) and all `ML.PREDICT` values are bit-for-bit identical to the pre-fix run, confirming `DUMMY_ENCODING` changed only weight interpretability, not model fit. Noted one more nuance for RESOURCES.md: across 2 independent runs with different random `AUTO_SPLIT`s, `DUMMY_ENCODING`'s dropped baseline category was identical both times and was consistently the *most frequent* category rather than the alphabetically-first one (`sex`: `MALE`, not alphabetically-earlier `FEMALE`, was the baseline both times) — added to the `category_encoding_method` option description. `models/linear_regression/` is now a fully verified second canonical reference alongside `models/logistic_regression/`. |
| 2026-06-20 | Phase 2 backlog: classifier/regressor split convention | Decided the structuring convention for the remaining Phase 2 techniques (boosted tree, random forest, DNN, wide-and-deep — each pairs a classifier + regressor `model_type`): build **separate folders/notebooks per `model_type`**, mirroring the `logistic_regression`/`linear_regression` precedent, rather than one combined notebook per technique. Expanded the Phase 2 backlog from 3 combined bullets to 6 explicit items (`boosted_tree_classifier`/`_regressor`, `random_forest_classifier`/`_regressor`, `dnn_classifier`/`_regressor`) plus 2 stretch items for wide-and-deep. RESOURCES.md continues to document each classifier/regressor pair under one combined heading (as it already does for GLM) — only the notebook/folder structure splits. |
| 2026-06-20 | Phase 2: `models/boosted_tree_classifier/` | Built the third model type: `BOOSTED_TREE_CLASSIFIER` on `bigquery-public-data.ml_datasets.census_adult_income` — deliberately reusing the exact data/label from `models/logistic_regression/` so the two notebooks form a direct "same problem, different technique" comparison (e.g. `roc_auc` side by side). Notebook + SQL mirror the `logistic_regression` skeleton, adding `ML.FEATURE_IMPORTANCE` (tree-specific split-based importance; N/A for GLMs) alongside `ML.GLOBAL_EXPLAIN`, and noting the two can rank features differently (attribution vs. split-based — expected, not a bug). TRANSFORM step uses `ML.QUANTILE_BUCKETIZE` (new preprocessing function for this project, vs. `ML.STANDARD_SCALER` in the GLM notebooks) to also broaden preprocessing-function coverage. Lifecycle: CREATE MODEL → ML.EVALUATE → ML.CONFUSION_MATRIX/ROC_CURVE → ML.PREDICT → ML.EXPLAIN_PREDICT → ML.GLOBAL_EXPLAIN/ML.FEATURE_IMPORTANCE → ML.FEATURE_INFO/TRAINING_INFO → TRANSFORM → HP tuning (ML.TRIAL_INFO) → magics → BigFrames → cleanup. All SQL pre-validated end-to-end against BigQuery via `bq query`. Confirmed a real runtime characteristic worth noting: `ML.TRAINING_INFO` iteration numbering starts at **1** (not 0, as in the GLM notebooks), and the first boosting iteration pays a large one-time data-loading/indexing cost (~14 min observed) before subsequent iterations run in milliseconds — documented in the notebook so it doesn't read as a stall. Also caught during HP-tuning validation: 1 of 6 trials transiently `FAILED` (`error_message = "An internal error happened during trial training."`, `NULL` objective metric) without failing the overall job — documented as a general `ML.TRIAL_INFO` gotcha (not boosted-tree-specific) in both the notebook/SQL and RESOURCES.md's `ML.TRIAL_INFO` entry. Notebook built with validated source but not yet executed live — pending a Restart & Run All pass. |
| 2026-06-20 | Phase 2: `models/boosted_tree_classifier/` — review + tree-visualization add | User ran Restart & Run All; reviewed all outputs (clean, matched pre-validation). Caught and fixed three issues: **(1)** a stale cross-reference — Step 1's markdown pointed to "(Step 8)" for `ML.TRAINING_INFO`, which actually lives in Step 6 (the notebook's Step numbering merges/splits differently than the `.sql` file's Example numbering; the two must each be internally self-consistent, not copied across); **(2)** the BigFrames comparison cell trained a plain `XGBClassifier()` with no class balancing while the SQL model uses `auto_class_weights=TRUE` — verified via `inspect.signature()` that `bigframes.ml.ensemble.XGBClassifier` has **no** `class_weight` parameter at all (unlike `LogisticRegression`, which does), so this is a real, permanent parity gap, not a fixable omission — documented inline rather than silently misleading; **(3)** added a new **Step 7 — Visualize a tree by exporting the model** per the user's suggestion, fully validated end-to-end first (`EXPORT MODEL` → download `model.bst` → load with `xgboost` → `xgboost.plot_tree()`). Verified two real gotchas in the process: exported boosted-tree/random-forest models use XGBoost 0.82's legacy binary format, unreadable by modern xgboost 2.0+ (must pin `xgboost==1.7.6`); and the export does not preserve `feature_names` (reassigned manually from the training query's column order, confirmed correct against actual split thresholds). Added a `BUCKET` config variable. Also corrected a stale, incorrect claim in this file's Resource Naming Strategy section (`EXPORT MODEL` does NOT need a connection — verified by testing; it already correctly said so in RESOURCES.md but this file had drifted). Documented all of this in RESOURCES.md (`EXPORT MODEL` entry, boosted-tree `BigFrames API` line) and as a new standing convention here (tree-ensemble notebooks get a visualization step; BigFrames parity must be checked against the actual installed signature, not assumed). `models/boosted_tree_classifier/` notebook updated with all fixes + the new step; cleared outputs, pending one more Restart & Run All. |
| 2026-06-20 | Phase 2: `models/boosted_tree_classifier/` — final verification | User re-ran the notebook (Restart & Run All) after all fixes. Reviewed every cell, including extracting and visually inspecting the rendered tree PNG from Step 7: it renders correctly with all 11 real feature names, and the run printed the expected `Loading model from XGBoost < 1.0.0` compatibility warning — confirming the `xgboost==1.7.6` pin works exactly as designed against a real (not toy) model. Cleanup deleted all 13 exported model files from GCS. All other steps (evaluate/confusion-matrix/predict/explain/importance/introspect/transform/HP-tuning/magics) matched the prior run's values exactly — boosted trees don't exhibit the GLM one-hot/intercept collinearity instability seen in `linear_regression`. `models/boosted_tree_classifier/` is now a fully verified third canonical reference. |
| 2026-06-20 | Phase 2: `models/boosted_tree_regressor/` | Built the fourth model type: `BOOSTED_TREE_REGRESSOR` on `bigquery-public-data.ml_datasets.penguins` (label `body_mass_g`) — deliberately reusing the exact data/label from `models/linear_regression/` for a direct technique comparison (`r2_score` 0.968 vs. 0.875 on identical data). Notebook + SQL copy the `boosted_tree_classifier` template (incl. the tree-visualization step) with regression-appropriate swaps: no `auto_class_weights` (regression has no class-weighting option), `TRANSFORM` uses `ML.LABEL_ENCODER` (a function not yet demonstrated in a model notebook) instead of `ML.QUANTILE_BUCKETIZE`. All SQL pre-validated end-to-end via `bq query`, including a standalone confirmatory export+load+plot test against the actual trained regressor (not just reusing the classifier's validation) — found one new, regressor-specific nuance: loading the exported booster prints an extra `reg:linear is now deprecated in favor of reg:squarederror` warning (harmless), documented alongside the shared `XGBoost < 1.0.0` warning in RESOURCES.md and the notebook. HP tuning this time had zero failed trials (contrast with `boosted_tree_classifier`'s 1-of-6, reinforcing that the earlier failure was transient/occasional, not systemic). Unlike the BigFrames caveat needed for the classifier, the regressor's BigFrames comparison is genuinely apples-to-apples (no weighting option is used in either the SQL or BigFrames path for regression). Notebook built with validated source; pending user Restart & Run All. |
| 2026-06-21 | Phase 2: `models/boosted_tree_regressor/` — final verification | User re-ran the notebook (Restart & Run All). Reviewed every cell and extracted/visually inspected the rendered tree PNG from Step 6: it matches the standalone pre-validation render exactly (`species<3` → `sex<2` → identical leaf values), and both expected warnings printed on load (`reg:linear` deprecation + `XGBoost < 1.0.0` compatibility). All other steps (evaluate/predict/explain/importance/introspect/transform/magics/BigFrames) matched pre-validation exactly, including the BigFrames step now being genuinely apples-to-apples with the SQL model (identical metrics on both sides — confirms the earlier fix was correct). HP tuning found a notably better model this run (`r2_score` 0.988 at `max_tree_depth=8`, vs. 0.968 in pre-validation) — expected run-to-run variability from random hyperparameter search, not a bug. Also reproducibly observed (both in pre-validation and this run) that `ML.TRAINING_INFO`'s `eval_loss` displays identically to `loss` at every iteration for this small dataset — a benign numerical curiosity, not documented further since it doesn't affect correctness or usage. Cleanup deleted all 8 exported GCS files. `models/boosted_tree_regressor/` is now a fully verified fourth canonical reference. |
| 2026-06-21 | Phase 2: `models/random_forest_classifier/` + `models/random_forest_regressor/` | Built the fifth and sixth model types together: `RANDOM_FOREST_CLASSIFIER` on `census_adult_income` (same data/label as `logistic_regression`/`boosted_tree_classifier`, three-way comparison) and `RANDOM_FOREST_REGRESSOR` on `penguins`/`body_mass_g` (same data/label as `linear_regression`/`boosted_tree_regressor`). Trained both in parallel background jobs to save wall-clock time; random forest trains notably faster than boosted trees (single-pass — ~5-7 min each vs. boosted trees' ~14-23 min) once past the first-job overhead. Three significant, verified findings from this pair: **(1)** `max_iterations` is not a valid `CREATE MODEL` option for `RANDOM_FOREST_*` at all — errors immediately (confirmed via `ML.TRAINING_INFO`: always exactly 1 iteration, `learning_rate=1.0`) — corrected an assumption from initial drafting (first attempt included it and failed). **(2)** A full-power random forest tree is far too dense to visualize (2,435 dump lines, depth 15 on the classifier's default-settings model) — `xgboost.plot_tree()` produces an illegible image even as SVG (which sidesteps the cairo bitmap-size limit but not the density). Fixed by training a small, separate shallow illustrative forest (`num_parallel_tree=10`, `max_tree_depth=3`) dedicated to the visualization step — verified this renders a clean, legible diagram — and recorded as a new standing addendum to the tree-visualization convention (RF-specific; does not apply to `BOOSTED_TREE_*`, whose early trees are naturally shallow). **(3)** On the small 333-row `penguins` dataset, `RANDOM_FOREST_REGRESSOR` genuinely underperforms both `BOOSTED_TREE_REGRESSOR` and `LINEAR_REG` (`r2_score` ≈ 0.74, best-tuned ≈ 0.76, vs. ≈ 0.97 and ≈ 0.88 respectively) — a real, reproducible comparison point (confirmed via HP tuning across a reasonable range), presented honestly in the notebook rather than hidden or hyperparameter-fished away. Also confirmed `bigframes.ml.ensemble.RandomForestClassifier` has no `class_weight` param (same gap as `XGBClassifier`) — same BigFrames caveat applied as `boosted_tree_classifier`. All SQL pre-validated end-to-end via `bq query` for both models including TRANSFORM (`ML.QUANTILE_BUCKETIZE` for classifier, `ML.LABEL_ENCODER` for regressor) and HP tuning (tuning `num_parallel_tree`/`max_tree_depth`). Both notebooks built with validated source; pending user Restart & Run All. |
| 2026-06-22 | Phase 2: `models/random_forest_classifier/` + `models/random_forest_regressor/` — final verification | User ran both notebooks (Restart & Run All), concurrently rather than back-to-back (longer per-cell wall-clock times observed, consistent with shared-project slot contention, not a defect). Reviewed every cell in both, including extracting and visually inspecting both rendered tree PNGs: both illustrative-forest diagrams are clean and legible (no `graph is too large` warning this time, confirming the fix), though the classifier's specific split structure matched an earlier standalone test exactly while the regressor's did not — leading to a new, useful finding (see below). Confirmed both zero-importance features (`island`, `culmen_length_mm`) reproduced exactly on the regressor. HP tuning showed a higher trial-failure rate under concurrent execution (2 of 6 failed on the classifier, vs. 1 of 6 in `boosted_tree_classifier`'s single-notebook run) — consistent with, not contradicting, the documented "occasional transient failure" gotcha. **New finding, and a correction to previously-recorded (but never actually verified) information:** the regressor's tuned model showed **two trials simultaneously marked `is_optimal = TRUE`** (tied `r2_score`) — disproving RESOURCES.md's prior unverified claim that "the smallest `trial_id` wins" implies a single `TRUE` row; corrected to state plainly that ties can produce multiple `TRUE` rows and callers needing exactly one should add an explicit tiebreak. Investigating this also surfaced the root cause of several "close but not bit-identical" values seen throughout this pair's validation and review: **`RANDOM_FOREST_*` retraining is inherently non-deterministic** (default `subsample`/`colsample_bynode` = 0.8, no exposed random seed) — unlike `BOOSTED_TREE_*`, which reproduced almost exactly across separate runs in earlier testing. Documented as a new RESOURCES.md limitation so it isn't mistaken for a bug in future model notebooks. Both `models/random_forest_classifier/` and `models/random_forest_regressor/` are now fully verified fifth/sixth canonical references. |
| 2026-07-16 | Phase 2: `models/dnn_classifier/` + `models/dnn_regressor/` | Built the seventh and eighth model types: `DNN_CLASSIFIER` on `census_adult_income` (same data/label as `logistic_regression`/`boosted_tree_classifier`/`random_forest_classifier`) and `DNN_REGRESSOR` on `penguins`/`body_mass_g` (same data/label as `linear_regression`/`boosted_tree_regressor`/`random_forest_regressor`). Every SQL statement pre-validated end-to-end via `bq query` before writing notebook cells, including several diagnostic runs beyond the final chosen config (documented below). **Major verified findings:** **(1)** DNN training in BigQuery ML is dramatically slower than every tree model built so far — a single baseline `CREATE MODEL` took 12-46 minutes (wall time varied heavily with concurrent BigQuery load in the project from my own parallel validation jobs — consistent with the established slot-contention gotcha, not a new one), for training sets of only ~32K and 333 rows respectively; per-iteration cost itself (~20-45s) is a small fraction of this, implying a large fixed DNN-worker startup cost. **(2)** With default settings (`learn_rate=0.001`, `early_stop=TRUE`) on unscaled numeric features, the classifier still reached a competitive `roc_auc≈0.89` despite `early_stop` cutting training to just 2 iterations, but the regressor was **badly broken** (`r2_score≈-27.5`, worse than predicting the mean) — classification tolerates unscaled inputs far better than regression here. **(3)** Diagnosed the regressor failure directly: `TRANSFORM` + `ML.STANDARD_SCALER` alone (the documented RESOURCES.md best practice) did **not** fix it (`r2_score` still ≈-27.4, verified) — the real binding constraint was `learn_rate` being far too conservative for a 333-row dataset combined with `early_stop`'s default sensitivity (training took only 2 gradient steps total before stopping). Scaling + a 50x higher `learn_rate` (0.05) + more iteration headroom together fixed it: `r2_score≈0.82`. This corrects/refines RESOURCES.md's prior best-practice framing, which implied scaling alone was the key lever. **(4)** HP tuning told two different stories: the classifier's tuning (`num_trials=4`, `HPARAM_RANGE(0.001,0.05)` on `learn_rate` + `HPARAM_CANDIDATES` on `hidden_units`) succeeded cleanly (`roc_auc` 0.895-0.897 across all trials, no failures). The regressor's identical-shape tuning (`HPARAM_RANGE(0.001,0.1)`) completely failed to find the good region — all 4 trials sampled `learn_rate` between 0.001-0.008 and all reproduced the catastrophic `r2_score≈-27`, even though a manually-chosen `learn_rate=0.05` (well within the searched range) fixes it. Presented honestly in the notebook as a real limitation of small tuning budgets, not smoothed over. **(5)** Verified `hidden_units = HPARAM_CANDIDATES([STRUCT([64,32]), STRUCT([32,16])])` is the correct working syntax for the `ARRAY<STRUCT<ARRAY<INT64>>>` tuning shape — RESOURCES.md previously only asserted this was the required type without a tested example. **(6)** Confirmed via the live BigFrames API reference that **no first-class DNN wrapper exists anywhere in `bigframes.ml`** (checked every module: `linear_model`, `ensemble`, `cluster`, `decomposition`, `forecasting`, `imported`, `llm`) — a permanent gap, not a version-specific omission as RESOURCES.md previously hedged. Both notebooks replace the usual BigFrames comparison cell with a markdown note explaining the gap. All validation models dropped after use. Both notebooks built with validated source; pending user Restart & Run All — expect a long wait (see finding 1) and do not run both concurrently with other BigQuery ML work in the same project if avoidable. |
| 2026-07-16 | Phase 2: `models/dnn_classifier/` + `models/dnn_regressor/` — final verification | User ran both notebooks (Restart & Run All). Classifier reviewed cell-by-cell: all metrics matched pre-validation almost exactly (`roc_auc` 0.8929 baseline, 0.8976 best-tuned trial), ROC curve PNG extracted and visually confirmed clean, tuning found no failed trials. **Reviewing the regressor surfaced a real gap in the notebook itself, not just a finding to document:** Step 6 ("the fix") never actually called `ML.EVALUATE` on the fixed model (`dnn_regressor_penguins_transform`) — the notebook asserted `r2_score≈0.82` in its markdown but gave the reader no cell to verify it, only a `ML.PREDICT` cell showing plausible-looking (but not conclusively correct) predictions. Fixed by inserting a `ML.EVALUATE` cell into Step 6 in both `dnn_regressor.ipynb` and `dnn_regressor.sql`, right after the model is created. **Separately, the actual tuning result in this run contradicted the "verified" claim written into Step 7's markdown:** the prior pre-validation run had all 4 tuning trials sample `learn_rate` between 0.001-0.008 and stay stuck at `r2_score≈-27`; this run's tuning (identical SQL, no seed control) had trial 4 sample `learn_rate=0.0275` and reach `r2_score≈0.87` — better than Step 6's hand-picked fix. This means the original claim ("tuning fails to find the good region") was an overgeneralization from a single run, not a reliable property of the config. Corrected the Step 7 markdown in both the notebook and `.sql` file, plus the corresponding RESOURCES.md limitation bullet, to frame this as "small-trial-budget tuning outcomes are not deterministic run-to-run" rather than a fixed failure — backed by both runs' actual `ML.TRIAL_INFO` values. Because the fix was inserted after the run, the new evaluate cell and the corrected markdown still need a fresh run of Step 6 onward (not necessarily a full Restart & Run All, since the kernel can be reused) before `models/dnn_regressor/` can be marked fully verified. `models/dnn_classifier/` is fully verified as-is. **Lesson for future notebooks: when a step's markdown makes a specific quantitative claim about a model's fitness (e.g. "reaches r2_score X"), always include the `ML.EVALUATE` cell needed to let the reader verify that claim themselves — don't rely on `ML.PREDICT` output looking plausible as a substitute.** |
| 2026-07-16 | Phase 2: `models/dnn_regressor/` — Step 6-onward re-run, second correction | User re-ran Step 6 onward after the `ML.EVALUATE` fix above. The new evaluate cell confirmed the fix works (`r2_score=0.8616`, close to the ~0.82 originally claimed). But comparing this run's `ML.TRIAL_INFO` output to the prior full run's revealed the previous correction (logged above, "tuning outcome is not reliable run-to-run") was **itself wrong** — both runs of `dnn_regressor_penguins_tuned` produced **bit-for-bit identical trial hyperparameters** (same 4 `learn_rate` values to 17 significant digits, same optimal trial). The apparent "run-to-run variance" in the prior correction was actually a same-name-vs-different-name artifact: the original comparison was between an ad-hoc `_validate`-suffixed pre-validation model and the real `_tuned` model — two DIFFERENT model names, not two runs of the same one. Corrected the framing a second time, now in the notebook, `.sql`, and RESOURCES.md (both the `DNN_CLASSIFIER`/`DNN_REGRESSOR` entry and the general `ML.TRIAL_INFO` entry): BQML's HP tuning search order appears tied to the model's identity (e.g. resource name) and is reproducible for a fixed name, but not guaranteed to transfer to a differently-named model with the same search config. **Separately, the re-run's `%%bigquery` magics cell failed with a real 404** (`Not found: Model ... dnn_regressor_penguins`) — not a notebook bug, but an artifact of the two-part re-run: the first full run's Cleanup cell already dropped the baseline model, and re-running only Step 6 onward never recreated it before the magics cell (which references the baseline) executed. Resolution: re-run Step 1's cell to recreate the baseline model, then re-run the magics cell, then Cleanup — no notebook code change needed for this part, since a normal single Restart & Run All never hits this (Cleanup only runs after the magics cell). **Lesson: don't generalize a "verified" cross-run finding from comparing two differently-configured/-named models — only compare truly identical reruns (same name, same SQL) when claiming something is or isn't deterministic.** `models/dnn_regressor/` still needs one more clean pass (recreate baseline + re-run magics cell) before final sign-off. |
| 2026-07-16 | Phase 2: `models/dnn_regressor/` — fresh Restart & Run All, third correction, final sign-off | User did a full kernel restart + Restart & Run All (not a partial re-run this time) — the `%%bigquery` magics cell now succeeds since the baseline model exists throughout. Comparing this third independent run's outputs against the prior two revealed the determinism finding is even broader than the second correction stated: **it's not just HP tuning's search order that's reproducible — the entire DNN training process is, for a fixed model name.** The baseline model's `ML.EVALUATE` (`r2_score = -27.351799`) and `ML.TRAINING_INFO` loss curve matched bit-for-bit across all three runs; the scaled+higher-learn-rate fix model's `ML.EVALUATE` (`r2_score = 0.861626`) matched bit-for-bit across its two runs. This meant Step 6's markdown claim of "~0.82" (from my original ad-hoc, differently-named pre-validation model) was itself an apples-to-oranges artifact, same mistake pattern as the tuning correction — the real, reproducible number under the actual notebook's model name is 0.861626, not ~0.82. Corrected the specific number (~0.82 → 0.86) and broadened the determinism framing in the notebook (Step 6 markdown, the new evaluate cell's comment, the top overview cell), `.sql`, and RESOURCES.md (both the DNN limitations/best-practices bullets and the general `ML.TRIAL_INFO` entry) to state plainly: DNN training under a fixed `CREATE OR REPLACE MODEL` name reproduces bit-for-bit (evaluation metrics, loss curves, and tuning trials all included) — a different model name is not guaranteed to follow the same trajectory. All other cells reviewed clean, no errors, tuned model's `is_optimal` trial confirmed the known-good `learn_rate≈0.0275` result a third time. `models/dnn_regressor/` and `models/dnn_classifier/` are both now fully verified seventh/eighth canonical references. **Standing lesson reinforced twice in one notebook: verify "run-to-run" claims by comparing the SAME model name across runs — comparing a throwaway `_validate`-suffixed pre-validation model against the real notebook model is not a valid same-config comparison, even when the SQL and options otherwise match.** |
| 2026-07-17 | Scope clarification: no stretch goals | User corrected the framing of `wide_and_deep_classifier`/`_regressor`: these are not optional "stretch goals" — the project's only goal is complete coverage of every BigQuery ML capability. Removed the "stretch goal" qualifiers from the Phase 2 backlog; all remaining backlog items (Phase 3+ unsupervised models, forecasting, imported/remote/exported models, model-free functions, workflows, pipelines) should be treated the same way — comprehensive, not optional. |
| 2026-07-17 | Phase 2: `models/wide_and_deep_classifier/` + `models/wide_and_deep_regressor/` | Built the ninth and tenth model types together: `DNN_LINEAR_COMBINED_CLASSIFIER` on `census_adult_income` (same data/label as the other four classifiers) and `DNN_LINEAR_COMBINED_REGRESSOR` on `penguins`/`body_mass_g` (same data/label as the other four regressors). All SQL pre-validated end-to-end via `bq query` before writing notebook cells. Applied every lesson learned from `models/dnn_classifier/`/`models/dnn_regressor/` rather than rediscovering them: **(1)** verified the identical small-data convergence failure occurs here too (unscaled features + default `learn_rate=0.001` + `early_stop` → `r2_score≈-27.4` on the regressor after 2 iterations) and applied the known fix directly (scale + `learn_rate=0.05` → `r2_score≈0.86`) rather than re-running the multi-step diagnostic; the regressor notebook cross-links `models/dnn_regressor/` for the detailed "why" instead of repeating it. **(2)** Included the `ML.EVALUATE` cell for the fix model from the start this time (the gap found and fixed in `dnn_regressor` last session). **(3) New, genuinely different finding for this model type:** `learn_rate` and `optimizer` are **NOT tunable** for `DNN_LINEAR_COMBINED_*` — both fail immediately with `"Unsupported hyperparameter <name> for model_type DNN_LINEAR_COMBINED_CLASSIFIER"` when passed to `HPARAM_RANGE`/`HPARAM_CANDIDATES`, contradicting RESOURCES.md's generic DNN-family "Tunable" claims (verified tunable instead: `hidden_units`, `dropout`, `batch_size`, `l1_reg`, `l2_reg`). Practical upside: since `learn_rate` can only be a fixed literal for this model type, the regressor's tuning step has no risk of trials getting stuck in the catastrophic low-`learn_rate` region the way `dnn_regressor`'s tuning did — all 4 trials succeeded cleanly (best `r2_score≈0.87`). **(4)** Confirmed (checked the live BigFrames API reference) no first-class wide-and-deep wrapper exists in `bigframes.ml` — same permanent gap as DNN. Corrected RESOURCES.md's wide-and-deep entry (tunable-options table, hyperparameter-tuning section, limitations, BigFrames API) accordingly. Both notebooks built with validated source; pending user Restart & Run All — expect the same long training times as DNN (large fixed per-`CREATE MODEL` overhead), and avoid running both notebooks concurrently with other BigQuery ML work in the project. |
| 2026-07-17 | Phase 2: `models/wide_and_deep_classifier/` + `models/wide_and_deep_regressor/` — final verification | User ran both notebooks (Restart & Run All). Classifier reviewed cell-by-cell: all metrics matched pre-validation closely (`roc_auc` 0.8929 baseline, 0.8987 best-tuned trial), ROC curve PNG extracted and visually confirmed clean, tuning found no failed trials — fully verified, no changes needed. **Reviewing the regressor caught a real numeric discrepancy, same failure pattern as the `dnn_regressor` corrections:** my pre-validation of the "fix" config (scale + `learn_rate=0.05`) used a differently-named ad-hoc model and got `r2_score≈0.86`, which I wrote into the notebook's Step 6 markdown and overview as the expected result. The actual notebook run, under the real model name, produced `r2_score=0.789591` — a real, meaningfully different number, not noise. Corrected the specific figure everywhere it appeared (notebook overview cell, Step 6 markdown, `.sql` comment, RESOURCES.md's wide-and-deep limitations bullet) to state the untuned fix reaches **0.79** with `hidden_units=[64,32]`, notably lower than `DNN_REGRESSOR`'s ~0.86 with the equivalent fix on identical data, and that Step 7's hyperparameter tuning closes most of the gap (best tuned trial: `r2_score=0.870350` at `hidden_units=[32,16]`). No notebook code changes were needed — only the documented expected values, since the actual pipeline behavior (baseline broken → scale+learn_rate fix works → tuning refines further) was already correct. **Standing lesson applied correctly this time, but the underlying trap keeps recurring: never write a specific expected metric value into a notebook/RESOURCES.md claim based on a differently-named pre-validation model — always cite the number the real, final-named model actually produces, and treat any pre-validation number as a rough sanity check only, not a citable result.** Both `models/wide_and_deep_classifier/` and `models/wide_and_deep_regressor/` are now fully verified ninth/tenth canonical references — all ten model-type notebooks in Phase 2 (GLM, boosted tree, random forest, DNN, wide-and-deep, each classifier+regressor) are complete. |
| 2026-07-17 | Backlog audit: found two missing model types | Per the "no stretch goals, full BQML coverage" scope clarification, audited the full `model_type` catalog already documented in RESOURCES.md against the PLANS.md Development Phases backlog. Found two model types with a complete RESOURCES.md entry but **no backlog item anywhere**: `AUTOML_CLASSIFIER`/`AUTOML_REGRESSOR` (added as new Phase 2 items) and `CONTRIBUTION_ANALYSIS` (added to Phase 3, renamed "Unsupervised + insight models" since contribution analysis isn't actually unsupervised — grouped there for scheduling convenience only). No other gaps found: `KMEANS`/`PCA`/`MATRIX_FACTORIZATION`/`AUTOENCODER` (Phase 3), `ARIMA_PLUS`/`ARIMA_PLUS_XREG` (Phase 4), imported/remote/export (Phase 5) were all already present. |
| 2026-07-17 | Phase 3: `models/kmeans/` — first unsupervised model | Built the eleventh model-type notebook and the first genuinely unsupervised one: `KMEANS` on `penguins`' four numeric physical measurements (culmen length/depth, flipper length, body mass) — no label, and `species`/`island`/`sex` are NOT training features. `num_clusters=3` was a deliberate, informed choice (3 real penguin species). All SQL pre-validated end-to-end via `bq query`. **New patterns/findings specific to unsupervised models:** **(1)** Carried the real `species` column through `ML.PREDICT` purely as an external validation check (never used in training) — verified clustering recovers Gentoo almost perfectly (123/123) but mixes Adelie/Chinstrap (they overlap more in body measurements), presented honestly as partial-not-perfect recovery. **(2)** Added a new visualization convention for clustering notebooks: a scatter plot colored by predicted cluster, shaped by actual species, to visually cross-check alignment — parallel to the tree-viz convention for tree ensembles and the ROC-curve convention for classifiers. **(3) Important, counter-intuitive, verified finding:** adding `island` as a TRANSFORM feature improved `davies_bouldin_index` (0.84 vs 1.02, lower is "better") but *degraded* species alignment — Adelie/Chinstrap merged into one cluster and the previously-clean Gentoo cluster split into two. This demonstrates that k-means' only built-in quality metric measures internal separation in whatever feature space you give it, not alignment with any domain-meaningful grouping — corrected/expanded RESOURCES.md's KMEANS best-practices section accordingly. **(4)** Verified `ML.DETECT_ANOMALIES` requires the 3rd (input-data) argument for `KMEANS` specifically — omitting it errors immediately ("expects 3 arguments... but 2 were passed"), a gap in the general `ML.DETECT_ANOMALIES` docs which show the 3-arg form but don't flag it as mandatory. **(5)** Hyperparameter tuning `num_clusters` over `HPARAM_RANGE(2, 10)` independently selected `num_clusters=3` as optimal — validating the manual choice from real evidence, not just a docs claim. **(6)** This is the first DNN-family-adjacent notebook with a genuine BigFrames comparison rather than a gap note — `bigframes.ml.cluster.KMeans` is a real first-class wrapper; verified its `fit(X)`/`score(X)` signatures take no `y` (unsupervised) via the live BigFrames API docs before writing the cell. Notebook built with validated source; pending user Restart & Run All — training is fast (under 4 minutes per `CREATE MODEL` in validation, unlike the DNN-family models). |
| 2026-07-17 | Phase 3: `models/kmeans/` — final verification, major retraction | User ran the notebook (Restart & Run All); reviewing it caught two real, related problems, both now fixed. **(1) Structural gap (same category as the `dnn_regressor` gap, now a fourth occurrence):** Step 8 ("the fix"/TRANSFORM step) claimed specific `davies_bouldin_index` values but had no `ML.EVALUATE` cell for the transform model in either the notebook or `.sql` — fixed by inserting one. **(2) A confidently-stated "counter-intuitive, verified" finding from the build session did NOT survive retesting and had to be retracted.** The original claim: adding `island` as a feature reliably makes `davies_bouldin_index` better while degrading species alignment. Retraining the baseline and transform models fresh (to get the number for fix #1) reversed BOTH directions — this time the baseline's metric was better and its species alignment was *worse* (Gentoo split, not clean) than the transform's. A third independent training (BigFrames' own `KMeans` call) produced yet a different `davies_bouldin_index` again. **Root cause: K-means retraining is genuinely non-deterministic here, even with `kmeans_init_method='KMEANS++'`** — this reframes the entire Step 8 finding from a specific causal claim into the more defensible, better-supported lesson that a single before/after comparison isn't reliable evidence of a feature's effect on clustering. **This also explained a THIRD issue I'd missed in the first review pass:** Step 9's hyperparameter tuning markdown claimed the tuner "independently confirms num_clusters=3 is optimal," but the notebook's own actual tuning cell output showed `num_clusters=2` won instead — a claim contradicted by the very cell sitting right below it in the same notebook, that I failed to cross-check on the first read. Corrected all three markdown cells (overview, Step 1, Step 8, Step 9) plus the `.sql` file plus RESOURCES.md's KMEANS best-practices/limitations sections to state the non-determinism finding plainly, retire the retracted causal claim with an explicit note that it didn't hold up, and stop citing a specific `num_clusters` as "the" tuned answer. **Standing lesson, now reinforced a second time (after the DNN/wide-and-deep number corrections): a finding built from exactly one comparison (one baseline run vs. one modified run) is not "verified" — it's an anecdote. For any non-deterministic model type (random forest, K-means, and apparently others), retrain at least twice before writing a causal or comparative claim into RESOURCES.md, and always double-check that a notebook's own later cells don't contradict an earlier cell's markdown claim before signing off.** Also worth noting for future reviews: **cross-check every markdown claim against every cell's actual output, including cells far downstream of where the claim is made** — the Step 9 contradiction was sitting in plain sight in the same notebook and was missed on the first pass. `models/kmeans/` is now fully verified as the eleventh canonical reference, with an honest non-determinism finding replacing the retracted one. |
| 2026-07-17 | Standing style correction: no editorial history in notebooks | User flagged that the K-means retraction's wording ("an earlier version of this notebook claimed X... revised after retesting...") is meaningless to a reader who never saw that earlier version — every notebook is read fresh, not as a diff against its own history. **Standing rule going forward: notebooks and `.sql` files must state every gotcha/finding directly, in present tense, as a self-contained fact — never as "revised from," "retracted," or "an earlier draft claimed."** This does not prohibit describing multiple REAL, verifiable training runs and their actual outputs (e.g. "retraining twice produced X and Y" is fine) — only editorializing about the writing process itself. Grepped all existing notebooks/`.sql` for the pattern (`earlier version`, `earlier draft`, `didn't survive retesting`, `retracted`, `revised after retesting`, `didn't hold up`, `does NOT survive`) and fixed the four affected locations in `models/kmeans/` (overview cell, Step 8 cell, `kmeans.sql` Example 7, RESOURCES.md's KMEANS bullet). Confirmed no other notebook (`dnn_classifier`, `dnn_regressor`, `wide_and_deep_classifier`, `wide_and_deep_regressor`) had this pattern — their similar-sounding "verified across two runs" language was already describing real multi-run findings, not editorial history. This rule applies to all notebooks built from here forward, including `models/pca/` below. |
| 2026-07-17 | Phase 3: `models/pca/` — second unsupervised model | Built the twelfth model-type notebook: `PCA` on `penguins`' four numeric physical measurements — no label, `species`/`island`/`sex` not training features, `species` carried through `ML.PREDICT` as an external check (same convention as `models/kmeans/`). `num_principal_components=2` baseline (for a clean 2D visualization) plus a `pca_explained_variance_ratio=0.90` alternative (auto-selects 3 components) as the no-HP-tuning substitute — PCA has no `HPARAM_RANGE`/`HPARAM_CANDIDATES` support. All SQL pre-validated end-to-end via `bq query`, applying the K-means lesson of retraining anything comparison-based at least twice before writing a claim. **New findings for this model type:** **(1)** Confirmed PCA is fully deterministic — retraining the identical model reproduced `total_explained_variance_ratio` bit-for-bit every time, a genuine, reassuring contrast to `KMEANS`'s non-determinism, since PCA is a closed-form eigendecomposition with no random initialization. **(2)** Confirmed `ML.DETECT_ANOMALIES` requires the 3rd (input-data) argument for PCA too, resolving the open question K-means' RESOURCES.md entry left hanging (whether the requirement was KMEANS-specific) — it is not. **(3)** Found and documented a genuine indexing inconsistency: `principal_component_id` (in `ML.PRINCIPAL_COMPONENTS`/`ML.PRINCIPAL_COMPONENT_INFO`) is 0-indexed, unlike `KMEANS`' `centroid_id` (1-indexed) and unlike `ML.PREDICT`'s own output columns `principal_component_1`/`_2` (1-indexed) — three different conventions across two closely related unsupervised model types. **(4)** Validated `ML.GENERATE_EMBEDDING` on a PCA model for the first time — confirmed its `ml_generate_embedding_result` array values match `ML.PREDICT`'s projection columns exactly. **(5)** The `island` TRANSFORM feature drops `total_explained_variance_ratio` from ~0.88 to ~0.76, verified reproducible across two retrains — framed as an expected, sensible effect (more one-hot dimensions dilute the same fixed component count), not a gotcha like the analogous K-means change. **(6)** Verified the exact BigFrames `bigframes.ml.decomposition.PCA` API before writing the comparison cell: `.fit(X)` then `.predict(X)` — not `.transform()`, a genuine difference from scikit-learn — and `.score(X)` returns `total_explained_variance_ratio` matching `ML.EVALUATE`; corrected RESOURCES.md's previously-inaccurate "fit/transform" BigFrames note. All gotcha/finding markdown written in the present-tense, no-editorial-history style per the standing correction above. Notebook built with validated source; pending user Restart & Run All — training is fast (single-digit seconds per `CREATE MODEL`, similar to K-means). |
| 2026-07-17 | Phase 3: `models/pca/` — final verification | User ran the notebook (Restart & Run All). Every numeric markdown claim cross-checked against its actual cell output and confirmed exact (baseline `total_explained_variance_ratio=0.881568`, transform `0.756762`, `pca_explained_variance_ratio=0.90` alternative `0.972877`/3 components, 0-indexed `principal_component_id`, 3-arg `ML.DETECT_ANOMALIES`). Scatter plot rendered clean. **New finding from cross-checking the BigFrames cell's actual output:** `bigframes.ml.decomposition.PCA`, training a completely independently-named model, reproduced the exact same `total_explained_variance_ratio` as the manual model — a third independent confirmation of full determinism, and a clean contrast to K-means (whose BigFrames training gave yet a third different value). Added to the notebook, `.sql`, and RESOURCES.md. `models/pca/` is now the twelfth fully verified canonical example. |
| 2026-07-18 | Phase 3: `models/autoencoder/` — third unsupervised model, first real training failure to debug | Built the thirteenth model-type notebook: `AUTOENCODER` on `penguins`' same 4 numeric measurements as K-means/PCA, no label, `species` carried through `ML.PREDICT` as an external check, `hidden_units=[3,2,3]` for a 2-dim latent space comparable to PCA's 2 components. Training is far more expensive than any prior model type in this project — each `CREATE MODEL` took roughly 7-15 minutes even on this tiny 342-row dataset, and the 4-trial HP-tuning job took ~25 minutes; pre-validation for this one notebook took well over an hour of real BigQuery compute. **Major finding: BigQuery ML's own default `activation_fn='RELU'` genuinely breaks this architecture** — a real dying-ReLU collapse, not a theoretical concern. Verified across two independent retrains: a large share of latent activations get clipped to exactly `0.0` (one run pinned an entire latent column to `0.0` for all 342 rows; a second run spread the zeros unevenly across both columns instead — same underlying symptom, different specific shape). `ML.EVALUATE`'s aggregate metrics looked like a normally-trained model in both cases — the collapse is only visible by directly querying `ML.PREDICT`'s `latent_col_*` output, which became a dedicated diagnostic step (Step 3) before introducing the fix. **The fix — switching only `activation_fn` to `'TANH'`** — eliminated the dead units and improved `mean_squared_error` by roughly 3x (~0.66 → ~0.21) on the identical architecture and data, a real quality difference not explainable by noise. **Second major finding, discovered specifically because the two RELU retrains needed for the first finding were compared closely:** `ML.EVALUATE`'s aggregate reconstruction metrics reproduce bit-for-bit across retrains of the identical model name (same as `DNN_*`), but the actual `latent_col_*` values do NOT — same reconstruction quality, different specific latent geometry. This is a real, explainable property (not a bug): unlike PCA's variance-ranked, uniquely-ordered components, a generic autoencoder bottleneck has no constraint forcing a canonical basis — any invertible transform of the latent space paired with the inverse transform in the decoder reconstructs identically. Documented as a direct, verified contrast to `models/pca/`'s full determinism. Also verified: `ML.DETECT_ANOMALIES` requires the 3rd (input-data) argument for `AUTOENCODER` too (all three unsupervised model types in this project now confirmed to share this requirement); `ML.GENERATE_EMBEDDING`'s output matches `ML.PREDICT`'s latent columns exactly; the `island` TRANSFORM feature improves `mean_squared_error` (~0.21→~0.13), a sensible, explained effect (a low-cardinality one-hot feature is easy to reconstruct, pulling the averaged error down), not a gotcha; HP tuning over `activation_fn`/`learn_rate` independently confirmed TANH beats RELU at a matched learn_rate, though its small 4-trial budget landed on a slightly worse config than the manually-chosen fix — reported honestly as a budget limitation, not a contradiction. Confirmed (checked the live BigFrames API reference) no `bigframes.ml` autoencoder wrapper exists — same permanent gap as DNN/wide-and-deep. All gotcha/finding markdown written in the present-tense, no-editorial-history style, calibrated to describe the general reproducible symptom rather than one run's exact numbers where retraining variance was directly observed. Notebook built with validated source; pending user Restart & Run All — given the extreme per-model training cost, this will likely be the longest-running notebook in the project yet (45-60+ minutes for a full run across four `CREATE MODEL` calls plus a 4-trial tuning job). |
| 2026-07-18 | `models/autoencoder/` — added Step 11: embedding matching via `VECTOR_SEARCH` | User asked to add a comparison of the manual embedding path (`ML.PREDICT` + hand-built `ARRAY` + `ML.NORMALIZER`) against `ML.GENERATE_EMBEDDING`'s automation, after reading the official `ML.GENERATE_EMBEDDING` docs and wondering whether it handles normalization internally. Pre-validated a fresh retrain of the fix model to test this directly rather than assume from docs. **Finding: `ML.GENERATE_EMBEDDING` does NOT normalize its output** — confirmed its raw `ml_generate_embedding_result` is bit-for-bit identical to `ML.PREDICT`'s un-normalized latent columns, and applying `ML.NORMALIZER` to each path independently produces the exact same normalized vector. So the function's real convenience is not having to enumerate `latent_col_1..N` into an `ARRAY` yourself — not automatic normalization, correcting the plausible assumption the docs link could suggest. **Second finding, while building the `VECTOR_SEARCH` step:** `VECTOR_SEARCH` rejects `ML.PREDICT`/`ML.GENERATE_EMBEDDING` output passed directly as its base-table argument ("Unsupported query pattern") — the embeddings must be materialized into a real table first, added as a new `CREATE TABLE` step. **Third finding:** manually normalizing + `distance_type='DOT_PRODUCT'` and using the raw embedding directly with `distance_type='COSINE'` produce mathematically equivalent `VECTOR_SEARCH` rankings (verified: identical top-5 neighbors, distances converting exactly via `COSINE = 1 - cosine_similarity` = `-DOT_PRODUCT` on unit vectors) — so `ML.NORMALIZER` turns out to be unnecessary for `VECTOR_SEARCH` specifically when `COSINE` is available, correcting RESOURCES.md's prior best-practice bullet (which recommended normalize + `DOT_PRODUCT` without noting `COSINE` makes that unnecessary). Updated `autoencoder.sql`/`autoencoder.ipynb` (inserted as the new Step 11, renumbering the determinism/TRANSFORM/tuning steps to 12/13/14) and RESOURCES.md (AUTOENCODER's best-practices and Verified bullets). |
| 2026-07-18 | Phase 3: `models/autoencoder/` — final verification, three corrections including a major retraction | User ran the full notebook (Restart & Run All). Cross-checking every markdown claim against the actual cell output found three real problems, all now fixed. **(1) Wording bug in Step 3's diagnostic claim:** the markdown said "roughly 40-50% of rows have at least one latent dimension clipped to exactly 0.0," but the query it described computes `COUNTIF(latent_col_1 = 0.0 AND latent_col_2 = 0.0)` — an AND condition ("both simultaneously zero"), not the OR condition ("at least one") the prose claimed. The actual run's real value (221/342 = 64.6%) also fell outside the claimed 40-50% range. Fixed by correcting the wording to match the query's actual AND semantics and widening the reported range to the three real observations so far (40%, 50%, 65%). **(2) Recurring pre-validation-vs-real-model trap, now hitting autoencoders (previously DNN/wide-and-deep/PCA):** Step 4's markdown cited the RELU baseline as "~0.66," a number that came from a differently-named `_validate`-suffixed pre-validation model, not the real notebook's `autoencoder_penguins` model — whose actual `ML.EVALUATE` in this run was 0.939008, a real and substantial difference, not noise. Fixed by removing the hardcoded baseline number and instead citing the honest observed range (~0.66-0.94) with an explanation that RELU's collapse severity itself varies retrain to retrain, which is why the baseline number isn't fixed. **(3) Major retraction — the Step 14 HP-tuning "TANH beats RELU, independently confirmed" claim did NOT survive retesting.** The build-session pre-validation run of the exact same 4-trial tuning config sampled mostly TANH trials with TANH winning; the actual notebook run sampled mostly RELU trials (3 RELU, 1 TANH) with a RELU trial winning instead — and, more strikingly, **all 4 of this run's tuned trials scored worse than both the untuned RELU baseline and the untuned TANH fix**, meaning the tuning search itself found nothing useful this time. Root cause, same pattern as K-means's Step 9 retraction: **HP-tuning trial-sampling is itself non-deterministic, and a 4-trial budget spread across two hyperparameters simultaneously is too sparse to reliably explore either one** — closely related to (but a new instance of) the already-documented DNN small-trial-budget limitation. Rewrote Step 14's markdown to report the actual result honestly, retract the "independently confirms" framing, and redirect the reader to Step 4's direct controlled comparison as the reliable evidence instead. **Standing lesson, reinforced a third time now (after K-means and DNN/wide-and-deep): never trust a single HP-tuning run's outcome as representative, especially when trials shrink the search space unevenly across multiple hyperparameters — always cross-check the tuning cell's actual `ML.TRIAL_INFO` output against whatever the markdown claims before signing off, even when the markdown was written confidently after a real pre-validation run.** Updated `autoencoder.ipynb`, `autoencoder.sql`, and RESOURCES.md's three affected Verified/best-practice bullets to match. `models/autoencoder/` is now the thirteenth fully verified canonical example. |
| 2026-07-19 | Phase 3: `models/matrix_factorization/` — fourteenth model, first to require real GCP infrastructure setup | Built the fourteenth model-type notebook: `MATRIX_FACTORIZATION` (IMPLICIT feedback) on `bigquery-public-data.google_analytics_sample.ga_sessions_*` (July 2017), aggregated to (visitor_id, product_name, view_count) — 42,178 users x 320 items, ~1.3M interactions. Chose this dataset over the official MovieLens tutorial data because MovieLens requires a manual download/load step (no public BigQuery mirror), breaking the project's established all-`bigquery-public-data`, no-manual-loading convention; the GA sessions data matches Google's own official IMPLICIT-feedback tutorial pattern and is directly queryable. **Major, project-first finding: this is the only model type in the entire project that cannot train under on-demand pricing** — verified directly, `CREATE MODEL` failed instantly with `"Training Matrix Factorization models is not available for on-demand usage."` Required actually setting up real GCP billing infrastructure to build this notebook (with explicit user authorization: "use the notebook to setup the reservation and remove the reservation, DO NOT purchase a commitment at any time"). Found through live trial-and-error: **(1)** a `STANDARD`-edition reservation is not sufficient — it fails with a different error ("BQML related functionalities is disallowed in STANDARD edition"); `ENTERPRISE` is required. **(2)** a reservation created with `--autoscale_max_slots` and no `--capacity_commitment` flag (baseline `--slots=0`) satisfies "no commitment purchased" while still unblocking training — pay-per-second, no minimum term. **(3)** a freshly created reservation assignment needs ~90 seconds to propagate before `CREATE MODEL` succeeds — the very first attempt right after assignment creation still failed. The notebook now creates this reservation in Setup (via `google.cloud.bigquery_reservation_v1`, matching the project's all-Python-SDK convention) and tears it down in Cleanup — a genuinely new notebook pattern, unique to this model type. **Real, measured cost, not hypothetical:** building/validating this notebook consumed ~6.4 cumulative slot-hours under `ENTERPRISE` autoscale (one base model, one 4-trial tuning job, one BigFrames retrain) — documented explicitly in the notebook's cost note and RESOURCES.md, with a pointer to check current Editions pricing rather than a guessed dollar figure. **Additional verified findings, several correcting or extending existing RESOURCES.md text:** `ML.RECOMMEND` does NOT error for a user/item unseen in training (contradicts the intuitive reading of RESOURCES.md's prior "cannot recommend for absent users/items" limitation bullet, now corrected) — it silently falls back to item-bias-driven recommendations, verified by comparing a real trained user against an obviously fake one and seeing different but still-populated top-5 lists; `ML.WEIGHTS` includes an extra `processed_input=NULL`/`feature='global__INTERCEPT__'` row beyond the per-user/per-item rows; `ML.GENERATE_EMBEDDING` for this model type takes ONLY the model (no input argument at all — a different signature from PCA/AUTOENCODER, which both require one) and returns every user's and item's embedding in a single call; `ML.EVALUATE` on a tuned model has no `STRUCT(trial_id)` 2-argument form (unlike `ML.RECOMMEND`) — it always returns all trials at once; `ML.TRAINING_INFO`'s `eval_loss` is `NULL` throughout, unlike supervised model types; HP tuning produced a genuine, positive result this time (`mean_average_precision` 0.873→0.915), a clean contrast to `models/autoencoder/`'s failed tuning search. Verified `bigframes.ml.decomposition.MatrixFactorization` is a real first-class wrapper, but confirmed via live constructor signature that it has no `wals_alpha` parameter (SQL-only knob for IMPLICIT tuning). Item-item similarity via `VECTOR_SEARCH` on the item factor embeddings produced sensible category-clustered neighbors (youth apparel grouping together). Notebook built with validated source; all live GCP reservation/assignment/model resources created during pre-validation were deleted immediately after testing — nothing left running. `models/matrix_factorization/` is the fourteenth model-type notebook, pending user Restart & Run All. |
| 2026-07-19 | `models/matrix_factorization/` — made Setup/Cleanup idempotent | User caught a real gap before running the notebook: "is the notebook using conditional logic to ensure we don't create multiple reservations and assignments?" It wasn't — re-running Setup after an interrupted prior run (Cleanup never executed) would have hit `AlreadyExists` and crashed, or worse, could have created duplicate billable reservations if the error handling had been sloppier. Fixed by wrapping `create_reservation` in `try/except google.api_core.exceptions.AlreadyExists` (falls back to `get_reservation` to reuse it), checking `list_assignments` before `create_assignment` to reuse an existing assignment (and skip the 90s propagation wait when reusing), and wrapping Cleanup's delete calls in `try/except google.api_core.exceptions.NotFound` so re-running Cleanup after it already succeeded doesn't crash. Live-tested the full idempotent Setup→Setup→Cleanup→Cleanup sequence against a real (throwaway) reservation to confirm both reuse paths and both graceful-no-crash paths work exactly as intended, then deleted the test reservation. Saved as its own durable memory ([[feedback_bqml_reservation_pattern]]) since any future notebook that creates named, billable cloud resources needs this same idempotency built in from the start. |
| 2026-07-19 | Phase 3: `models/matrix_factorization/` — final verification | User ran the full notebook (Restart & Run All) — the idempotent reservation Setup/Cleanup worked correctly on the real run (single create, single delete, no errors). Cross-checking every markdown claim against actual output found one real issue: Step 10's hyperparameter-tuning citation ("baseline 0.873 → best tuned 0.915, wals_alpha=30.8") was the SAME "stale number from a differently-run model" trap that has now hit DNN, wide-and-deep, K-means, and autoencoder — except this time the model name was identical between the pre-validation run and the real run, and the discrepancy revealed a genuine new finding rather than an unrelated model: **`MATRIX_FACTORIZATION` retraining shows measurable variance, similar to `KMEANS`/`RANDOM_FOREST_*`.** The real run's baseline was `mean_average_precision=0.860` (not `0.873`), and its tuned-best trial reached `0.899` with `wals_alpha=49.2` (not `0.915`/`30.8`). The qualitative finding — tuning beats the untuned baseline — held in both runs; only the specific numbers differed. Corrected Step 2's and Step 10's markdown (added a retraining-variance note to Step 2, reworded Step 10 to cite both runs' real numbers honestly instead of asserting one as canonical), `matrix_factorization.sql`'s matching comments, and RESOURCES.md's HP-tuning bullet (split into a new "retraining variance" bullet plus a reworded tuning bullet). **Standing lesson extended: the "cite the real, final-named model's number, not a differently-named pre-validation model's" rule isn't just about avoiding cross-model citation errors — even when the name IS identical, a model type with retraining variance can still make a pre-validation number stale by the time the user's real run happens. Always verify a cited number against the actual notebook's own displayed output before signing off, regardless of whether the citation came from the same or a different model name.** **Also caught during this review, a genuinely new structural finding, not just a number correction:** `ML.GENERATE_EMBEDDING`'s output array for this model type has `num_factors + 1` elements (17, not 16, for `num_factors=16`) — one more than `ML.WEIGHTS`' `factor_weights` array for the same model (confirmed 16 elements there), almost certainly the per-user/per-item `intercept` appended onto the raw factor vector. This was found by manually counting the array length in the real cell output during review — a reminder that array-valued outputs deserve the same "verify the actual shape, don't just skim" scrutiny as scalar metric values. Corrected Step 6's markdown, `matrix_factorization.sql`, and RESOURCES.md accordingly. `models/matrix_factorization/` is now the fourteenth fully verified canonical example — all fourteen model-type notebooks built so far are complete and verified. |
| 2026-07-20 | Phase 3: `models/contribution_analysis/` — fifteenth model, closes out Phase 3 | Built the fifteenth model-type notebook: `CONTRIBUTION_ANALYSIS` on `bigquery-public-data.new_york_citibike.citibike_trips`, deliberately reusing the exact same test/control split (April 2017 vs April 2016) and dimensions (usertype, gender, start_station_name) as the sibling `bq-ai-functions/functions/ai_key_drivers/` notebook, so the two are directly comparable. Checked the `ds-l400` repo the user pointed to for a reference lab combining `CONTRIBUTION_ANALYSIS`/`AI.KEY_DRIVERS` — found only a lab that *manually implements* dimensional/contribution-style analysis via plain SQL and explicitly documents "we implement the logic manually here" rather than calling the real BQML feature; reported this honestly rather than pretending a literal code reference existed, and used the project's own real `ai_key_drivers.sql` as the actual comparison baseline instead. **Major finding: `ML.GET_INSIGHTS` has three genuinely different output schemas depending on `contribution_metric` type — not documented in the official reference at the time this was tested.** Summable metrics (`SUM(x)`) return `difference`/`relative_difference`/`unexpected_difference`/`relative_unexpected_difference` with `contribution=ABS(difference)`; ratio metrics (`SUM(a)/SUM(b)`) drop all four of those and return `metric_test_over_metric_control`/`metric_test_over_complement`/`metric_control_over_complement`/`aumann_shapley_attribution` instead, with `contribution` equal to the Shapley attribution; category metrics (`SUM(a)/COUNT(DISTINCT b)`) return a third mix (`difference`/`relative_difference` return, but `unexpected_difference` is replaced by `metric_test_over_population`/`metric_control_over_population`). Verified directly by training all three forms on identical data. **Also verified the two concrete "beyond AI.KEY_DRIVERS" capabilities RESOURCES.md already claimed but hadn't tested:** ratio and category metrics both train and produce insights (Steps 3-6), and >12 dimensions genuinely works — 13 low-cardinality dimensions trained successfully — but at a real cost: ~13 minutes vs ~5 seconds for a 3-dimension model on the same data. A first attempt at 13 dimensions with several HIGH-cardinality dimensions (raw station IDs, individual bike IDs) ran over 18 minutes and then failed with a generic "internal error, usually transient" message rather than a clean validation error; this did not reproduce with low-cardinality dimensions at the same count, so the notebook frames per-dimension cardinality (not just dimension count) as the likely real cost driver. Also verified: training queries may contain ONLY the columns referenced by `contribution_metric`/`dimension_id_cols`/`is_test_col` (extra columns error immediately, unlike most other model types); `min_apriori_support`/`top_k_insights_by_apriori_support` are genuinely mutually exclusive; `pruning_method` has a dramatic effect (1,559 vs 15 insight rows on identical data); `ML.PREDICT`/`ML.EVALUATE`/`TRANSFORM` all confirmed unsupported with real (if unhelpfully-worded) error messages; no BigFrames wrapper exists (confirmed by listing `bigframes.ml`'s actual submodules). **Caught and fixed two real notebook bugs during construction (not during review — found via a script-based leaked-build-variable scan before handoff):** two code cells referenced `{SUMMABLE_FQN}`/`{LOWCARD_FQN}` as if they were notebook-runtime Python variables, but those names only existed in the build script that generated the notebook — the actual notebook cells would have raised `NameError` on execution. Fixed by substituting the literal `{PROJECT_ID}.{DATASET_ID}...` FQN text at build time instead, matching the pattern used correctly everywhere else, then re-scanned every code cell programmatically to confirm no other build-script variable names had leaked through. **New process lesson: after generating a notebook via a Python build script, grep/scan the generated cells for any build-script-only variable names before considering the notebook done — this is a distinct failure mode from the wrong-number/wrong-claim errors caught during past reviews, since it produces a hard runtime crash rather than a misleading number.** Training was fast (seconds) for most steps, avoiding the extended runtimes of autoencoder/matrix_factorization, except the intentional 13-dimension demonstration. All validation models/tables cleaned up immediately after testing. `models/contribution_analysis/` closes out Phase 3 (unsupervised + insight models) entirely — K-Means, PCA, Autoencoder, Matrix Factorization, and Contribution Analysis are all built. Pending user Restart & Run All. |
| 2026-07-20 | `models/contribution_analysis/` — cross-reference fix, then final verification | User asked whether the notebook covers all `ML.GET_INSIGHTS`/`CONTRIBUTION_ANALYSIS` usages and whether the `AI.KEY_DRIVERS` cross-link goes both ways. Confirmed `ML.GET_INSIGHTS` has only one calling form (verified via live docs search) and the notebook already exercises every meaningful `CREATE MODEL` configuration variation. Found the cross-link was one-directional — `ai_key_drivers.sql`/`.ipynb`/`bq-ai-functions/RESOURCES.md` had no link back to `models/contribution_analysis/` (built before it existed) — fixed by adding backward links in all three (the `.ipynb` edit preserved its existing real outputs from a prior run). **User then ran the full notebook; reviewing it caught two real inaccuracies, both from cross-checking claims against the actual cell output:** **(1)** Step 4's claim that ratio-metric `contribution` "equals `aumann_shapley_attribution`" was checked against only the first (positive) row when written — the actual output shows `contribution=ABS(aumann_shapley_attribution)` (e.g. `usertype=Subscriber`: `aumann_shapley_attribution=-49.86`, `contribution=49.86`), the same absolute-value pattern as the other two metric types. **(2)** Step 9's `ML.EVALUATE` description claimed behavior ("asks for input data, then a label column") that the notebook's single zero-argument call doesn't actually demonstrate — reworded to describe only what the shown call proves, with the label-column escalation noted as a separate fact about passing a data argument (verified during pre-validation, not re-demonstrated live). Fixed the notebook, `.sql`, and RESOURCES.md's ratio-metric output table. **Standing lesson reinforced: when documenting "X equals Y," check the claim against a row where the values could plausibly differ (e.g. a negative one), not just the first row shown — a single positive-value spot-check can hide an ABS() wrapping that only reveals itself on negative values.** `models/contribution_analysis/` is now the fifteenth fully verified canonical example — Phase 3 (K-Means, PCA, Autoencoder, Matrix Factorization, Contribution Analysis) is complete and verified. |
| 2026-07-20 | Phase 2: `models/automl_classifier/` + `models/automl_regressor/` — sixteenth/seventeenth models, close out Phase 2, first real-dollar-cost model type | Built `AUTOML_CLASSIFIER` on `census_adult_income` (same data/label as the other five classifiers) and `AUTOML_REGRESSOR` on `penguins`/`body_mass_g` (same data/label as the other five regressors). **This is the first model type in the entire project with real, substantial dollar cost** — Vertex AI AutoML Tabular training is billed at roughly $21.25/node-hour (confirmed via web search against Google's official Vertex AI pricing), `budget_hours` cannot go below 1.0, and this project's own existing (already-tested, pre-project) repo example (`02 - Vertex AI AutoML/BQML AutoML.ipynb`) took ~1.51 wall-clock hours for a 1-hour budget, illustrating the compression-overhead overrun. Presented the real cost estimate to the user via AskUserQuestion (~$21-32 + ~1.5hr per model, ~$40-65/~2-3hr total for both) before proceeding; user approved `budget_hours=1.0` for both. **Departed from this project's standard pre-validation practice for cost reasons:** every other notebook so far has been validated with a real, throwaway `bq query` training run before handoff (dropped afterward) in addition to the user's own Restart & Run All — for AutoML this would silently double the approved cost/time (four real trainings instead of two). Instead, validated only `CREATE MODEL` syntax for free via `bq query --dry_run` (confirmed working: returns a clean "Query successfully validated" result with a bytes-processed estimate, no training triggered) against both the classifier and regressor statements using the exact production column lists/label columns (`income_bracket`, `body_mass_g`) verified against the existing `logistic_regression`/`linear_regression` notebooks for consistency. Markdown throughout both notebooks avoids citing specific expected metric values (accuracy/roc_auc/r2_score numbers) since there is no live run to verify them against — this defers the usual "verify every markdown claim against real output" review to after the user's single real Restart & Run All, exactly as with every other model type, just without a throwaway pre-validation run first. Both notebooks follow the established classifier/regressor skeleton with AutoML-specific adjustments per RESOURCES.md's already-researched entry: no `TRANSFORM`, no `ML.WEIGHTS`/`ML.ADVANCED_WEIGHTS`, no `ML.EXPLAIN_PREDICT` (only model-level `ML.GLOBAL_EXPLAIN`, which needs no `enable_global_explain` option unlike GLM/tree models), no user-configurable hyperparameter tuning (`budget_hours` is the only lever) — each demonstrated live via a `try/except` cell capturing the actual error text, the same pattern used for `contribution_analysis`'s unsupported-function step. `EXPORT MODEL` is supported but the exported artifact is an opaque ensemble (no tree/booster file to visualize, unlike `BOOSTED_TREE_*`/`RANDOM_FOREST_*`). No BigFrames AutoML estimator exists in any `bigframes.ml` submodule (same permanent gap as DNN/wide-and-deep) — a markdown-only gap note, no code cell. Built both notebooks directly as raw `.ipynb` JSON (no `nbformat` package available in this environment) via a temporary, uncommitted Python build script; ran the standing leaked-build-variable scan afterward (caught and fixed one self-inflicted bug: an initial `f.write('\n')` call in the build script had its escape doubled during authoring, embedding a literal two-character `\n` after the closing `}` in the classifier file and breaking JSON parsing — fixed and re-verified both files parse cleanly). Both notebooks are handed off unrun (no outputs) awaiting the user's single real training run per model before final review and PLANS.md/README.md checkoff — this is the first model type in the project reviewed post-run without a pre-validated numeric baseline to compare against. |
| 2026-07-20 | `models/automl_regressor/` — real platform limitation found on the user's actual run, dataset switched | User's Restart & Run All hit a real error at Step 1, the model's very first `CREATE MODEL` call: `BadRequest 400: "Input data contains 333 rows. The minimum number of input rows for AutoML Tables models is 1000."` — the `bq query --dry_run` syntax check used for pre-validation (see the entry above) cannot catch this, since dry-run validates SQL syntax/schema only and does not execute AutoML's own row-count check. **This is a genuine, previously-undocumented platform limitation, not a notebook bug: `penguins` (~333 rows after filtering) can never train an `AUTOML_REGRESSOR`/`AUTOML_CLASSIFIER`, regardless of `budget_hours`.** `AUTOML_CLASSIFIER` is unaffected since `census_adult_income` has ~32,000 rows. Fixed by switching `models/automl_regressor/`'s dataset from `penguins`/`body_mass_g` to `bigquery-public-data.samples.natality` (Google's own canonical BQML regression tutorial dataset), filtered to `year=2003`/`mother_residence_state='WA'` (~80,000 rows, comfortably above the 1,000 floor), predicting `weight_pounds` from `mother_age`/`gestation_weeks`/`plurality`/`is_male`/`mother_married`/`cigarette_use`. Re-validated the new `CREATE MODEL` (including the `TRANSFORM`-rejection cell, which now correctly reproduces the exact `"Transform clause is not supported for the model type AUTOML_REGRESSOR"` error under `bq query --dry_run` even before a model exists) and rebuilt `automl_regressor.ipynb`/`.sql` from scratch — the only real output that existed (the free Setup cells) was trivial to reproduce, so no output-preservation tradeoff was needed. Added a prominent GOTCHA callout to the notebook's overview (with the exact error text) and to RESOURCES.md's AutoML Limitations section. **This breaks the project's `models/automl_regressor/` from the "same data/label as the other five regressors" convention that every other model type follows — an unavoidable, platform-forced exception, not a design choice.** Backlog entry and README.md updated accordingly; awaiting the user's next Restart & Run All attempt on the corrected notebook. |
| 2026-07-20 | `models/automl_classifier/` — Step 3 `ML.CONFUSION_MATRIX` hang diagnosed and fixed | User's Restart & Run All hung at Step 3's zero-argument `ML.CONFUSION_MATRIX(MODEL ...)` cell for hours, then errored; restarting the cell hung again. Diagnosed directly against the live project rather than guessing: `bq ls -j -a` showed 100+ identical `ML.CONFUSION_MATRIX` job attempts against `automl_classifier_income`, every one failing in under 1 second with `internalError`/`"Error: 21631273"` ("usually caused by a transient issue") — confirmed via a fresh `bq query` reproduction that this is NOT actually transient, it fails on every attempt. **Root cause of the "hang": `google-cloud-bigquery`'s default client-side retry logic treats this internal error as retryable and keeps resubmitting with exponential backoff — a query that fails in <1s looks like it's hanging for hours because the client silently retries a deterministic failure indefinitely.** Confirmed via direct testing that `ML.EVALUATE` and `ML.ROC_CURVE`'s zero-argument forms both work fine on the identical model — this is isolated specifically to `ML.CONFUSION_MATRIX`. A web search surfaced the actionable lead (some model types require an explicit data argument rather than the automatic-eval-split zero-arg form) — tested and confirmed: `ML.CONFUSION_MATRIX(MODEL ..., (SELECT ...))` with an explicit table works normally, returning a correct-looking confusion matrix in ~11 seconds against the full training table. Fixed Step 3 in `automl_classifier.ipynb` (via NotebookEdit, preserving Steps 1-2's already-captured real outputs) and `automl_classifier.sql` to pass the explicit training-table argument, plus added a GOTCHA callout in both and in RESOURCES.md's `AUTOML_CLASSIFIER`/`AUTOML_REGRESSOR` entry. **Instructed the user to interrupt the currently-hung cell rather than keep waiting — it will not resolve on its own.** General lesson: when a cell "hangs" on a BigQuery ML lifecycle function call, check `bq ls -j -a` for the actual job history before assuming it's slow — a fast, silently-retried failure looks identical to a genuinely slow query from inside a notebook, but the job list reveals the difference immediately. Also noticed (not yet investigated further) that Step 2's real `ML.EVALUATE` output shows `accuracy=0.5` exactly alongside `roc_auc=0.9297` — worth checking during the full review whether this reflects a poorly-calibrated default classification threshold on this imbalanced label, a known general phenomenon, or something else. `models/automl_classifier/`'s full review is still pending; regressor review is on hold until the classifier's fix is confirmed. |
| 2026-07-20 | `models/automl_classifier/` (run up to Cleanup) + `models/automl_regressor/` (fully run incl. Cleanup) — full review | Reviewed both notebooks cell-by-cell against real captured output (classifier: model still live, user held back Cleanup; regressor: fully run, Cleanup already dropped the model). **Two more real, verified findings beyond the Step 3 hang fix, both cost/time and metric-accuracy corrections to claims already shown to and approved by the user:** **(1) Wall-clock time was far worse than quoted.** `ML.TRAINING_INFO.duration_ms` showed the classifier took **2.63 hours** and the regressor **2.25 hours** for `budget_hours=1.0` — both far beyond the "~1.5 hours" estimate quoted during the AskUserQuestion cost approval (which came from the OLD, pre-project repo example on different data, not this project's own runs). Corrected the "~1.5 hours" claim to the verified real numbers in both notebooks' overview/Step 1 markdown, both `.sql` headers, and RESOURCES.md's AutoML repo-example line — future AutoML cost/time estimates in this project should budget 2-3 hours wall-clock per run, not 1.5. **(2) A second, deeper `ML.EVALUATE` anomaly, distinct from the Step 3 confusion-matrix bug.** Classifier: zero-arg `ML.EVALUATE` returned `accuracy=0.5` exactly alongside `roc_auc=0.930`. Diagnosed via `bq show --model` (the model was still live) — the evaluation ran against a class-balanced ~6,416-row internal eval set (3,208 per class, not the natural ~76/24 split), but `accuracy=0.5`/`precision=0.531`/`recall=0.509` matched NONE of the model's own 203 confidence-threshold rows in `binaryConfusionMatrixList` (whose literal threshold=0.5 row shows `accuracy=0.844`) — the Vertex AI methodology behind this aggregate isn't reconcilable from BigQuery's side. Regressor: `median_absolute_error` and `explained_variance` both returned exactly `0.0` while `mean_absolute_error`/`mean_squared_error` (matching `ML.TRAINING_INFO`'s `eval_loss` exactly)/`r2_score=0.352` all looked genuine — **could not be root-caused as deeply since Cleanup had already dropped the regressor model**, illustrating a real tradeoff of running Cleanup before review is complete. Documented both as verified GOTCHAs in Step 2 of each notebook, both `.sql` files, and RESOURCES.md, with the workaround already proven for the classifier (explicit data argument returns clean, reconcilable metrics — `accuracy=0.8508` matching a manual confusion-matrix calc exactly) generalized as guidance for both. **(3) A benign, sensible data-quality observation, not a bug:** the regressor's `cigarette_use` feature is NULL for 98% of rows (78,400/79,993) in the WA/2003 slice — consistent with, not contradicting, `ML.GLOBAL_EXPLAIN` ranking it the least important feature by a wide margin. Documented in the regressor's overview and `.sql` header as a sanity-check the model behaved sensibly. **Standing lesson: for a model type with real per-run dollar cost, don't run Cleanup until the full review is complete** — the regressor's Cleanup-before-review sequencing cost the ability to fully diagnose its `ML.EVALUATE` anomaly the way the still-live classifier could be. `models/automl_regressor/` is now fully reviewed and can be marked complete; `models/automl_classifier/` awaits the user re-running from Step 3 onward to confirm the `ML.CONFUSION_MATRIX` fix, after which it can be marked complete too (Cleanup still pending on that one). |
| 2026-07-20 | `models/automl_classifier/` — Step 3 fix confirmed, Cleanup did not actually run | User re-ran Step 3 through Cleanup. Reviewed the fresh output: Steps 1-8, the `%%bigquery` magics example, and the BigFrames markdown note are all bit-for-bit identical to the first run (same trained model, same full-table queries) — confirms the `ML.CONFUSION_MATRIX` explicit-data-argument fix is stable, not a one-off fluke. **However, Cleanup did not actually execute:** the saved notebook's Cleanup cell shows `execution_count: None` and empty `outputs`, and live checks confirmed the model is still real — `bq show --model` returns `automl_classifier_income` successfully, `bq ls -j -a` shows no `DROP MODEL` job in the recent history, and the exported GCS files (`saved_model.pb`, `assets/`, `variables/`) are still present in the bucket. Flagged this to the user directly rather than assuming success from the stated intent — asked them to run the Cleanup cell to drop the model and delete the exported files, which are still live and billing (minimally) for storage. No further notebook content changes needed; `models/automl_classifier/` is content-verified and ready to mark fully complete once Cleanup is confirmed to have actually run. |
| 2026-07-20 | `models/automl_classifier/` — Cleanup confirmed, Phase 2 complete | User re-ran Cleanup. Verified live rather than trusting the stated action: `bq show --model` now returns `Not found`, the exported GCS files under `bq_ml/automl_classifier/model/` are gone, and the saved notebook's Cleanup cell shows `execution_count=33` with the expected `Dropped automl_classifier_income` / `Deleted 12 exported model file(s)` output. `models/automl_classifier/` and `models/automl_regressor/` are both now fully built, reviewed, and verified — the ninth and tenth model types close out **Phase 2 (core supervised models) entirely** (GLM, boosted tree, random forest, DNN, wide-and-deep, AutoML — all five classifier/regressor pairs complete). Committing and pushing this work. |
| 2026-07-21 | Phase 4: `models/arima_plus/` — eleventh model, first in Phase 4 (time series) | User asked to start ARIMA-based work, pointing to three existing pre-project notebooks in `Applied Forecasting/` to modernize and expand: `BQML Univariate Forecasting with ARIMA+.ipynb`, `BQML Multivariate Forecasting with ARIMA+ XREG.ipynb`, and `BQML Regression Based Forecasting.ipynb` (the last uses `LINEAR_REG`/`BOOSTED_TREE_REGRESSOR` for forecasting via lagged features, not a new model type — scoped as a `workflows/` entry instead of a fourth `models/` folder). Entered plan mode given the scope (3 notebooks/1 workflow, new data choices, a new `workflows/` folder never used before in this project) and confirmed three decisions with the user via AskUserQuestion: **(1)** multi-series (`time_series_id_col`) is the PRIMARY example in both ARIMA notebooks, with a single-series example first as a simpler warm-up — this is also the concrete "modernization" of XREG, whose old notebook predates GA multi-series support and works around it with an `EXECUTE IMMEDIATE` loop. **(2)** Use 3 covariates for XREG (`avg_tripduration`/`pct_subscriber`/`ratio_gender`), dropping the old notebook's 4th (`capacity`, which needed a stations-table join and was NULL for some stations). **(3)** Fold the separate old "Notes - BQML ARIMA+ Handling of Granularity and Missing Data.ipynb" into `models/arima_plus/` as a step/GOTCHA callout, matching this project's one-notebook-per-model-type convention. Confirmed the user's suggestion that NYC Citi Bike data (already used in `contribution_analysis`/`ai_key_drivers`) works well here too, and that it's exactly what the old repo notebooks already used — picked 5 real stations (Pershing Square North, E 17 St & Broadway, W 21 St & 6 Ave, Lafayette St & E 8 St, West St & Chambers St; daily trip counts; 4 of 5 span 2013-07-01 to 2018-05-31, Pershing Square North starts later at 2014-09-01 — genuine multi-length-series data, not synthetic messiness). TEST = last 28 days (`horizon=28`), same window planned for all three notebooks/workflow so their forecast accuracy is directly comparable. Built `models/arima_plus/`: single-series `CREATE MODEL` (Pershing Square North alone) as a warm-up, then the main multi-series model (all 5 stations via `time_series_id_col`); full lifecycle (`ML.ARIMA_EVALUATE`, `ML.FEATURE_INFO`/`TRAINING_INFO`, `ML.EVALUATE`, `ML.FORECAST` + plot, `ML.EXPLAIN_FORECAST` + decomposition plot, `ML.ARIMA_COEFFICIENTS`, `ML.HOLIDAY_INFO`, `ML.DETECT_ANOMALIES`); the folded-in granularity/missing-data GOTCHA step; unsupported-functions step (`ML.PREDICT`, `ML.GLOBAL_EXPLAIN`, `ML.WEIGHTS`, `ML.CONFUSION_MATRIX`); magics; BigFrames (`bigframes.ml.forecasting.ARIMAPlus`, confirmed `id_col` param gives multi-series parity). **Fully pre-validated via real `bq query`/BigFrames runs against the live project (native BQML training is fast/free — ~12-16s per model — so, unlike the AutoML notebooks, full real pre-validation is back in play)**, all validation resources cleaned up afterward. **New findings verified live, not previously documented:** `ML.EVALUATE` with explicit eval data + `perform_aggregation=TRUE` includes `mean_absolute_scaled_error`, not previously listed in RESOURCES.md's ARIMA_PLUS metrics; a real missing day (`2015-10-20`, confirmed zero rows in the source data) gets linearly interpolated by `ML.EXPLAIN_FORECAST` to exactly `(141+363)/2=252` — a clean, exact confirmation of the documented interpolation behavior; `WEEKLY` `data_frequency` on daily data reproduces the documented error exactly; all four unsupported-function errors captured verbatim. Updated RESOURCES.md accordingly. Notebook built with fully pre-validated source; pending user Restart & Run All. Next: `models/arima_plus_xreg/`, then `workflows/regression_based_forecasting/` (fleshed out the vague existing Phase 7 "Demand forecasting" backlog item into this concrete plan). |
| 2026-07-21 | `models/arima_plus/` — review pass 1: removed unsupported-functions section, fixed a real chart bug, added custom-holiday/manual-order coverage | User ran the notebook and asked for three things during review. **(1) Removed the "Functions that do NOT apply" step entirely** (5 cells) per explicit request — a departure from every other model type in this project, which keeps that step; updated the overview's Lifecycle line to match. **(2) Fixed a real bug the user spotted**: Step 6's forecast chart lines "zig-zagged" because the `history` query powering the plot had no `ORDER BY` — BigQuery does not guarantee row order without one, so `ax.plot()` connected points in an arbitrary (not chronological) order. Added `ORDER BY start_station_name, date`. The `forecast` query and Step 7's decomposition query were already correctly ordered, so this was isolated to the one cell. **General lesson: every query feeding a line plot needs an explicit `ORDER BY` on the x-axis column — BigQuery result order is never implied by `GROUP BY`/aggregation alone, and a missing `ORDER BY` produces a visually obvious but easy-to-miss-in-code bug.** **(3) User asked whether custom holidays and other CREATE MODEL options were covered — they weren't.** Researched and verified live (via `bq query`, not just docs) the exact custom-holiday syntax: `CREATE MODEL`'s `AS` clause takes a special two-block form with NO `WITH` keyword and two required block names (`training_data`, `custom_holiday`) — confirmed working end-to-end (`ML.HOLIDAY_INFO` lists the custom holiday, `ML.EXPLAIN_FORECAST` gains a `holiday_effect_<name>` column) using a real NYC Marathon custom holiday on Pershing Square North. Also verified `auto_arima=FALSE` + `non_seasonal_order=STRUCT(p,d,q)` pins an exact ARIMA order (confirmed via `ML.ARIMA_EVALUATE` echoing back the exact specified order). Added both as a new Step 10 (small standalone single-station demo models, not the main multi-series model) and added `forecast_limit_lower_bound=0` directly to the main multi-series model as a real best practice (trip counts can't be negative) rather than just a side demo. Documented the exact verified custom-holiday syntax and manual-order option in RESOURCES.md's `ARIMA_PLUS` entry (previously only listed in a passing Limitations bullet with no example). All new SQL pre-validated live before insertion; all validation resources cleaned up. Notebook handed back to the user for a fresh Restart & Run All (Step 2's `CREATE MODEL` changed, invalidating everything downstream). |
| 2026-07-21 | `models/arima_plus/` — review pass 2: `forecast_limit_lower_bound` breaks `ML.EXPLAIN_FORECAST` | User's fresh run hit a real error in Step 7: `ML.EXPLAIN_FORECAST` on `arima_plus_multi` failed with `"This model was trained with either 'forecast_limit_lower_bound' or 'forecast_limit_upper_bound' being specified. In this case, EXPLAIN_FORECAST is not supported."` — a genuine, previously-undocumented incompatibility introduced in review pass 1, when `forecast_limit_lower_bound=0` was added to the main model as a "real best practice" without re-validating `ML.EXPLAIN_FORECAST` against the changed model (a real gap in that pass's pre-validation — only the `CREATE MODEL` call itself was re-tested, not every downstream lifecycle function). Confirmed live: `ML.FORECAST` still works fine with the bound set; only `ML.EXPLAIN_FORECAST` is blocked. **Fix: reverted `forecast_limit_lower_bound` from the main `arima_plus_multi` model** (Step 7's decomposition is used throughout the rest of the notebook) and **moved it into Step 10 as a third standalone demo** (small single-station model), alongside custom holidays and manual order, with a live `try/except` cell capturing the actual `ML.EXPLAIN_FORECAST` failure text — matching this notebook's live-capture convention. Corrected RESOURCES.md's `ARIMA_PLUS` entry: added the incompatibility as a GOTCHA under Supported Lifecycle Functions, and fixed the `forecast_limit_lower_bound` best-practices bullet (previously claimed "no adverse effect," now correctly caveated). **Lesson: when adding a CREATE MODEL option to a model that many downstream lifecycle-function cells already depend on, re-validate every one of those functions against the modified model, not just the CREATE MODEL call itself** — an option can be individually valid while still breaking a specific downstream function in a way that's only surfaced by actually calling it. Re-validated the full corrected sequence live (main model without the bound + `ML.EXPLAIN_FORECAST` working + the bounded side-model's `ML.FORECAST` working + its `ML.EXPLAIN_FORECAST` failing exactly as expected); all validation resources cleaned up. Notebook handed back for another Restart & Run All. |
| 2026-07-21 | `models/arima_plus/` — review pass 3: full run confirmed clean, one completeness gap closed | User's fresh run completed fully end to end — every cell's real output cross-checked against expectations and matched exactly (Steps 1-12, all three Step 10 demos, magics, BigFrames, Cleanup dropping all 6 models + the table). Reviewing closely surfaced one real completeness gap, not a bug: Step 10's markdown claims the custom holiday's effect "shows up in `ML.EXPLAIN_FORECAST` as `holiday_effect_<holiday_name>`," but the notebook never actually queried that column — only `ML.HOLIDAY_INFO` was shown live. Verified the claim directly (re-created the custom-holiday model temporarily) and added a live cell querying `holiday_effect_NYCMarathon` near a real marathon date. **Honest finding, not a dramatic one: the effect comes out to exactly `0.0`** — with only 4 marathon dates in the training history, the model doesn't detect a statistically distinguishable effect for this station. Framed accurately in the notebook/`.sql` as "confirms the column exists, not that the effect is large" rather than implying a strong demonstrated effect. `models/arima_plus/` is now fully reviewed and complete pending one final quick re-run to populate the new cell's output (no other cell needs to change — everything else already matched). Eleventh model type, first in Phase 4, ready for final checkoff once that last cell is confirmed. |
| 2026-07-21 | `models/arima_plus/` — final confirmation, fully verified | User re-ran and the new cell populated exactly as pre-validated: `holiday_effect_NYCMarathon = 0.0` across all five checked dates. Cleanup output and a live `bq ls` both confirm all 6 models and the table were dropped — nothing left running. `models/arima_plus/` is now the eleventh fully verified canonical example in this project, closing the first notebook of Phase 4 (time series). Next: `models/arima_plus_xreg/`. |
| 2026-07-21 | Phase 4: `models/arima_plus_xreg/` — twelfth model, second in Phase 4 | Built the multivariate ARIMA_PLUS_XREG model on the identical 5 Citi Bike stations and TEST window (last 28 days, `horizon=28`) as `models/arima_plus/`, so the two models' forecast accuracy is directly comparable — plus 3 covariates (`avg_tripduration`/`pct_subscriber`/`ratio_gender`; `capacity` dropped as already decided, needs a stations-table join and was NULL for some stations). Single-series warm-up then multi-series `time_series_id_col` main model, directly replacing the old repo notebook's `EXECUTE IMMEDIATE`-loop workaround (that notebook predates GA multi-series support for this type). **Applying the standing lesson from `models/arima_plus/`'s review pass 2 (re-validate every downstream lifecycle function, not just CREATE MODEL, after any option change): tested every option combination against every lifecycle function before writing a single notebook cell**, surfacing three real, previously-undocumented cross-model-type differences from plain `ARIMA_PLUS`: **(1)** `ML.EVALUATE` does NOT include `mean_absolute_scaled_error` (confirmed by comparing actual output columns of both types on the same underlying data). **(2)** `forecast_limit_lower_bound` is REJECTED OUTRIGHT at `CREATE MODEL` time (`"Option(s) FORECAST_LIMIT_LOWER_BOUND are not supported for ARIMA_PLUS_XREG model training"`) — a stricter, different failure mode than plain `ARIMA_PLUS`, where the option is accepted but only breaks `ML.EXPLAIN_FORECAST` downstream. Demonstrated live via `try/except` in Step 11, alongside confirming custom holidays and manual `non_seasonal_order` both work identically to `ARIMA_PLUS` (same syntax, verified, not just assumed). **(3)** `ML.FORECAST`'s 2-argument form fails immediately (`"Model type ARIMA_PLUS_XREG requires three parameters in ML.FORECAST"`) — confirms and sharpens the documented "covariates required" limitation into an exact, quotable error message; `ML.EXPLAIN_FORECAST` has the identical 3-argument requirement. **Also found and documented a real, benign data-quality gotcha:** 4 rows (`E 17 St & Broadway`) have a NULL `ratio_gender` (all-unknown-gender days, `0/0`) — `CREATE MODEL` emits a warning but trains successfully with a real, sensible non-NULL regression weight for that covariate; a genuinely different situation from the existing "all-NULL for an entire series" limitation (which does block training). Applied the `ORDER BY`-on-plot-queries lesson from the start this time (Step 6's history query). No "unsupported functions" section, matching the precedent set for `models/arima_plus/`. No dedicated BigFrames `ARIMAPlusXReg` class (confirmed: `bigframes.ml.forecasting` exposes only `ARIMAPlus`) — markdown-only gap note. Fully pre-validated live end-to-end (every lifecycle function against the actual multi-series model, not just the CREATE MODEL statement), all validation resources cleaned up. Updated RESOURCES.md's `ARIMA_PLUS_XREG` entry with all four new findings. Notebook built with fully pre-validated source; awaiting the user's Restart & Run All. |
| 2026-07-21 | `models/arima_plus_xreg/` — final confirmation, fully verified on first run | User ran the notebook completely in one pass. Every cell's real output cross-checked against pre-validation and matched exactly, including the three verified GOTCHAs' exact error text (`ML.FORECAST` 3-parameter requirement, `forecast_limit_lower_bound` rejection, custom holiday/manual order confirmations) and `ML.ARIMA_COEFFICIENTS`' full 25-row output (5 rows × 5 stations, correctly grouped by station despite no explicit `ORDER BY` — verified this is BigQuery's natural result order here, not a bug, before concluding no fix was needed). Cleanup dropped all 4 models + the table; live `bq ls` confirmed nothing left running. Thanks to applying the "re-validate every downstream function" lesson rigorously during the build, this is the first Phase 4 notebook to pass full review with zero required fixes. `models/arima_plus_xreg/` is now the twelfth fully verified canonical example, closing out the second notebook of Phase 4. Committing and pushing `models/arima_plus/` + `models/arima_plus_xreg/` together. Next: `workflows/regression_based_forecasting/`. |
| 2026-07-21 | `models/arima_plus/` + `models/arima_plus_xreg/` — post-commit visual enhancements | User asked, after both notebooks were already committed: could the `ML.DETECT_ANOMALIES` step include a plot (time series + forecast + labeled anomalies) instead of just the top-10 table, and could other explanatory visuals be added anywhere? Added two things to both notebooks: **(1)** a combined history+forecast+anomaly plot at the end of the anomaly-detection step — full per-station history (not just a recent window, since flagged anomalies are spread across the ~5-year history) plotted alongside the already-computed `forecast` (reused from the earlier `ML.FORECAST` step, no re-fetch needed), with flagged points marked as red scatter dots directly on the line. **(2)** a per-station MAPE bar chart in the `ML.EVALUATE` step, for an at-a-glance accuracy comparison across the 5 stations. Deliberately did NOT add charts for `ML.ARIMA_EVALUATE` (AIC/order) or `ML.ARIMA_COEFFICIENTS` (regression weights) — AIC isn't a valid cross-series comparison metric (only valid for comparing candidate orders *within* one series), and coefficient weights differ wildly in scale/units per covariate, so a chart there would mislead more than clarify; explained this reasoning to the user rather than adding decoration for its own sake. Live-validated the new full-series anomaly queries against fresh throwaway models for both model types (both types' `ML.DETECT_ANOMALIES` return every historical point with an `is_anomaly` flag, not just flagged ones, confirmed row counts ~1300-1800/station), all validation resources cleaned up. Both notebooks need a re-run to populate the new cells (kernel state from the prior run would also work, since the new cells depend only on variables — `forecast`, `eval_metrics` — already computed earlier in the existing step order). |
| 2026-07-21 | `models/arima_plus/` + `models/arima_plus_xreg/` — review of new visuals uncovers a real, major finding: a genuine 6-month data gap + different gap-handling between model types | User re-ran both; reviewing the rendered anomaly PNG (actually visually inspected the image, not just checked for "no error") revealed a suspicious near-straight diagonal line during ~Oct 2016 - Mar 2017 across ALL 5 stations simultaneously in `models/arima_plus/`. Investigated directly rather than assuming a plotting bug: confirmed live that `bigquery-public-data.new_york_citibike.citibike_trips` has **zero rows for October 2016 through March 2017** — a real, previously-unnoticed ~6-month gap in the public dataset itself, not station-specific. First hypothesis (missing rows causing a matplotlib "bridge" line) was WRONG and had to be corrected: `ML.DETECT_ANOMALIES`'s output has exactly one row per calendar day with NO missing rows at all — confirmed the "diagonal line" is BQML's own **linear interpolation of a real numeric value across the entire 6-month span** (verified precisely: one station's value declined by a constant `~0.415`/day for all ~183 gap-days, from `194.0` to `118.0`). An initial "fix" (reindexing to introduce NaN for gaps) was implemented, tested, found to do NOTHING (confirmed 0 NaN rows after reindex, since there were no missing rows to begin with), and reverted — a real instance of diagnosing before fixing rather than patching symptoms. **Then checked `ARIMA_PLUS_XREG` specifically rather than assuming identical behavior (per the now-standing lesson) — found a genuinely different result: `ARIMA_PLUS_XREG`'s `ML.DETECT_ANOMALIES` returns `NULL` for the target/anomaly columns throughout the same gap, which correctly renders as a real break in the chart with no fix needed.** Documented both findings precisely and differently in each notebook's Step 10/11 markdown (not copy-pasted as identical, since they aren't) and in RESOURCES.md's `ARIMA_PLUS`/`ARIMA_PLUS_XREG` Limitations sections. **General lesson: visually inspect rendered plot images (not just "did it error"), and don't accept the first plausible explanation for a visual anomaly — verify the actual underlying data before writing a fix or a GOTCHA claim.** All validation resources (including a lightweight standalone Python render to test the fix before touching the notebook) cleaned up. Both notebooks need a final re-run to confirm the corrected markdown/code combination. |
| 2026-07-21 | `models/arima_plus/` + `models/arima_plus_xreg/` — final confirmation after visual fixes, committed | User's final re-run confirmed both corrected notebooks: `arima_plus`'s anomaly plot shows the expected smooth interpolation line across the 6-month gap (matches its markdown), `arima_plus_xreg`'s shows a genuine break with no interpolation (matches its markdown) — visually verified from the actual rendered PNGs, not assumed. Cleanup cells in both dropped all models/tables; confirmed via `bq ls` that nothing was left behind. Committed the visual-enhancement + gap-investigation changes (`ed565697`), not yet pushed. Both notebooks are now fully closed out. Next: `workflows/regression_based_forecasting/`. |
| 2026-07-21 | Phase 7 — first workflow: `workflows/regression_based_forecasting/` built and fully verified | `LINEAR_REG`/`BOOSTED_TREE_REGRESSOR` applied to forecasting on the identical Pershing Square North station/TEST window as `models/arima_plus/`: time features only, + lags (LEAKED/TRUNCATED/RECURSIVE evaluation comparison), and direct multi-step (one model per horizon day via `LEAD`) for both model types. Discovered and diagnosed a real `BOOSTED_TREE_REGRESSOR` training-time GOTCHA (~2.5-4.5 min/model regardless of data size) — ruled out hyperparameters and a slot reservation as causes via live testing (see [[feedback_bqml_reservation_pattern]]), fixed via concurrent job submission (verified BigQuery runs distinctly-named `CREATE MODEL` jobs in true parallel). Executed the full assembled notebook once via `nbclient` before handoff (see [[feedback_full_notebook_preexecution]]), which caught a real `ML.EVALUATE` column-name bug (borrowed from `ARIMA_PLUS`'s schema by mistake) before the user ever saw it. Real results: `ARIMA_PLUS` wins MAE/RMSE/pMAE, but direct multi-step `BOOSTED_TREE_REGRESSOR` gets the best MAPE of any technique in the comparison. Committed (`d72b0137`). |
| 2026-07-21 | `models/arima_plus/` — added `hierarchical_time_series_cols` to Step 10, backlogged `workflows/hierarchical_forecasting/` | User pointed to an existing pre-project notebook, `Applied ML/Forecasting/BigQuery ML For Hierarchical Forecasting.ipynb`, for review. It covers BQML's built-in hierarchical forecasting (bottom-up reconciliation via `hierarchical_time_series_cols`) plus a from-scratch custom top-down disaggregation on Iowa liquor sales data (real State/County/City/Store hierarchy) — a real, previously undocumented-in-practice `ARIMA_PLUS` option (listed in RESOURCES.md's options table but never demonstrated live). Split per user's direction: the option itself goes into `models/arima_plus/` (Citi Bike, for consistency with the rest of that notebook); the top-down comparison becomes a new workflow using the original Iowa data (which has a real hierarchy Citi Bike doesn't). Since Citi Bike's `region_id` field turned out useless for this (nearly all NYC trip history collapses into one region), built a real geographic 2-level hierarchy instead: 2 Manhattan neighborhoods (Midtown, Downtown) x 3 stations each, all verified to have full 2013/2014-2018 history. Verified live, exact to the penny: reconciliation is bottom-up only — station forecasts sum exactly to their neighborhood, neighborhoods sum exactly to the overall total. Added as a new subsection of Step 10 (matching the existing custom-holiday/manual-order/bounds pattern), updated the overview and RESOURCES.md's `ARIMA_PLUS` entry. `workflows/hierarchical_forecasting/` added to the Phase 7 backlog, not yet built. |
| 2026-07-21 | Phase 7 — second workflow: `workflows/hierarchical_forecasting/` built and fully pre-validated | Modernizes the old Iowa notebook onto a real `State → County → City → Store` hierarchy (2 counties x 2 cities x 2 stores = 8 stores, 15 series total). **Modernization decision, confirmed with the user first:** switched from the old notebook's DAILY granularity to WEEKLY — checked real per-store data density and found daily coverage is only ~14-30% for several stores (most liquor stores don't sell every day) vs. ~90%+ at weekly, meaning daily granularity would have made most training points BQML-interpolated placeholders rather than real observations. Also found and fixed a real data-quality gotcha: `county`/`city` in the raw source table are occasionally inconsistent for the same `store_number` (a handful of historical rows blank) — fixed by mapping each store to its county/city explicitly via a `CASE` on `store_number`, rather than trusting those columns directly. Built: Step 1 (base-level, no hierarchy), Step 2 (built-in bottom-up `hierarchical_time_series_cols`, re-verified on this deeper 4-level hierarchy), Step 3 (custom top-down: flatten every level into one series set, train one flat `ARIMA_PLUS` model, show the forecast-proportions disaggregation formula explicitly for one level, then a **generalized Python function** — `build_hierarchical_topdown_forecast` — that cascades the same formula through any number of hierarchy levels, replacing the old notebook's ~150-line hardcoded-depth automation with a much shorter loop-based one). **Verified the generalized function reproduces the manually-computed county-level disaggregation bit-for-bit** (included as a sanity-check cell in the notebook). Step 4 compares BQML bottom-up vs. custom top-down accuracy (MAE/MAPE) at every level against real TEST actuals — **found a genuine, honest result: neither technique universally wins** (bottom-up better on some series, e.g. Ankeny; top-down better on others, e.g. the state total and Polk county) — a different picture from `workflows/regression_based_forecasting/`'s comparison, where one technique was consistently strongest, because here both techniques start from the same underlying `ARIMA_PLUS` forecasts and just reconcile them in different directions. **Also surfaced a real MAPE-instability GOTCHA**: one store sold exactly 1 bottle in a single TEST week, producing a MAPE in the thousands of percent for an otherwise-unremarkable absolute error — documented as a known property of percentage metrics near zero, not a modeling bug. Executed the full assembled notebook once via `nbclient` before handoff (per [[feedback_full_notebook_preexecution]]) — zero errors on the first pass, Cleanup verified live via `bq ls`. Outputs cleared before handoff, awaiting the user's Restart & Run All. |
| 2026-07-21 | `models/arima_plus/` + `workflows/hierarchical_forecasting/` — both fully reviewed and verified after the user's Restart & Run All | Every output in both notebooks cross-checked against pre-validation and matched exactly (bit-for-bit on `arima_plus`'s new hierarchy step; matched to displayed precision on `hierarchical_forecasting`, including the MARION MAPE outlier at 27526.4%/15568.2% exactly as pre-validated). `arima_plus` Cleanup dropped all 7 models + 2 tables; `hierarchical_forecasting` Cleanup dropped all 4 models + 4 tables; both confirmed via live `bq ls`. One trivial fix in `hierarchical_forecasting`: removed an unused `matplotlib` import/install (this notebook has no charts) — dead code with zero effect on any executed cell, fixed without requiring a re-run. Both notebooks are now fully closed out — `arima_plus` remains the eleventh model-type notebook (now including hierarchical forecasting), and `hierarchical_forecasting` is the second fully verified workflow. |
| 2026-07-21 | Retired the source notebook: `Applied ML/Forecasting/BigQuery ML For Hierarchical Forecasting.ipynb` deleted | User asked for a confidence check on the rebuild before removing the original. Compared feature-by-feature: base-level forecasting, built-in bottom-up hierarchy, bottom-up mechanics verification, flattened top-down model, forecast-proportions disaggregation (explicit + generalized), and the bottom-up-vs-top-down accuracy comparison all carried over faithfully (and the rebuild fixes a real bug the original had — daily granularity mismatched to real per-store data density — plus adds a live bit-for-bit reproducibility check the original didn't have). One legitimate, intentional simplification: the rebuild's generalized Python function covers forecast-building only, not a second generalized function for the accuracy-comparison step (the original had both) — a reasonable scope reduction, not a lost capability. Confirmed no other repo file references the old notebook by content (only its own folder's readme). Deleted the file (fully recoverable via git history) and updated `Applied ML/Forecasting/readme.md` to point to `models/arima_plus/` (bottom-up option) and `workflows/hierarchical_forecasting/` (top-down comparison) instead. |
| 2026-07-22 | Phase 7 — fourth workflow: `workflows/embeddings_classification/` built, expanded with 2 baselines, fully verified | User handed off a 334-cell notebook from the `Applied GenAI/Embeddings` modernization effort — a BQML+BigFrames workflow off-pattern for that GenAI-SDK-focused folder. Retail product-catalog hierarchy placement via a binary "does this product belong to this hierarchy node?" classifier over (product, candidate node) pairs. **Real cost risk found pre-build: the original's 5 BigFrames `XGBClassifier` variants took 52 min - 9h39m EACH to train.** Entered plan mode; a permission gate on an IAM grant for the initially-planned `AI.GENERATE_EMBEDDING` connection led to discovering `AI.EMBED` needs no connection at all for text — simpler than planned, not just cheaper (see [[feedback_permission_gate_reverify_premise]]). Built with 3 reduced-scope pairwise approaches (256-dim embeddings, native SQL `BOOSTED_TREE_CLASSIFIER` with embeddings as `ARRAY<FLOAT64>` features directly — verified live, no unnesting needed) — training now 19-40 min/model, 13x+ faster than the original, still trained concurrently. **After reviewing, the user asked for an honest assessment of the approach's merit — the answer (pairwise binary-relevance doesn't scale gracefully and may be overkill for a small static hierarchy) led to two added baselines**: a direct multiclass classifier and a pure `VECTOR_SEARCH` lookup (no index needed at this scale, per user's steer). **Major finding, confirmed on a full fresh run: both baselines beat every pairwise variant, on accuracy AND cost** — multiclass baseline reaches ~69-70% category accuracy on a ~38x smaller table in minutes; zero-training `VECTOR_SEARCH` reaches ~52%; the best pairwise variant (`conadiff` + `auto_class_weights`) only reaches ~42-48%. This inverted the notebook's own teaching point: from "here's how to do embeddings-as-features classification" to "here's when the fancy approach isn't the right one, and how to check." Cross-checked the `conadiff`/`conadiff_weighted` retraining-variance numbers against a genuinely fresh full run before finalizing the Interpretation (used hedged "~" language throughout rather than exact figures, given known `BOOSTED_TREE_CLASSIFIER` retraining variance). Retired the original notebook to `Applied GenAI/Embeddings/legacy/`, repointed all 3 inbound links. |
| 2026-07-22 | Phase 5 — Model categories, all 4 notebooks built and fully verified | User picked Phase 5 next and pointed out `MLOps/Serving/` already has deep production-grade notebooks for these same three patterns (import/remote/export) — bq-ml's job is the focused CREATE MODEL/lifecycle mechanics, cross-linked both directions to that existing content rather than duplicated. **Scope amended from 3 to 4 items** (see the Phase 5 section above) to fold in `TENSORFLOW_LITE` and add `TRANSFORM_ONLY`, both already fully spec'd in RESOURCES.md. Built in cost/risk order — cheapest and lowest-risk first, live billable infra last: `transform_only` (no external deps) → `imported` (local-only conversions, needed an isolated test kernel with `numpy<2`/`xgboost==1.5.1` pinned to avoid polluting the shared bq-ml venv) → `export` (GCS only) → `remote` (the only one with live billable infra). **`models/remote/`'s IAM grant (`roles/aiplatform.user` for the new connection's service account) was blocked by the permission-gate classifier and required explicit live user authorization** — unlike the `AI.EMBED` false alarm earlier in this project, this premise was correct (REMOTE really does need a connection), so the right move was to ask, not to route around it; user authorized it via `AskUserQuestion`. The Vertex AI Endpoint was live only a few minutes (deploy → predict single-row + 200-row batch, 0 errors → full teardown) before complete cleanup, confirmed via `bq ls`/`gcloud`. Real findings beyond what RESOURCES.md already had: `XGBOOST` import caps at **XGBoost ≤ 1.5.1** for the training-side file (new — the reverse-direction `xgboost==1.7.6` export-side pin was already known from `boosted_tree_classifier`); a **correction** to RESOURCES.md's own capability table — `ML.EXPLAIN_PREDICT` is documented as supported for `TENSORFLOW` imports but rejected live; `ML.TRANSFORM` silently passes through unreferenced input columns; a downstream model with no embedded `TRANSFORM` silently mispredicts on raw data (reproduced live — every row predicted "Gentoo penguin" until re-wrapped in `ML.TRANSFORM`); dropping a `model_registry='VERTEX_AI'`-registered model also cascades-deletes its Vertex AI Model Registry entry. Also corrected `setup/README.md`'s connection matrix, which incorrectly listed imported models and `EXPORT MODEL` as needing a connection (verified live: neither does). Hit a sandbox-specific `nbclient`/BigQuery-client hang on `models/export/`'s long boosted-tree training cell (worked around by training that one model directly via `bq query`, validating everything else live through the kernel) — not reproduced in the other three notebooks and not expected to affect the user's own run in a normal Jupyter/Colab environment. `models/remote/` was validated via direct `bq`/`aiplatform`/`gcloud` calls rather than a second `nbclient` pass, to avoid deploying (and paying for) a second Endpoint. |
| 2026-07-22 | Phase 5 — real-run fix + review round: `models/imported/`, then all three non-`remote` notebooks reviewed after the user's own run | User ran `transform_only`, `imported`, and `export` themselves. `imported` and `export` both hit the exact same class of error (`ModuleNotFoundError: No module named 'numpy.strings'`/`'numpy.rec'`) — root-caused to running both notebooks' Setup/`install()` cells around the same time: they share one `.venv`, and `imported`'s `numpy<2`/`xgboost==1.5.1` pins raced with `export`'s `xgboost==1.7.6` pin, corrupting `scipy` mid-install (confirmed by reproducing the exact failure in an isolated venv seeded with a "polluted" pre-existing state, and confirming the same package combination works perfectly when installed without a race). **Fixed `imported.ipynb`** with an environment health-check cell right after Setup that verifies `numpy`/`xgboost` landed correctly and fails immediately with an actionable "re-run this cell" message instead of a cryptic scipy trace three cells later — plus a markdown note warning not to run two bq-ml notebooks' Setup cells concurrently. **Also found and fixed a real, independent gap while debugging**: `xgboost==1.5.1`'s `compat.py` imports the now-deprecated `pkg_resources` from `setuptools`, which is missing from very recent `setuptools` releases — added `setuptools<81` to `imported.ipynb`'s install list (this was already known from earlier ad-hoc validation but never made it into the shipped notebook). Verified the fix by running the full notebook end-to-end from a deliberately pre-polluted environment — zero errors, health check confirms self-heal. On the subsequent review of all three notebooks' real run outputs: zero errors across all three, everything matched pre-validation almost exactly (including `transform_only`'s Step 7/8 gotcha reproducing identically — every row predicting "Gentoo penguin" until `ML.TRANSFORM` is re-applied — and `export`'s cascade-delete-on-drop finding for `model_registry='VERTEX_AI'`). **Caught one real overclaim during review**: `imported.ipynb`'s TFLite step said predictions were "numerically identical" to the TensorFlow import — the real numbers differ at the 7th-8th decimal place (ordinary float32 kernel differences between the TF runtime and TFLite interpreter, not quantization) — corrected the wording in both the notebook and RESOURCES.md's `TENSORFLOW_LITE` repo-example line to "match to ~7 significant figures." **New standing lesson for this project specifically: notebooks sharing the bq-ml `.venv` must not have their Setup/`install()` cells run concurrently**, even when the notebooks' own SQL/logic is fully independent — added to memory. |
| 2026-07-23 | Phase 5 — `models/remote/` revised: native `model_registry='VERTEX_AI'` shortcut investigated and found broken (real Google bug), XGBoost-genericity demo added, two real bugs fixed from the user's live run | User's live run hit a silent failure at the connection/IAM step and separately proposed replacing the notebook's manual `EXPORT MODEL`+upload path with the "simpler" native `model_registry='VERTEX_AI'` registration, since BigQuery can register a model directly to Vertex AI Model Registry without an explicit export. **Investigated the native path live before committing to the rewrite, and found it's currently broken**: BQML unconditionally bakes a full Sampled-Shapley `explanationSpec` into every model registered this way (confirmed via `gcloud ai models describe`), and deploying such a model to a Vertex Endpoint fails with `InvalidArgument: 400 Error occurred in Explanation preprocessing. ValueError: NodeDef mentions attr 'debug_name' not in Op<name=VarHandleOp...>` — reproduced live, and confirmed as a known, currently-open Google issue ([vertex-ai-samples#2723](https://github.com/GoogleCloudPlatform/vertex-ai-samples/issues/2723), filed against Google's own official BQML-online-prediction sample, closed "not planned"). **This reversed the planned revision**: kept the notebook's manual export+upload path (Steps 1-2) as the reliable primary route (no baked-in explanation spec, never hits the bug) and instead added a new **Step 4** that demonstrates the native shortcut as a documented failure — trains a scratch model, prints the empty `container_spec`, attempts `.deploy()` inside a `try/except` so the real error prints without crashing "Restart & Run All," then drops the scratch model. Reported this finding to the user before implementing, per standing practice of not silently deciding on a reversed architecture. **Added Step 5** per the user's separate ask to show `REMOTE` works with "nearly any model" — an XGBoost model trained entirely outside BigQuery (no BQML involvement at all) is registered to Vertex AI Model Registry with the `xgboost-cpu.2-1` pre-built container, proving genericity; deliberately *not* deployed (stays within the notebook's one-live-Endpoint cost boundary), cross-linked instead to `MLOps/Serving/SQL Inference/BQML Remote Model on Vertex AI Endpoint.ipynb` for the fully-deployed version. **Fixed two real bugs surfaced by the user's live run**, both in the connection/IAM setup step (now Step 6): (1) a silent failure — `subprocess.run(...)` calls for `bq mk --connection` and the IAM grant never checked `.returncode`, so a failed `gcloud add-iam-policy-binding` printed a false "Granted roles/aiplatform.user" success message; root-caused by directly checking `gcloud projects get-iam-policy` and finding zero bindings for the connection's service account despite the success message — fixed with explicit returncode checks that `raise RuntimeError(...)` including captured stderr, and (with the user's follow-up ask) left the grant itself in the notebook's own code so future users don't need a side-channel fix; (2) a case-sensitivity bug **introduced by fix (1) and caught on the user's very next re-run** — the real `bq` CLI "already exists" error text is `"Already Exists: Connection ..."` (capitalized), but the new check was lowercase-only; verified the exact casing live by deliberately creating a duplicate scratch connection, then fixed with a `.lower()` comparison. Also replaced the single fixed 60s `CREATE MODEL` wait with a retry loop (up to 8 attempts, 20s apart, only retrying on a permission-flavored error) after observing IAM propagation take over two minutes in one case. All live scratch resources (test models, a zero-model-attached test endpoint, the casing-test connection, the native-shortcut demo's cascade-deleted Vertex resources, the XGBoost-external Vertex Model + GCS blob) were deleted after each investigation. Renumbered the notebook to 8 steps (native-shortcut and XGBoost-genericity demos inserted as Steps 4-5, connection/CREATE MODEL/PREDICT pushed to Steps 6-8); updated `remote.sql` to match with two new commented-out Example sections. Outputs cleared for the user's fresh run; not re-executed via `nbclient` (same live-Endpoint cost-avoidance rationale as the original build). |
| 2026-07-23 | Phase 5 — full review round after the user's fresh run of all four notebooks: `transform_only`/`imported`/`export` pass clean (minor wording fixes only), `remote` had a real infra-leak bug fixed | Reviewed each notebook's actual executed output (dedicated pass per notebook, cross-checking every markdown claim against the cell output nearby, per the project's standing practice). **`transform_only`**: one real wording bug — Step 2's gotcha markdown claimed the passed-through `sex` column was "not referenced anywhere in the TRANSFORM clause," but `sex` IS referenced (as `ML.ONE_HOT_ENCODER(sex)`'s input); the actual rule is about output-alias naming, not references — reworded to "any input column whose name isn't reused as an output alias..."; also cleared a stale leftover output artifact on the commented-out final cleanup cell (execution_count was null, meaning it wasn't part of the real run, but still carried an old display object). **`imported`**: fully clean — health-check cell showed exactly the pinned versions, TFLite-vs-TF wording held up numerically, all 4 models dropped, no leftover overclaims; fixed one wording nit (an error message said "re-run the two cells above" when only the install cell needs re-running). **`export`**: fully clean, no fixes needed — TF/XGBoost exports verified numerically consistent, cascade-delete-on-drop confirmed again, both "outside BigQuery" proof cells verified to contain no disguised BigQuery client calls. **`remote`**: Steps 1-3 and 6-8 clean (Step 7's retry loop needed 0 retries this run), but **Step 4's native-shortcut demo had a real bug**: `native_model.deploy(...)` was called with no `endpoint=` argument, so the Vertex AI SDK auto-created a brand-new Endpoint before the deploy failed as expected — that endpoint was never captured in a variable and Cleanup never touched it, directly contradicting the notebook's own "only one Endpoint is ever live" claim. Confirmed live via `gcloud ai endpoints list` that the orphaned, empty endpoint (`bq_ml_remote_native_shortcut_demo_endpoint`) was still sitting in the project. **Asked the user for authorization before deleting live infrastructure** (per this project's standing practice for any live-infra action, even cleanup of a bug's own byproduct) — authorized, deleted via `gcloud ai endpoints delete`, confirmed removed. **Fixed the root cause**: Step 4 now creates an explicit `aiplatform.Endpoint.create(...)`, passes it via `deploy(endpoint=...)`, and deletes it in a `finally` block regardless of whether the deploy succeeds or fails (`force=True` in case a model partially attaches) — so a future run can no longer leak an endpoint here. Softened the intro markdown's "only one Endpoint is ever live" claim to "only one Endpoint carries real cost," explaining Step 4's create-then-immediately-delete pattern. **General lesson: when a demo cell intentionally triggers a failure via `try/except`, audit every side effect the attempted operation could have had before it failed — not just whether the exception itself was caught correctly.** All four Phase 5 notebooks are now fully reviewed and verified. |
| 2026-07-23 | `models/remote/` — fix confirmed on a full fresh Restart & Run All | User re-ran the entire notebook end to end after the Step 4 endpoint-leak fix. Confirmed via output: Step 4's scratch endpoint was created, the deploy failed with the same documented error, and this time printed "Scratch endpoint deleted" — no leak. Step 6/7's retry loop got real exercise this run (needed 1 of 8 attempts before the IAM grant propagated, vs. 0 retries on the prior run), confirming the retry logic actually works under real timing, not just in theory. Verified live via `gcloud ai endpoints list`/`gcloud ai models list`/`bq ls --connection` that zero resources were left behind — only a pre-existing, unrelated endpoint (`05`) remains in the project. All four Phase 5 notebooks are now fully built, reviewed, fixed, and confirmed clean on a real run. |
| 2026-07-23 | Project-wide RESOURCES.md completeness audit (closing the standing "refine from running code" task) | Before committing all Phase 5 work, audited PLANS.md's full Audit log (all ~60 entries, every model type built so far) against RESOURCES.md to confirm every verified finding is actually reflected there, not just captured in PLANS.md/memory. Split the audit three ways (Phase 2 GLM/tree-ensemble/DNN models; Phase 3 unsupervised+AutoML models; Phase 4 time-series+Phase 7 workflows) plus a direct check of Phase 5's own entries. **Result: coverage was already excellent** — the standing per-notebook RESOURCES.md refinement practice had genuinely kept pace; only 3 minor gaps found, all in *general* (non-model-specific) function entries that hadn't been updated when a model-specific section discovered something first: (1) `ML.DETECT_ANOMALIES`'s general entry still said "not yet verified whether `AUTOENCODER`" needs the 3-argument form, contradicting the `AUTOENCODER` section which already confirmed it — updated to state all three IID types (`KMEANS`/`PCA`/`AUTOENCODER`) require it; (2) `ML.GENERATE_EMBEDDING`'s general entry implied its output "can be consumed directly by `VECTOR_SEARCH`," contradicting the `AUTOENCODER` section's verified finding that `VECTOR_SEARCH` rejects that pattern and needs a materialized table first — added the caveat (plus the no-normalization finding) to the general entry's Limitations; (3) `ML.TRAINING_INFO`'s model-specific-differences table had rows for K-means and time-series but none for the boosted-tree/random-forest family's iteration numbering starting at 1 (not 0) and the first-iteration data-loading overhead — added a row. **General lesson: the standing practice of refining RESOURCES.md per-notebook is sound, but periodically audit the *general* function entries specifically** — a finding discovered while building one model-specific section doesn't automatically propagate to the general entry for the underlying function, since those are usually written first (during the docs-first research pass) and not revisited unless something explicitly contradicts them. |
| 2026-07-23 | Phase 6 — Model-free functions, all 6 notebooks built and fully verified in one session | User picked Phase 6 next, wanting these "super helpful" since the functions are rarely discovered/used — the bar set was matching (and exceeding) the depth of the legacy `03 - BigQuery ML (BQML)/BQML Feature Engineering*` notebooks. **Grouped by sub-category** (scalers, feature_engineering, encoding, bucketizing, distance, text) rather than one-function-per-notebook or one-mega-notebook, mirroring the `models/imported/` precedent; **scope amended** to fold in `ML.NORMALIZER`/`ML.HASH_BUCKETIZE`/`ML.MULTI_HOT_ENCODER` (already covered by RESOURCES.md/legacy content but missing from this checklist), same pattern as Phase 5's amendments. Research pass before building (3 parallel Explore agents: legacy-content catalog, bq-ai-functions template extraction, RESOURCES.md coverage audit) found the per-function template didn't need inventing (already documented in PLANS.md, mirrors bq-ai-functions) and, critically, that the legacy source material almost never actually plugged `ML.BUCKETIZE`/`ML.QUANTILE_BUCKETIZE`/`ML.HASH_BUCKETIZE`/`ML.ONE_HOT_ENCODER`/`ML.MULTI_HOT_ENCODER`/`ML.FEATURE_CROSS`/`ML.POLYNOMIAL_EXPAND`/`ML.NGRAMS` into a live `CREATE MODEL` — only scalers, `ML.IMPUTER`, and `ML.LABEL_ENCODER` got that treatment — and that `ML.DISTANCE`/`ML.LP_NORM`/`ML.TF_IDF`/`ML.BAG_OF_WORDS` had **zero examples anywhere in the repo**. Built in research-risk order (most legacy material first, zero-precedent groups last): `scalers` → `feature_engineering` → `encoding` → `bucketizing` → `distance` → `text`, each fully pre-validated live via `bq query` before writing notebook cells and fully pre-executed via `nbclient` (zero errors across all 6). **Real findings that go beyond the legacy source material and the existing RESOURCES.md docs, several correcting/sharpening prior wording**: `ML.STANDARD_SCALER` uses population stddev (`STDDEV_POP`), not the sample stddev a naive check would reach for — genuinely new, not previously documented anywhere; `ML.MIN_MAX_SCALER`'s prediction-time `[0,1]` capping and `ML.ROBUST_SCALER`'s outlier immunity both proven live rather than just asserted; `ML.FEATURE_CROSS`/`ML.POLYNOMIAL_EXPAND` verified to train/predict fine in `TRANSFORM` with the "not exportable" limit only biting at `EXPORT MODEL` (exact error text captured) — closing the exact gap the legacy notebooks left; **the encoder `frequency_threshold=5` default discrepancy (previously just a flagged-but-untested docs footnote) turned out to be a real, practical trap** — verified live that any category/term with fewer than 5 occurrences silently disappears into the unknown bucket under current defaults, confirmed to affect `ML.ONE_HOT_ENCODER`/`ML.LABEL_ENCODER`/`ML.MULTI_HOT_ENCODER` AND (newly discovered, since neither had ever been tested before) `ML.TF_IDF`/`ML.BAG_OF_WORDS` identically; `ML.BUCKETIZE`'s `exclude_boundaries=TRUE` clarified live to merge outer bins rather than null out-of-range values, correcting an easy docs misreading; `ML.NORMALIZER` confirmed to equal `v / ML.LP_NORM(v,p)` exactly, a concrete cross-link between two function groups. Every notebook ends with a real downstream `CREATE MODEL TRANSFORM` actually consuming that group's functions (not just standalone SQL) — the concrete way this build exceeds the legacy source material's depth. RESOURCES.md refined throughout (10+ targeted edits across the scaler/encoder/bucketizer/feature-engineering/distance/text entries); README.md's Functions table populated with all 6 rows (was `*(planned)*`). Outputs cleared for the user's own Restart & Run All; no live billable infrastructure anywhere in this phase. |
| 2026-07-23 | Phase 6 — detailed pedagogical + completeness review after the user's own run of all 6 notebooks, real gaps found and fixed in 5 of 6 | User ran all 6 notebooks and asked for a detailed review specifically checking "full scope of the functions' usage" and "great explainers" — a deeper bar than the standard correctness-only review. Ran 6 parallel review agents (one per notebook), each cross-checking every parameter documented in RESOURCES.md against what the notebook actually demonstrates, not just whether cells ran without error. **Result: all 6 notebooks were correctness-clean (zero errors, every markdown claim matched its output), but 5 of 6 had genuine completeness or teaching gaps** — parameters mentioned in RESOURCES.md but never actually exercised with a live example, or findings asserted by analogy rather than independently tested. Fixed all of them: **`feature_engineering`** — added `ML.IMPUTER`'s `most_frequent` on a numeric column (previously only shown on a string column), non-default `degree` for `ML.FEATURE_CROSS`/`ML.POLYNOMIAL_EXPAND` (previously only default `2`), and an actual `PolynomialFeatures` BigFrames cell (the markdown claimed coverage the code never delivered). **`encoding`** — added a `top_k`-capping demo isolated from `frequency_threshold` (previously `top_k` was never shown actually capping anything), extended the `frequency_threshold=5` gotcha to `ML.LABEL_ENCODER`/`ML.MULTI_HOT_ENCODER` (previously only shown for `ML.ONE_HOT_ENCODER`), added a live `NULL`-collapses-to-bucket-0 proof, and restructured Step 1 to remove a confusing unexplained column that spoiled/muddled the Step 2 gotcha reveal. **`bucketizing`** — added `ML.QUANTILE_BUCKETIZE`'s missing `bucket_ranges_json` format and a real hash-collision demo for `ML.HASH_BUCKETIZE`. **`distance`** — added a plain-language explanation of what L0/L1/L2 norms actually mean (the cell computed them with zero interpretation) and an explanation of the Step 4 result's negative cosine similarity (a beginner-confusing number left uninterpreted). **`text`** — added `ML.NGRAMS`'s `separator` parameter demo, extended the `frequency_threshold=5` gotcha to `ML.TF_IDF` directly (previously only tested for `ML.BAG_OF_WORDS`, despite RESOURCES.md attributing the finding to `ML.TF_IDF` specifically), and added a concrete side-by-side `ML.BAG_OF_WORDS`-vs-`ML.TF_IDF` contrast showing TF-IDF genuinely down-weighting a term that appears in every document. **Bonus real finding while fixing the `ML.NGRAMS` gap**: RESOURCES.md's own claim that `range` accepts "a single int `x`" meaning `[x,x]` is **wrong** — verified live that a bare `INT64` fails outright (`"Unable to coerce type INT64 to expected type ARRAY<INT64>"`); corrected both the notebook and RESOURCES.md. **`scalers`** needed only minor wording polish (no real gaps) — the only notebook of the 6 that was fully complete on the first pass. Re-validated every notebook with code changes via `nbclient` (zero errors); notebooks with markdown-only changes (`scalers`, `distance`) kept the user's original real-run outputs untouched rather than being needlessly re-run. **General lesson: a "did it run correctly" review and a "does it cover the full documented parameter surface" review are different checks — the first can pass cleanly while the second finds real gaps, since an unexercised parameter never produces an error, it just produces silence.** |
| 2026-07-23 | Phase 6 amendment (`functions/data_quality/`) + Phase 7 build-out (4 new workflows) — comprehensive plan, all 5 notebooks built and verified | User asked what would round out `bq-ml` for "full data science," then gave direct corrective feedback: data-quality/drift functions belong in `functions/`, not `workflows/` (asking "where did `ML.DESCRIBE_DATA` go?"); confirmed churn/retention and anomaly/fraud as new workflows, noting `ML.DETECT_ANOMALIES` is already covered 5x but never against real ground truth. Research confirmed both gaps were real (5 monitoring functions fully documented in RESOURCES.md but zero dedicated notebook; all 5 `ML.DETECT_ANOMALIES` demos are "mechanism only," no labeled data to check precision/recall). **Corrected a wrong research-agent finding via direct user input**: asked via `AskUserQuestion` how to source fraud ground truth (synthetic injection / external load / none); user corrected "the credit card fraud data is available inside bigquery public datasets" — confirmed live that `bigquery-public-data.ml_datasets.ulb_fraud_detection` exists natively (284,807 rows, 492 real fraud cases), avoiding both the synthetic-data compromise and external-loading complexity the research agent had missed. Entered plan mode, approved as written. Built in risk order: `functions/data_quality/` (see its own checklist entry above) → `workflows/customer_segmentation/` → `workflows/churn_retention/` → `workflows/recommendation/` → `workflows/anomaly_fraud_detection/` (checklist entries above have full per-workflow detail). **Cross-cutting practices this session**: every SQL statement pre-validated live via `bq query` before being written into a notebook cell (not just syntax-checked), including a from-scratch feature-engineering/label-definition/precision-recall pipeline for each of the 3 novel workflows; every notebook fully pre-executed via `nbclient` before handoff, with markdown re-synced to match the shipped run's actual numbers when non-deterministic training produced different results than the pre-validation pass (`customer_segmentation`'s segment percentages, `recommendation`'s cold-start/popularity overlap count). **`workflows/recommendation/` required explicit user authorization** (via `AskUserQuestion`) before creating the real-dollar-cost temporary Enterprise reservation `MATRIX_FACTORIZATION` needs — same disclosure pattern as `models/matrix_factorization/`, granted and torn down cleanly, confirmed via `bq ls --reservation` afterward. **`workflows/anomaly_fraud_detection/` surfaced a genuinely new, previously-undocumented finding**: `PCA`'s `pca_explained_variance_ratio` mode is far less reproducible than RESOURCES.md's existing "PCA is fully deterministic" claim suggested — three identical retrainings produced `ML.DETECT_ANOMALIES` true-positive counts of 3, 235, and 279 (out of 492 known fraud cases) despite `ML.EVALUATE`'s `total_explained_variance_ratio` staying bit-for-bit stable across all three; diagnosed as near-threshold eigenvalue ties flipping which components get retained; mitigated by switching to a fixed `num_principal_components` (substantially more stable, though not perfectly bit-for-bit); RESOURCES.md's PCA entry updated with this nuance rather than left as a clean "fully deterministic" claim. The notebook was rebuilt and fully re-executed a second time after this fix (each full run costing ~20 minutes of real `AUTOENCODER`/`BOOSTED_TREE_CLASSIFIER` training time). "Model validation & ensembling" remains explicitly deferred, unchecked, per the user's own request to keep it separate from this build-out. README.md's Workflows table populated with all 4 new rows plus the `functions/data_quality/` Functions-table row. |
| 2026-07-23 | Deep pedagogical + completeness review of all 5 new notebooks after the user's own run; real bugs found and fixed in 4 of 5 | User ran all 5 notebooks and asked for a deep review of whether they meet the project's "deeply teach and explain" bar. Ran 5 parallel review agents (one per notebook), each cross-checking every markdown claim against the notebook's actual live output and checking full documented-parameter coverage — same methodology as the Phase 6 review round. **Result: 4 of 5 had real, fixable issues; `functions/data_quality/` alone had a genuine completeness gap without a factual error.** Fixed all of them: **`customer_segmentation`** — MAJOR bug: the segment narrative (percentages/characteristics) was misattributed to the wrong centroid IDs versus the notebook's own displayed table, because KMEANS centroid numbering reshuffled between the pre-validation run the prose was written from and the user's actual run — resynced the narrative to the real output and added an explicit "always read the numbers from your own table, not from prose" caveat; also fixed a real, independently-confirmed visualization weakness — the frequency-vs-monetary scatter plot rendered as uninterpretable vertical stripes (`frequency` only takes small integer values) — replaced with recency-vs-monetary plus a legend, verified genuinely interpretable after a live re-run. **`churn_retention`** — a metric misattribution ("recall on the retained class" was actually recall on the churned/positive class, confirmed via the confusion-matrix math), a CM claim implying a before/after comparison that didn't exist (only one confusion matrix was ever queried), a perfunctory `ML.FEATURE_IMPORTANCE` dump with no discussion of its real disagreement with `ML.GLOBAL_EXPLAIN`'s ranking, and a missing mechanical explanation for why `roc_auc` can stay flat while accuracy/recall/F1 move (threshold-independent ranking metric vs. fixed-0.5-cutoff metrics) — all fixed via markdown-only edits, no retraining needed. **`recommendation`** — a stale "6/10 this run" claim that actually contradicted the notebook's own adjacent cell output (which showed 9) — corrected to match. **`anomaly_fraud_detection`** — a stale "TP=114-124" range that no longer bracketed the actual run's TP=132 (widened to match the already-correct 114-132 figure elsewhere in the same notebook), and a wrong "a few dozen" eval-set fraud count — resolved by retraining the supervised model once live and reading `ML.CONFUSION_MATRIX` directly (13 TP + 2 FN = 15, confirming the `.sql` file's "~15" was right all along). **`functions/data_quality`** — no factual errors, but real completeness gaps: `ML.VALIDATE_DATA_SKEW`/`DRIFT`'s `categorical_metric_type` and `thresholds` STRUCT options were documented in RESOURCES.md but never exercised live, and `ML.TFDV_VALIDATE`'s `'SKEW'` mode was never demonstrated (only `'DRIFT'`) — added live-validated cells for all three: a `categorical_metric_type` comparison showing `L_INFTY` and `JENSEN_SHANNON_DIVERGENCE` genuinely flag different columns at an identical threshold (`race`/`sex` drop out under JS while `L_INFTY` flags all three — a concrete, non-footnote demonstration of an existing RESOURCES.md caveat), a `thresholds` per-column override demo, and a `'SKEW'` mode example; also fixed a truncated `ML.TFDV_VALIDATE` print that couldn't actually show the claimed matching divergence value — now JSON-parsed and printed in full. Re-validated every code change live via `bq query` before writing, then fully re-executed `customer_segmentation` and `functions/data_quality` via `nbclient` (the two with code changes); `churn_retention`/`recommendation`/`anomaly_fraud_detection` needed markdown-only fixes, so their real user-run outputs were left untouched rather than needlessly re-executed. **General lesson, reinforcing the standing pattern from the Phase 6 review round**: non-deterministic model types (`KMEANS`, `MATRIX_FACTORIZATION`, `PCA`) make "verified: this run showed X" claims inherently fragile — every retrain is a chance for prose written against one run to silently drift from whatever the notebook's own cells actually display, and the fix is either resyncing prose to the current output every time or writing claims in a way that's explicitly robust to the centroid-ID/exact-number instability rather than hardcoding a specific run's labels. |
| 2026-07-24 | Phase 7 complete — final two workflows: `workflows/cross_validation/` + `workflows/ensembling/` | User asked to finish the last Phase 7 backlog item ("Model validation & ensembling"), then agreed it should split into two separate notebooks rather than one combined one, matching this project's one-technique-per-workflow convention. Researched both legacy notebooks in full via 2 parallel Explore agents before planning: confirmed BQML has no native k-fold CV (hand-rolled via `FARM_FINGERPRINT` hashing in the legacy notebook) and that the legacy ensemble notebook is a genuine stacking approach with a fragile cross-notebook dependency on 6 other notebooks' models (`03a`-`03f`) via `bq.list_models()`. Confirmed via direct file comparison that `models/logistic_regression/`, `models/boosted_tree_classifier/`, `models/random_forest_classifier/` already share an identical `census_adult_income` recipe — the natural self-contained foundation for ensembling. **Asked the user via `AskUserQuestion` to choose the ensembling scope** (3 fast base models vs. all 5 including the 12-46-minute-each DNN/Wide&Deep types) — user chose 3 (`LOGISTIC_REG`/`BOOSTED_TREE_CLASSIFIER`/`RANDOM_FOREST_CLASSIFIER`) for practical rebuild time. Entered plan mode, wrote and got approval on a plan covering both notebooks' dataset choices, methodology, and build order before writing any code. Checklist entries above have full per-workflow detail (fold-count/dataset choice for cross_validation; the 3-way-split leakage-avoidance design and the real row-id join bug caught live for ensembling). **Both notebooks fully pre-validated live via `bq query` before any notebook cell was written** (per standing practice), then fully pre-executed via `nbclient` — zero errors in both. Cross-validation's `LOGISTIC_REG` results reproduce bit-for-bit (deterministic); ensembling's `RANDOM_FOREST_CLASSIFIER`-derived numbers shift slightly run to run (documented non-determinism), so its comparison markdown states its finding with "~" precision rather than exact figures. This closes out Phase 7 entirely — only Phase 8 (pipelines) remains in the `bq-ml` backlog. README.md's Workflows table gained both rows. |
| 2026-07-24 | Phase 7 amendment (`workflows/ga4_churn_prediction/`) + Phase 8 kickoff research | User asked for full research and planning on Phase 8 before building anything. Confirmed `MLOps/Pipelines`/`03Tools - Pipelines Example 1/2.ipynb`/`MLOps/Serving/Batch` as the relevant existing repo material (KFP fundamentals with zero BQML content; the actual BQML-in-KFP precedent is legacy `kfp.v2`; Composer provisioning + "DAG 3" `RunPipelineJobOperator` pattern reusable from batch inference). Researched and confirmed 3 additional real orchestration paths beyond the original 5-item backlog (Cloud Workflows, Dataform, dbt) — scope amended to 8 pipelines, same "no stretch goals, every capability" pattern as prior amendments. Mid-planning, user proposed a real GA4 public dataset for a new churn workflow to be the single workflow all 8 pipelines operationalize — built `workflows/ga4_churn_prediction/` first (full detail in its own checklist entry above). Entered plan mode for the full Phase 8 scope before building; approved. |
| 2026-07-24 to 2026-07-25 | Phase 8 complete — all 8 pipeline notebooks built, reviewed, and shipped | Built in cost/complexity order per the approved plan: `sql_scripting/` → `scheduled_queries/` → `dataform/` → `cloud_workflows/` → `dbt/` → `vertex_kfp/` → `composer_airflow/` + `airflow_with_kfp/` (final two, sharing one live Cloud Composer 3 environment per explicit user request to use Composer 3 over the originally-planned Composer 2). Full per-pipeline build detail, live-verified results, and every gotcha found is recorded in each pipeline's own checklist entry above — not repeated here. **Cross-cutting findings spanning multiple pipelines, the most significant work product of this phase beyond the pipelines themselves:** (1) a major, previously-undocumented BigQuery gotcha — the query result cache can silently serve a stale `ML.EVALUATE` value after `CREATE OR REPLACE MODEL` when the before/after reads are separate top-level query jobs (found in `cloud_workflows/`, recurred in `dataform/`, proactively checked and found to need a *different* fix in `vertex_kfp/`'s KFP components, and confirmed live to NOT recur in `composer_airflow/`'s `BigQueryInsertJobOperator` — verifying per-library rather than assuming a fix generalizes was the key discipline this thread reinforced); documented in `RESOURCES.md` under `CREATE MODEL — Model Types` and `feedback_bqml_query_cache_stale_after_retrain.md`. (2) The user's mid-build standing instruction that pipeline notebooks must self-provision (enable APIs, check/grant IAM) rather than just document requirements in prose — applied consistently from `dataform/` onward. (3) Every notebook demonstrates a real, live-verified "halt downstream work on a failed quality check" mechanism appropriate to its orchestrator (`dependOnDependencyAssertions` in Dataform, `dbt build`'s native test-failure skip, `dsl.If` in KFP, `BranchPythonOperator`+`trigger_rule` in Airflow) — genuinely comparable capabilities verified independently, not assumed to transfer from one orchestrator's design to another's. (4) `composer_airflow/`+`airflow_with_kfp/` additionally surfaced real Cloud Composer 3 operational gotchas (webserver restarts periodically at floor resources, a paused DAG's manually-triggered run hangs at `queued` forever with no error, deleting an environment doesn't delete its GCS bucket) — captured in `feedback_composer3_minimal_env_gotchas.md` for reuse in any future Composer work. The real, uncommented shared-Composer-environment teardown was explicitly confirmed with the user via `AskUserQuestion` before executing, separate from the general plan approval — consistent with this project's standing practice of pausing for explicit confirmation before destructive/costly infrastructure actions. `README.md`'s Pipelines table, this file's Phase 8 checklist and Current Mapping table, and `RESOURCES.md` were all updated incrementally as each pipeline shipped, plus a final consistency pass (stale "planned" `pipelines/` subfolder list in Content Architecture corrected to the actual 8; Current Mapping backfilled with all 8 pipeline rows) once Phase 8 closed. **This closes the entire `bq-ml` backlog — Phases 1 through 8 are all complete.** |
| 2026-07-29 | Post-Phase-8 addition: `workflows/propensity_score_matching/` — first causal-inference workflow, plus a real live dataset-selection pivot | User asked whether a Propensity Score Matching deep dive would be a good, teachable addition; agreed, with explicit direction to teach the topic well (strong upfront overview, real cross-industry use cases) and find a genuine dataset, not settle for the first plausible one. Full detail (dataset trial-and-error, gotchas, verified numbers) in this phase's own checklist entry above — not repeated here. **The headline finding of this session**: two dataset candidates that looked correct on paper (`thelook_ecommerce`/GA4 marketing channels, then `bigquery-public-data.cms_synthetic_patient_data_omop`'s metformin-vs-glyburide, the textbook pharmacoepi PSM example — fully built out with a real new-user cohort design before the problem surfaced) turned out to be **schema-realistic but statistically inert** synthetic data with no real confounding or effect baked in, discovered only by directly querying and checking, not by inspecting the schema. Surfaced this to the user via `AskUserQuestion` rather than silently forcing a workaround; user chose to find real data, landing on `bigquery-public-data.samples.natality` (already used in this project for `models/automl_regressor/`), where both the confounding and the effect were verified live on the first check. **General lesson for any future workflow needing a specific treatment/confounder/outcome causal story**: verify the actual relationship exists in the candidate data via direct queries before committing to a build, even for a dataset that's schema-perfect for the story — synthetic public datasets in particular should not be assumed to encode realistic real-world relationships just because their columns suggest they should. **Second lesson from the user's own run**: the honest-finding markdown's first draft asserted a specific direction ("matching pulls the estimate toward zero, IPTW pushes it further") that didn't hold on the user's independent run — `LOGISTIC_REG`'s `AUTO_SPLIT` re-randomizes the train/eval split on every retrain, so which correction method lands closer to naive is itself non-deterministic. Fixed by resyncing to state only what's actually robust (the narrow band, the "confounders don't dominate" conclusion) with an explicit non-determinism hedge — always re-check a notebook's specific directional/numeric claims against the user's own independent run, not just the pre-validation run, before considering it done. |
| 2026-07-29 | Post-PSM addition: `workflows/survival_analysis/` — second technique BQML doesn't fully cover natively, handled with an upfront honest-limitation framing | User asked whether survival analysis (time-to-event, censoring) would fit, correctly noting BQML has no Cox Proportional Hazards model type. Explicit instruction: state the Cox limitation right up front, before building anything, name real Python options, then show what genuinely is possible natively — "let's do this honestly." Confirmed via `AskUserQuestion` that the Cox limitation section should include a small, real, working `lifelines` example, not prose-only naming. Full build detail (KM curves, discrete-time hazard reshape, the two real `lifelines` convergence errors and their fixes) in this phase's own checklist entry above. **Dataset lesson recurred a second time on a second technique**: `thelook_ecommerce` was checked first for repeat-purchase/time-to-second-order signal and again showed flat rates across every segment checked — the same "verify live, don't assume from schema" lesson from PSM's dataset search, now confirmed on a completely different technique. **A real internal-consistency lesson from this build, distinct from PSM's non-determinism lesson**: an early scratch validation of the Cox hazard ratio (done before the final notebook SQL was locked in) produced `~4.29`; once the actual notebook was fully assembled and executed end-to-end via `nbclient`, the real value was `~4.32` (displayed as "~4.3") — traced to a minor difference between the scratch cohort-build query and the final notebook's own Step 1 query (both correct and internally consistent, but not byte-identical in a way that shifted a few borderline day-boundary cases). Caught by re-reading the actual executed notebook's cell outputs before finalizing markdown claims, not by re-deriving numbers from an earlier ad-hoc check. **General lesson: only ever transcribe verified numbers from the exact, final, fully-assembled notebook's own executed output — a scratch/exploratory validation query, even one that's logically equivalent, is not a substitute for reading the real run's actual displayed numbers.** Built and fully pre-executed via `nbclient` twice (a second full run after a plot-legend-ordering polish fix); outputs cleared for handoff. |
| 2026-07-29 | `workflows/survival_analysis/` review after the user's own run + a visual enhancement round | User ran the notebook and confirmed every number/plot matched the pre-handoff `nbclient` run almost exactly (`roc_auc` 0.90102, Cox hazard ratio 4.32, concordance 0.9501 — `LOGISTIC_REG`'s `AUTO_SPLIT` barely moved anything this time, unlike PSM's larger swing) — no correctness fixes needed. User then asked specifically how visuals could play a bigger role, noting survival analysis is an unusually visual subfield. Added two `lifelines` built-ins to Step 4, essentially free off the already-fitted `CoxPHFitter`: `cph.plot(hazard_ratios=True)` (the standard Cox forest plot — HR + 95% CI per covariate, reference line at HR=1) and `cph.plot_partial_effects_on_outcome(covariates='log_n_events', values=[...])` (holds other covariates at their average and plots the model's *predicted* survival curve at a few representative activity levels — the model-based counterpart to Step 2's empirical KM-by-band chart, built from completely different math and landing on the same shape). Chose round representative values (1/10/50/200 events) over band-midpoint values after checking both live — band midpoints invited a false apples-to-apples comparison with Step 2's band-*average* curves (a point prediction at the band's rough middle understates a heavily right-skewed band's true average activity), so the notebook doesn't claim exact numeric correspondence between the two charts, only shape/direction agreement. Re-validated both plots live via a scratch Python check before adding to the notebook, then fully re-executed the whole notebook via `nbclient` a third time (zero errors); outputs cleared for handoff. Narrative extraction + manifest regenerated via the `agent-skills` tooling to pick up the new cells; all three skills re-validated clean. |
| 2026-07-29 | Post-survival_analysis addition: 4 new workflows (`price_elasticity_dml`, `uplift_cate`, `difference_in_differences`, `synthetic_control`) — verified against a real industry-currency challenge, plus a major BQML gotcha and a real live-data instability finding | User asked for more workflow ideas, then explicitly challenged whether the picks were "real approaches that are still used in industry today" rather than "antiquated" methods dressed up as easy — verified via `WebSearch` before committing to any of them, not assumed. This changed the lineup twice (multi-touch attribution swapped for Marketing Mix Modeling, naive price elasticity swapped for Double ML), and MMM itself was then researched live and dropped (no real ad-spend data exists in `bigquery-public-data`), disclosed honestly rather than shipped as a weak-signal build; user chose Synthetic Control as the replacement. Full per-workflow build detail (datasets, exact numbers, every gotcha) is in each workflow's own checklist entry above — this entry covers only the cross-cutting findings. **The most significant technical finding of this batch**: BigQuery ML's `LINEAR_REG` default `optimize_strategy='AUTO_STRATEGY'` can select iterative gradient descent that stops before converging to the true least-squares solution on small, collinear designs (found in `difference_in_differences/`'s 28-row DiD panel — silently returned a coefficient 68% too small, with `ML.TRAINING_INFO` showing the loss still decreasing at the final iteration) — `optimize_strategy='NORMAL_EQUATION'` is the fix; confirmed this does NOT generalize to larger/less-collinear designs (`price_elasticity_dml/`'s 1,130-row single-predictor regression was unaffected). **A second real, live-observed finding**: `covid19_open_data` is a still-updated public dataset — an identical staggered-TWFE query returned opposite-signed coefficients (+9.78 during planning, -7.36 in the shipped notebook) at two different points in time, confirmed not to be a bug by re-running the exact same code against the live dataset. Documented honestly in the notebook rather than picking whichever number looked better. **A recurring join-key gotcha, hit twice independently** (`price_elasticity_dml/`'s `item_number`, `uplift_cate/`'s `fullVisitorId`+`visitId`): neither was actually a safe unique key against its respective public dataset — both fixed with a synthetic `ROW_NUMBER()` `row_id`, reinforcing [[feedback_join_needs_stable_key]] a third and fourth time in this project. Every SQL statement and every headline number was pre-validated live before writing to a notebook cell, per standing practice; all 4 notebooks fully pre-executed via `nbclient` (with several notebook cells' markdown corrected post-execution to match the actual run's numbers rather than an earlier scratch check, the same discipline reinforced in `survival_analysis`'s own audit entry above). `README.md`'s Workflows table, this file's mapping table, and the `bigquery-ml` agent skill's reference/narrative/manifest were all updated in the same pass. Outputs cleared for handoff; not yet reviewed against the user's own run. |
| 2026-07-29 | Post-run review of all 4 new workflows — `price_elasticity_dml`/`uplift_cate`/`difference_in_differences` fully clean; `synthetic_control` confirms its own instability finding a third time | User ran all 4 notebooks. `price_elasticity_dml` and `uplift_cate` matched pre-handoff numbers almost exactly (naive/DML elasticity -1.4281/-0.7222, both within already-disclosed hedged ranges; uplift's numbers matched bit-for-bit, a static 2016-2017 dataset with no live-update risk). `difference_in_differences` reproduced every headline number exactly (-6.19/-19.29, the 7.45→-19.29 post-horizon progression, -7.36 naive TWFE, -31.79 Callaway-Sant'Anna) — the live `covid19_open_data` dataset held steady between the shipped run and this one. **`synthetic_control`'s unconstrained-weights finding reproduced a third time, each with a *different* extreme-weight pattern**: the user's run gave yet another distinct set (weights summing to ~+3.08, vs. ~-0.83 in the shipped notebook's own run and a third pattern from an earlier planning-time scratch check) — confirming the underdetermined 9-weeks-vs-13-donors system is genuinely unstable, not a one-off fluke. Fixed the notebook and `.sql` file to state the general pattern (some negative, some >1, never summing to 1, unstable run to run) rather than citing specific state/weight values that would keep going stale — and explicitly contrasted this with the constrained fit (Step 4), which reproduced identically (82.4% GA + 17.6% ID, -19.28 ATT) across all three runs, a genuinely nice additional demonstration of why the constrained method is the trustworthy one. Also softened a minor, real drift in `price_elasticity_dml` (154 vendors in the shipped run vs. 155 in the user's run — `iowa_liquor_sales` is itself a still-updated public dataset) to an explicit "~154, shifts by 1-2 on a rerun" hedge rather than a fixed count. **General lesson, extending the one from `survival_analysis`'s own review**: an underdetermined regression's coefficients aren't just "non-deterministic" in the usual `BOOSTED_TREE`/`KMEANS` sense — they can be arbitrarily different across runs of *identical* code on *identical* data, since the solution space itself isn't a single point. Never cite specific coefficient values from an underdetermined fit as if they were the answer; describe the qualitative pattern instead. |
